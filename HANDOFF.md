# 교과서 제작 스튜디오 핸드오프

최종 갱신: 2026-07-30

저장소: `shinyk84/textbook-studio`

작업 브랜치: `agent/supabase-collaboration`

서비스 대상: 2022 개정 교육과정 초등 체육 3~6학년 교과서 편집

## 1. 프로젝트 목적

평가원·교육부 공식 PDF를 전처리한 데이터를 공통 근거로 사용하여 다음 작업을 하나의 웹 흐름으로 연결한다.

1. 교육과정 분석
2. 개발 방향 설정
3. 성취기준·차시 배분
4. 내용·종목 선정
5. 목차·쪽수·차시 설계
6. 단원 설계
7. 원고 초안 생성·편집
8. 공식 기준에 따른 독립 모의심사
9. HWPX 내보내기와 공동 편집

핵심 운영 방식은 `AI 첫 초안 → 편집자 웹 수정 → 버전 저장·승인 → 다음 단계`이다. 제작 단계의 판단과 모의심사는 전처리 자료를 공유하지만, 모의심사는 앞 단계의 선호를 배제하고 공식 기준과 결과물만 독립적으로 평가한다.

## 2. 출발점: 공식자료 5종 전처리

원본 작업 위치는 `../criteria`, 최초 전처리 결과는 `../processed`이다. Git pull만으로 재개할 수 있도록 웹서비스가 사용하는 사본은 `official-data/processed`에 포함했다. 전처리는 최초에 `../preprocess_criteria.py`로 수행했고, 저장소 사본 갱신은 `scripts/sync_official_data.py`를 사용한다.

| 자료 | PDF 쪽수 | 청크 | 표 | 경고 쪽 | 추출 |
|---|---:|---:|---:|---:|---|
| 체육과 교육과정 | 125 | 373 | 26 | 10 | 기본 텍스트 |
| 초등학교 교육과정 | 521 | 1,662 | 99 | 43 | 기본 텍스트 |
| 체육 편찬상의 유의점 및 검정기준 | 25 | 108 | 5 | 1 | 기본 텍스트 |
| 검정도서 개발 가이드라인(평가원 검토용) | 11 | 11 | 0 | 0 | 전 쪽 OCR |
| 편수자료 II | 531 | 1,819 | 519 | 11 | 기본 텍스트 |

각 자료 폴더에는 다음 산출물이 있다.

- `document.md`: 사람이 검토하는 원문 중심 문서
- `chunks.jsonl`: AI 검색·근거 연결용 청크
- `manifest.json`: 원본 SHA-256, 쪽수, 추출 방식, 산출물 무결성
- `review.csv`: 원본 대조가 필요한 페이지와 사유
- `report.md`: 전처리 요약

전처리 원칙:

- 원문을 임의로 요약하거나 바꾸지 않는다.
- 모든 청크에 PDF 원본 페이지를 남긴다.
- 표는 Markdown 표로 분리한다.
- 글자가 부족하거나 추출이 의심스러운 페이지는 검토 대상으로 남긴다.
- 웹의 ‘공식자료 검수’ 메모는 데이터베이스에 누적되며, 원본 전처리 파일을 자동 수정하지는 않는다.
- 원본 PDF가 없는 컴퓨터에서는 `processed_only`로 표시하지만 제작 기능은 사용할 수 있다.
- 원본 PDF가 `official-data/criteria` 또는 저장소 상위 `criteria`에 있으면 원본 SHA-256까지 검증한다.

## 3. 확인된 공식 기준

- 체육 교과서 기준 분량: 책별 120쪽, 부록 포함
- 증감 범위: 기준 쪽수의 ±10% → 108~132쪽
- 기타 부속자료: 책별 최대 10쪽
- 겉표지를 제외한 가능한 전체 최대: 142쪽
- 초등 체육 3~4학년군 기준 수업 시수: 2년간 204시간
- 현재 프로젝트의 학년별 배분값: 3학년 102차시, 4학년 102차시
- 5~6학년군도 프로젝트에서 학년별 102차시로 배분
- 체육 성취기준: 총 49개(3~4학년군 23개, 5~6학년군 26개)

주의: 공식 교육과정은 3~4학년군 합계 204시간을 제시한다. 102+102는 웹서비스에서 사용하는 학년별 기본 배분값이다.

## 4. 주요 편집 결정

### 내용·목차 구조

- `영역(운동·스포츠·표현)` → 대단원명
- `세부 영역` → 대단원 부제
- `신체활동 예시의 항목` → 중단원명의 토대
- 괄호 안 세부 활동 → 소단원 내용과 새 소단원명 작성의 토대
- 한 내용·종목을 여러 학년에 공통 선정할 수 없다.
- 중단원·소단원명의 중복은 3~4학년군과 5~6학년군 모두 허용한다.
- 실제 편집 상황에 맞게 중·소단원 카드를 복제하고 수정할 수 있다.

### 원고 구조

- 소단원은 일반적으로 짝수 쪽의 좌우 펼침면 단위로 구성한다.
- 한 펼침면에는 활동 2~3개를 배치한다.
- 소단원명, 도입/본문, 활동명, 활동 목표, 활동 방법, 안전·전략·평가·도움말, 정리를 편집한다.
- 지정한 소단원의 목표 쪽수와 펼침면 수를 유지한다.
- 전체 원고를 처음부터 브라우저에 보내지 않고 `학년 → 대단원 → 중단원 → 소단원`을 선택해 한 소단원만 불러온다.
- 소단원 목록 응답은 전체 원고 응답보다 크게 축소되었고, 선택 소단원만 저장·HWPX 출력·AI 재생성한다.

### 모의심사

- 체육 검정기준 PDF 20~21쪽의 22개 심사 항목을 사용한다.
- 전체 원고, 학년 전체, 특정 쪽 범위를 선택해 심사할 수 있다.
- 앞 단계의 선택 근거나 선호 점수와 분리해 현재 결과물만 평가한다.

## 5. 현재 구현

- 1~9단계 웹 화면과 이전 단계 승인 게이트
- 공식자료 5종 연결, 원본 SHA-256 확인, 검수 상태·메모
- 성취기준 49개와 해설·원본 페이지 연결
- 학년별 102차시 및 108~132쪽 자동 검증
- 14개 세부 영역과 신체활동 예시의 목차 구조 연결
- 웹 편집, 버전 기록, 낙관적 잠금으로 동시 덮어쓰기 방지
- 선택 소단원 단위 원고 편집과 펼침면 미리보기
- OpenAI Responses API의 구조화 출력 기반 선택 소단원 생성
- 처리 중 퍼센트·진행 막대 표시
- 분석·배분·목차·설계·원고·심사 결과의 표 중심 HWPX 출력
- GitHub 저장소, Vercel Python Functions, Supabase Postgres/Auth 연결
- 관리자 승인 편집자 공동 사용, 비밀번호 재설정, 편집자 관리

주요 실행 위치:

- 로컬 시작: `start-studio.cmd` 더블클릭
- 직접 실행: `python app.py`
- 로컬 주소: `http://127.0.0.1:8000`
- 원고: `/manuscript`
- 모의심사: `/review`
- HWPX: `/export`

## 6. 데이터·배포 구조

- 로컬 데이터: `data/studio.db` SQLite
- 공통 전처리 데이터: `official-data/processed`(Git·Vercel 포함)
- 배포 데이터: `POSTGRES_URL`이 있으면 Supabase Postgres
- 인증: Supabase 이메일·비밀번호
- 배포: Vercel 프로젝트 `textbook-studio`
- 민감정보: `.env.local`과 Vercel 환경 변수에만 저장하고 Git에는 포함하지 않는다.

필요한 환경 변수 이름:

- `POSTGRES_URL`
- `SUPABASE_URL`
- `SUPABASE_PUBLISHABLE_KEY` 또는 `SUPABASE_ANON_KEY`
- `STUDIO_OWNER_EMAIL`
- `OPENAI_API_KEY`
- 선택: `OPENAI_MODEL`

## 7. 현재 확인된 문제

### OpenAI API 생성

- API 키는 로컬과 Vercel에 등록되어 서버에서 인식된다.
- 실제 호출은 OpenAI까지 도달하지만 `429 quota/billing` 오류가 확인되었다.
- 키의 `Permissions: All`은 API 기능 접근 범위이며, 사용 가능 금액을 의미하지 않는다.
- 화면에 보인 `Monthly spend $0.00`도 사용 금액이 없다는 뜻이지 무료 호출 잔액이 있다는 뜻이 아니다.
- OpenAI API 결제수단·크레딧·프로젝트 사용 한도를 확인해야 실제 생성이 된다.
- 오류는 키 불일치, 쿼터 부족, 요청 과다, 시간 초과, 네트워크 실패를 한국어로 구분해 표시하도록 구현했다.

### 운영상 확인할 사항

- 다른 컴퓨터에서는 Git pull 후 별도 전처리 폴더 복사 없이 실행할 수 있다.
- 원본 PDF 대조가 필요할 때만 `criteria`를 별도로 복사한다.
- 배포 후 Supabase의 Site URL과 Redirect URL이 실제 Vercel 주소인지 확인한다.
- 신규 편집자는 관리자가 `/editors`에 이메일을 등록한 뒤 최초 계정을 만든다.
- 전처리의 `review.csv` 경고 페이지는 원본과 계속 대조한다.
- 로컬 SQLite와 Supabase 데이터는 자동 양방향 동기화가 아니다. 필요한 경우 제공된 이전 스크립트를 명시적으로 실행한다.

## 8. Git 상태와 이력

2026-07-30 문서 작성 직전 상태:

- 브랜치: `agent/supabase-collaboration`
- 원격: `origin`
- 기존 최신 커밋: `333d05e Fix login failing on devices without saved autofill`
- 기존 작업 트리: 깨끗함

핸드오프와 푸시 기록 자동화는 `ba02d86 Add durable handoff and push logging`으로 원격 브랜치에 반영했다.

주요 커밋:

| 날짜 | 커밋 | 내용 |
|---|---|---|
| 2026-07-26 | `950c73a` | 1~9단계 초기 웹서비스 구현 |
| 2026-07-27 | `7b28a6d` | Supabase 공동 편집과 Vercel 배포 |
| 2026-07-27 | `a303ab7` | 검증된 HWPX 출력 |
| 2026-07-27 | `1e584cb` | 비밀번호 재설정 |
| 2026-07-27 | `aee2f9a` | 선택 소단원 HWPX, 진행 표시, 개발 과정 페이지, 원고·심사 개선 |
| 2026-07-27 | `dfb3a52` | HWPX 표 가독성 개선 |
| 2026-07-27 | `333d05e` | 자동완성 없는 기기의 로그인 문제 수정 |
| 2026-07-30 | `ba02d86` | 지속형 핸드오프와 푸시 전 작업 기록 Hook |

## 9. 검증 기준

코드 수정 후 기본 검증:

```powershell
python -m unittest discover -s tests -v
python -m py_compile app.py hwpx_export.py
node --check static/workflow.js
git diff --check
```

최근 전체 자동 테스트 결과: 33개 통과.

수동 검증 시 확인:

- 3~6학년 전환과 선택 상태
- 내용·종목이 두 학년에 동시에 선정되지 않는지
- 소단원 선택 시 해당 원고만 표시되는지
- 펼침면당 활동 2~3개와 쪽수 유지
- HWPX가 한글에서 열리고 표가 읽기 쉬운지
- 모의심사 범위와 22개 기준 결과
- 다른 편집자 계정 로그인과 버전 충돌 안내

## 10. 다음 작업 시작점

1. OpenAI API 결제·크레딧 설정 후 선택 소단원 실제 생성 재시험
2. 생성 결과의 문장 반복, 활동 다양성, 안전·평가 구체성 점검
3. 실제 교과서 샘플과 비교해 펼침면 편집 UI 및 HWPX 레이아웃 개선
4. 검정심사 기준과 전체 150쪽 이상 원고 처리 성능 재검증
5. 다른 컴퓨터에서 pull 후 공식자료 5종과 49개 성취기준이 바로 표시되는지 확인
6. 필요 시 최신 변경을 커밋·푸시하고 Vercel 재배포

세부 세션 이력과 최신 재개 지점은 `WORKLOG.md`에서 확인한다.

## 11. 푸시 전 자동 기록

사용자가 “깃푸시해줘”라고 요청하면 별도의 추가 지시가 없어도 다음 순서로 처리한다.

1. 현재 대화와 Git 변경 내용을 확인한다.
2. `WORKLOG.md`에 이번 작업의 목적·방법·수정 파일·검증·남은 문제·다음 시작점을 기록한다.
3. 프로젝트 전체 상태가 달라졌으면 `HANDOFF.md`도 갱신한다.
4. 기록과 코드가 함께 커밋되는지 확인한다.
5. `.githooks/pre-push` 검사를 통과한 뒤 원격 브랜치에 푸시한다.

새 컴퓨터에서 저장소를 clone/pull한 뒤 `setup-hooks.cmd`를 한 번 더블클릭하면 Hook이 활성화된다. Hook은 작업 내용을 대신 작성하지 않고, 코드만 기록 없이 푸시되는 실수를 막는다.
