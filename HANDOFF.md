# 교과서 제작 스튜디오 핸드오프

최종 갱신: 2026-08-19

저장소: `shinyk84/textbook-studio`

작업 브랜치: `main`

서비스 대상: 2022 개정 교육과정 초등 체육 3~6학년 교과서 편집

## 1. 프로젝트 목적

평가원·교육부 공식 PDF를 전처리한 데이터를 공통 근거로 사용하여 다음 작업을 하나의 웹 흐름으로 연결한다.

1. 교육과정 분석
2. 개발 방향 설정
3. 성취기준·차시 배분
4. 내용·종목 선정
5. 목차·쪽수·차시 설계
6. 단원 설계
7. 원고 초안 생성·편집
8. 공식 기준에 따른 독립 모의심사
9. HWPX 내보내기와 공동 편집

핵심 운영 방식은 `AI 첫 초안 → 편집자 웹 수정 → 버전 저장·승인 → 다음 단계`이다. 제작 단계의 판단과 모의심사는 전처리 자료를 공유하지만, 모의심사는 앞 단계의 선호를 배제하고 공식 기준과 결과물만 독립적으로 평가한다.

### 1-1. 스포츠 문화 프로토타입의 현재 생성 구조

- 교육과정이 교과서·지도서로 변환되는 공통 규칙과 초등/스포츠 문화의 분기 규칙은 `ANALYSIS_RULES.md`에 기록했다. 초등 스튜디오를 수정할 때 이 문서를 공통 엔진의 기준으로 사용한다.
- 스포츠 문화 4단계 초안 생성은 `internal-rules-v1`(외부 API 없이 규칙 조합)과 `external-ai-v1`(서버의 `OPENAI_API_KEY`로 실시간 생성) 두 공급자 중 화면의 `생성 제공자` 드롭다운에서 선택한다. `internal-rules-v1`은 전처리 자료의 교육과정·인정기준·출판사 교과서·지도서 식별자를 근거로 조합하고, `external-ai-v1`은 같은 근거를 서버로 보내 매번 실제 문장을 새로 쓴다(2026-08-14 추가, 아래 참고).
- (2026-08-14) 생성기는 소단원 제목을 `static/sports-culture-evidence.js`(4개 출판사 22개정 교과서·지도서 전처리 원문에서 `scripts/build_sports_culture_evidence.py`가 소단원별로 뽑아낸 근거 인덱스)와 매칭해 실제 근거(출판사·자료 유형·원본 쪽수·발췌문)를 원고에 연결한다. 소단원 제목이 근거와 매칭되지 않으면 범용 문장으로 때우지 않고 생성을 오류로 중단한다. 대단원 도입·마무리·특별 페이지처럼 원래 소단원 단위 근거가 없는 범위만 범용 출처로 대체하며, 이 경우 결과에 `matchedSmallUnit: false`가 표시된다.
- (2026-08-14) `internal-rules-v1`은 규칙 조합기라서 스타일·종목·소단원명이 자유롭게 바뀌어도 실제 자연스러운 문장까지는 못 따라간다는 한계가 재확인되어(사용자가 실제 미래엔 교과서 서술과 직접 비교해 지적함), `external-ai-v1` 공급자를 신설했다. 근거 매칭은 여전히 클라이언트 JS(`evidenceRecordFor` 등)가 맡고, 서버(`POST /api/prototype/sports-culture-manuscript`, `app.py`의 `call_openai_for_sports_culture_manuscript`)는 클라이언트가 조립해 보낸 소단원·종목·전체 스타일 값·전개각도·근거 발췌문을 받아 실제 문장만 새로 쓴다. 대단원 도입·마무리·특별 페이지도 같은 경로로 연결되어 있다(2026-08-19부터, 아래 참고). 사용 전 프로젝트 루트에 `.env.local`(로컬)·Vercel 프로젝트 환경변수(배포본)에 각각 `OPENAI_API_KEY`(결제수단 연결된 키)가 필요하다. 배포본 접속은 랜덤 해시가 붙은 배포 고정 URL이 아니라 `https://textbook-studio.vercel.app`(정식 도메인)로 해야 최신 배포·환경변수가 반영된다.
- (2026-08-19) `external-ai-v1`의 응답 스키마에서 펼침면 하단 시각자료가 페이지당 고정 3개(`visual_briefs`)였던 것을 없애고, 페이지별 `left_visuals`/`right_visuals`(각 0~3개, 항목마다 `size: full|half|small`, `placement`, `description`)로 바꿨다. 실제 전처리 교과서 콘택트시트를 확인해 보면 펼침면마다 삽화 구성이 크게 다르므로(전면 일러스트 1장 vs 작은 컷 여러 개 vs 삽화 없음), AI가 텍스트를 쓰는 같은 호출에서 그 펼침면 내용에 맞는 삽화 개수·크기·배치도 함께 판단한다. 화면 렌더링은 `manuscript.visuals` 존재 여부로 새 유연한 레이아웃과 내부 조합기의 기존 고정 3박스 레이아웃을 구분해 그린다. 실제 이미지 생성은 같은 날 뒤이어 구현됨(아래 참고).
- (2026-08-19) 삽화 자리에 실제 이미지를 채우는 기능을 추가했다. 4단계 "삽화 이미지" 체크박스(기본 꺼짐, `state.includeImages`)를 켜면 AI 실시간 생성 시 `POST /api/prototype/sports-culture-image`(`app.py`의 `call_openai_for_sports_culture_image`, OpenAI `gpt-image-2`)를 각 시각 요소마다 호출해 실제 그림을 붙인다. 체크박스를 끄면 기존처럼 설명 문구 placeholder만 나온다(비용 없음). 이미지 한 장이 실패해도 그 자리만 문구로 남고 나머지 생성은 계속됨. 교과서 원고 TXT 내보내기(`textbookManuscriptText`)의 "사진·삽화 발주" 목록도 이 새 `visuals` 구조를 반영하도록 함께 고쳤다. **PPT 다운로드는 아직 이 원고 구조를 전혀 반영하지 않고 더 오래된 활동 카드 구조를 쓴다 — 별도로 다시 만들어야 함(미착수).**
- 사용자는 체제와 매개 종목을 정한다. 생성 결과는 성취기준 6개 중 해당 기준, 탐구·직접 참여·간접 참여·창작·진로 등의 펼침면 유형, 근거 자료, 지도서 파생안을 함께 가진다.
- 기존 브라우저 저장 데이터에는 누락된 성취기준 코드와 내부 공급자 설정만 보완하며 사용자가 정한 단원·시수 값은 덮어쓰지 않는다.
- 3단계 단원 구성 초기안은 4개사 전처리 자료의 공통 내용 축을 반영해 `2개 대단원 → 6개 중단원 → 18개 소단원`으로 세분화했다. 32회, 본문 176쪽과 부록 4쪽 합계는 유지한다. 기존 6개 기본안이 수정되지 않았으면 자동 전환하고, 편집 이력이 있으면 화면의 `18개 소단원 초기안 적용` 버튼으로 사용자가 명시적으로 교체한다.
- 소단원 카드는 구성 근거와 성취기준을 위쪽에 표시하고, `소단원명`을 라벨이 있는 넓은 입력 영역으로 분리해 긴 제목이 잘리지 않도록 했다.
- 넓은 화면에서는 소단원명·차시·쪽수·이동/삭제/복제 조작을 한 줄에 배치하고 입력 상자와 버튼 높이를 42px로 통일했다. 좁은 화면에서는 조작 버튼만 다음 줄로 내려간다.
- 적용 종목 선택은 4단계의 숨은 단일 입력에서 3단계로 옮겼다. 전처리 교과서 기반 12개 후보를 복수 선택하거나 기타 종목을 입력할 수 있으며, 선택 결과는 4단계 내부 초안 생성의 `carrierSport` 입력으로 자동 전달된다.
- 4단계는 소단원 선택 시 `이론·인문 탐구형`, `문학·예술 감상형`, `종목 직접 참여형`, `대회 프로젝트형`, `융합·진로형` 중 하나를 자동 추천한다. 편집자는 주 유형과 선택적 보조 유형을 바꿀 수 있고 내부 생성기는 유형별 펼침면 순서를 적용한다.
- 스포츠 문화 생성 결과는 초등식 `활동 1·2` 카드 전체를 바로 펼치지 않는다. 체제 3안을 가로로 놓고 각 체제의 펼침면을 실제 Canvas 이미지 와이어프레임으로 먼저 보여 준다. 체제별 펼침면은 가로 스크롤, 크게 보기, 개별 PNG 저장을 지원하며 원고·지도서·근거 텍스트는 접힌 상세 영역에서 확인한다.
- 생성기는 비동기 공급자 등록 계약(`registerProvider`, `generateDraftSet`)을 사용한다. 향후 외부 API는 서버에서 인증 정보를 관리하는 별도 공급자로 추가하고, 화면과 결과 스키마는 그대로 유지한다.
- 5단계 PDF 모의심사는 아직 기존 OpenAI 연동 기능이다. 현재 내부 데이터 생성 전환 범위는 4단계 교과서·지도서 초안까지다.

## 2. 출발점: 공식자료·교과서·지도서 29종 전처리

원본 작업 위치는 `../criteria`, 최초 전처리 결과는 `../processed`이다. Git pull만으로 재개할 수 있도록 웹서비스가 사용하는 사본은 `official-data/processed`에 포함했다. 전처리는 최초에 `../preprocess_criteria.py`로 수행했고, 저장소 사본 갱신은 `scripts/sync_official_data.py`를 사용한다.

| 자료 | PDF 쪽수 | 청크 | 표 | 경고 쪽 | 추출 |
|---|---:|---:|---:|---:|---|
| 체육과 교육과정 | 125 | 373 | 26 | 10 | 기본 텍스트 |
| 초등학교 교육과정 | 521 | 1,662 | 99 | 43 | 기본 텍스트 |
| 체육 편찬상의 유의점 및 검정기준 | 25 | 108 | 5 | 1 | 기본 텍스트 |
| 검정도서 개발 가이드라인(평가원 검토용) | 11 | 11 | 0 | 0 | 전 쪽 OCR |
| 편수자료 II | 531 | 1,819 | 519 | 11 | 기본 텍스트 |
| 초·중등학교 교육과정 총론 | HWP 2구역 | 24 | - | - | HWP 구조 텍스트 |
| 고등학교 교육과정 | HWP 7구역 | 1,183 | - | - | HWP 구조 텍스트 |
| 인정도서 편찬상의 유의점 및 인정기준 | HWP 2구역 | 71 | - | - | HWP 구조 텍스트 |
| 인정도서 심의본 접수·수수료 안내 | HWP 2구역 | 10 | - | - | HWP 구조 텍스트 |
| 인정도서 심의본 접수 질의응답 | HWPX 1구역 | 2 | - | - | HWPX XML 텍스트 |
| 2009 개정 체육과 교육과정 | 158 | 158 | 0 | 0 | PDF 페이지별 텍스트 |
| 2009 개정 체육과 교육과정 최종수정본 | 147 | 145 | 0 | 2 | PDF 페이지별 텍스트 |
| 2009 개정 초·중등학교 교육과정 총론 | 31 | 30 | 0 | 1 | PDF 페이지별 텍스트 |
| 2009 개정 YBM 스포츠 문화 교과서 | 256 | 338 | - | 펼침면 126쌍 | 페이지·펼침면·이미지 |
| 2009 개정 YBM 스포츠 문화 지도서 | 368 | 1,478 | - | 펼침면 183쌍 | 페이지·펼침면·이미지 |
| 2022 개정 천재 스포츠 문화 교과서 | PDF 89(물리 176) | 200 | - | - | 페이지·펼침면·이미지 |
| 2022 개정 천재 스포츠 문화 지도서 | 144 | 182 | - | 펼침면 1쌍 | 페이지·펼침면·이미지 |
| 2022 개정 지학사 스포츠 문화 교과서 | 196 | 210 | - | 펼침면 5쌍 | 페이지·펼침면·이미지 |
| 2022 개정 지학사 스포츠 문화 지도서 | 224 | 332 | - | 펼침면 34쌍 | 페이지·펼침면·이미지 |
| 2022 개정 동아출판 스포츠 문화 교과서 | 196 | 257 | - | 펼침면 18쌍 | 페이지·펼침면·이미지 |
| 2022 개정 동아출판 스포츠 문화 지도서 | 288 | 649 | - | 펼침면 20쌍 | 페이지·펼침면·이미지 |
| 2022 개정 미래엔 스포츠 문화 교과서 | 198 | 262 | - | 펼침면 15쌍 | 페이지·펼침면·이미지 |
| 2022 개정 미래엔 스포츠 문화 지도서 | 192 | 343 | - | 펼침면 25쌍 | 페이지·펼침면·이미지 |
| 2022 개정 YBM 초등 체육 5 교과서 | 144 | 142 | - | 텍스트 검토 8쪽 | 페이지·펼침면·이미지 |
| 2022 개정 YBM 초등 체육 5 지도서 | 292 | 298 | - | 텍스트 검토 2쪽 | 페이지·펼침면·이미지 |
| 2022 개정 동아출판 초등 체육 5 교과서 | 146 | 145 | - | 텍스트 검토 3쪽 | 페이지·펼침면·이미지 |
| 2022 개정 동아출판 초등 체육 5 지도서 | 384 | 392 | - | 텍스트 검토 1쪽 | 페이지·펼침면·이미지 |
| 2022 개정 천재교과서 초등 체육 5 교과서 | 142 | 136 | - | 텍스트 검토 6쪽 | 페이지·펼침면·이미지 |
| 2022 개정 천재교과서 초등 체육 5 지도서 | 354 | 355 | - | 텍스트 검토 2쪽 | 페이지·펼침면·이미지 |

각 자료 폴더에는 다음 산출물이 있다.

- `document.md`: 사람이 검토하는 원문 중심 문서
- `chunks.jsonl`: AI 검색·근거 연결용 청크
- `manifest.json`: 원본 SHA-256, 쪽수, 추출 방식, 산출물 무결성
- `review.csv`: 원본 대조가 필요한 페이지와 사유
- `report.md`: 전처리 요약

전처리 원칙:

- 원문을 임의로 요약하거나 바꾸지 않는다.
- PDF 청크에는 원본 페이지를 남긴다. HWP/HWPX는 문서 구역과 문단 순서를 보존하고 최종 인용 시 원본 쪽수를 대조한다.
- 표는 Markdown 표로 분리한다.
- 글자가 부족하거나 추출이 의심스러운 페이지는 검토 대상으로 남긴다.
- 웹의 ‘공식자료 검수’ 메모는 데이터베이스에 누적되며, 원본 전처리 파일을 자동 수정하지는 않는다.
- 모든 manifest와 검색 청크는 `curriculum_revision` 값(`2009 개정` 또는 `2022 개정`)으로 세대를 구분한다. 교과서·지도서 전처리 폴더명도 `09개정_`/`22개정_` 접두사를 사용한다.
- 교과서·지도서는 `pages.jsonl`, `spreads.jsonl`, `spread_chunks.jsonl`과 페이지·펼침면 미리보기 이미지를 추가로 보존한다.
- 초등 체육 5학년 비교 자료는 manifest에서 `education_level: 초등학교`, `grade: 5학년`, `curriculum_band: 5~6학년군`, `review_framework: 검정`으로 구분한다. 5학년 한 권의 구성을 5~6학년군 전체 교육과정과 동일시하지 않고, 출판사가 학년군 내용을 5학년에 어떻게 배분했는지 분석한다.
- 원본 PDF가 없는 컴퓨터에서는 `processed_only`로 표시하지만 제작 기능은 사용할 수 있다.
- 원본 PDF가 `official-data/criteria`, `official-data/textbook`, `official-data/teacher_guide` 또는 저장소 상위 `criteria`에 있으면 원본 SHA-256까지 검증한다.
- `22개정 씨마스 스포츠 문화` 원본은 `official-data/textbook`에 있으나 현재 저장소 규격의 전처리 산출물은 아직 없다.

개정 비교 분석 범위는 사용자의 확인에 따라 `2009 개정 ↔ 2022 개정`으로 한정한다. 2015 개정 스포츠 문화 개발물은 비교 대상에 포함하지 않는다.

초등 체육 5학년 3개사 1차 구조 대조에서는 교과서가 공통으로 `운동·스포츠·표현` 대영역을 사용하고, 스포츠는 `기술형·전략형·생태형`, 표현은 `스포츠·전통·현대 표현`으로 조직하는 패턴이 확인되었다. 지도서는 `교육과정·수업·평가·연간 계획·편찬 방향`의 총론과 `영역 개관·차시 지도·평가·자료`의 각론 구조를 공통으로 사용한다. 지도서 총론에 5~6학년군 26개 성취기준 전체가 들어 있으므로 5학년 실제 배분 분석은 문서 전체 코드 출현 수가 아니라 각론의 차시별 성취기준 연결을 기준으로 해야 한다.

## 3. 확인된 공식 기준

- 체육 교과서 기준 분량: 책별 120쪽, 부록 포함
- 증감 범위: 기준 쪽수의 ±10% → 108~132쪽
- 기타 부속자료: 책별 최대 10쪽
- 겉표지를 제외한 가능한 전체 최대: 142쪽
- 초등 체육 3~4학년군 기준 수업 시수: 2년간 204시간
- 현재 프로젝트의 학년별 배분값: 3학년 102차시, 4학년 102차시
- 5~6학년군도 프로젝트에서 학년별 102차시로 배분
- 체육 성취기준: 총 49개(3~4학년군 23개, 5~6학년군 26개)
- 고등학교 「스포츠 문화」 인정도서 기준 분량: 부록 포함 180쪽, ±10% → 162~198쪽
- 고등학교 「스포츠 문화」는 진로 선택 과목이며 기본 2학점(50분 수업 32차시), 1학점 범위에서 감축 편성 가능
- 「스포츠 문화」 교육과정은 ‘스포츠 인문 문화’와 ‘스포츠 경기 문화’ 2개 영역, 성취기준 6개로 구성

주의: 공식 교육과정은 3~4학년군 합계 204시간을 제시한다. 102+102는 웹서비스에서 사용하는 학년별 기본 배분값이다.

## 4. 주요 편집 결정

### 내용·목차 구조

- `영역(운동·스포츠·표현)` → 대단원명
- `세부 영역` → 대단원 부제
- `신체활동 예시의 항목` → 중단원명의 토대
- 괄호 안 세부 활동 → 소단원 내용과 새 소단원명 작성의 토대
- 한 내용·종목을 여러 학년에 공통 선정할 수 없다.
- 중단원·소단원명의 중복은 3~4학년군과 5~6학년군 모두 허용한다.
- 실제 편집 상황에 맞게 중·소단원 카드를 복제하고 수정할 수 있다.

### 원고 구조

- 소단원은 일반적으로 짝수 쪽의 좌우 펼침면 단위로 구성한다.
- 한 펼침면에는 활동 2~3개를 배치한다.
- 소단원명, 도입/본문, 활동명, 활동 목표, 활동 방법, 안전·전략·평가·도움말, 정리를 편집한다.
- 지정한 소단원의 목표 쪽수와 펼침면 수를 유지한다.
- 전체 원고를 처음부터 브라우저에 보내지 않고 `학년 → 대단원 → 중단원 → 소단원`을 선택해 한 소단원만 불러온다.
- 소단원 목록 응답은 전체 원고 응답보다 크게 축소되었고, 선택 소단원만 저장·HWPX 출력·AI 재생성한다.

### 모의심사

- 체육 검정기준 PDF 20~21쪽의 22개 심사 항목을 사용한다.
- 전체 원고, 학년 전체, 특정 쪽 범위를 선택해 심사할 수 있다.
- 앞 단계의 선택 근거나 선호 점수와 분리해 현재 결과물만 평가한다.

## 5. 현재 구현

`/prototype.html`의 5단계(프로젝트 설정 → 교육과정 확인 → 단원·차시 배분 → 체제안 선택 → 모의심사) 최소비용 프로토타입은 좌측 `책 프로젝트` 드롭다운에서 교육과정의 체육 관련 교과를 직접 선택한다. 목록은 카탈로그 항목 9개 — 초등 체육 3~4학년군·5~6학년군, 중등 체육, 고등 체육(체육1·체육2 통합), 고등 운동과 건강·스포츠 문화·스포츠 과학·스포츠 생활1·스포츠 생활2 — 이다. **초등 3~4/5~6학년군, 고등 체육(체육1+체육2), 중등 체육**은 모두 같은 "분권 프로젝트"(책 프로젝트 1개 안에 책 2권을 두고 나란히 비교) 구조를 쓰고, 나머지 고등 5과목은 단권 구조다. 초등 두 학년군과 고등 7과목(체육1·2 포함)은 쪽수 공식자료가 연결되어 선택 즉시 1단계 쪽수·차시가 자동 입력된다. 중등 체육은 성취기준 51개·1~3학년 272시간은 공식자료로 연결됐지만 교과서 쪽수 공식 기준이 아직 없어, 1단계에 빨간 "공식 쪽수 자료 미연결" 배너를 띄우되 **책별 쪽수·차시를 직접 입력할 수 있게 하고**(두 책 합계가 272차시가 되어야 함만 검증), 2단계에서도 51개 성취기준을 초·고등과 같은 형식으로 정식 표시한다. 기존 단일 `localStorage` 데이터와 이번 세션 이전의 구형 분권 구조(고등체육1·2가 별개 프로젝트였던 상태 등)는 `migratePairedCatalogProjects`가 자동으로 새 구조로 병합한다.

분량 정책은 `scripts/build_prototype_curriculum_data.py`가 전처리 문서에서 찾아 `prototype-curriculum-data.js`에 생성한다. 1단계에서 초등 체육은 120쪽·108~132쪽·부속자료 0~10쪽, 고등 체육 7과목은 교과서·부록 180쪽·162~198쪽을 카드형 통계 그리드(교과서·부록 기준/부속자료/시수 등 각각 박스, 숫자 강조)로 표시한다. 고등 부속자료는 공식 상한 미확인 상태라 0쪽을 기본으로 하되 편집 중에는 임시 수정할 수 있고 팀장 승인 후 잠긴다. 고등 기본 수업량은 체육1·체육2·운동과 건강·스포츠 생활1·2가 48차시, 스포츠 문화·스포츠 과학이 32차시다. 이는 정적 `localStorage` 프로토타입이며 기존 운영 서비스의 1~9단계·DB·로그인 구조를 변경한 것은 아니다.

근거 표시는 자료명·PDF 원본 쪽·문서 인쇄면·추출 원문을 함께 제공한다. PDF 기반 교육과정과 편찬기준은 성취기준·해설 단위까지 원본 쪽수를 표시한다. 인정기준 HWP는 자동 전처리에서 과목별 페이지 경계가 보존되지 않으므로 임의 값을 만들지 않고, 원본 목차에서 확인되는 고등학교 체육 전체 범위 20~45쪽과 과목별 소제목 대조 안내를 표시한다. 고등학교 「스포츠 문화」는 신체활동 예시표(&lt;표 2&gt;) 자체는 있으나 종목명이 "스포츠 종목 중 택 1 이상"으로만 적혀 있어 구체적 종목명이 없다는 사실을 정확히 안내한다(예전엔 "표가 없다"고 잘못 안내했던 것을 수정함).

1단계 프로젝트 설정은 제작 프로필별로 다르게 렌더링한다. 분권형(초등 학년군·중등체육·고등체육)은 공통 정보와 책별 쪽수·부속자료·차시를 한 화면에 표시하며, 초등은 두 책 차시 합계 204, 중등체육은 두 책 합계 272를 검증한다(고등체육1·2는 각 책을 독립적으로 검증, 합계 제약 없음 — 공식 근거가 없어 임의로 만들지 않음). 나머지 고등 5과목은 선택과목별 단권형으로 쪽수·학점·차시를 표시한다. 프로젝트 설정은 `편집 중 → 팀장 승인 대기 → 승인 완료` 상태를 가지며 승인본을 잠그고 수정본을 시작해 재승인할 수 있다. 현재 승인 버튼은 로그인 권한이 연결되지 않은 로컬 프로토타입 시뮬레이션이다.

프로젝트 설정의 쪽수·차시 근거는 하나의 `쪽수·차시 근거 한 번에 보기` 카드로 열리며, PC에서는 좌우 2단, 760px 이하에서는 위아래 1단으로 표시된다(펼침 상태는 재렌더링에도 유지됨). 앞부속·뒷부속·부록 쪽수 입력 카드는 3단계 단원 목록 위쪽에 3단 그리드로 모여 있다(760px 이하에서 3단으로 쌓임). 배열표 미리보기는 기존 고정 팝업을 유지하되 헤더와 `닫기 · Esc` 버튼이 내부 스크롤 중에도 상단에 고정되고 Esc 키로 닫을 수 있으며, 성취기준·목차·배열표를 모두 CSV로 다운로드할 수 있다(엑셀에서 바로 열림; 실제 HWPX는 서버·외부 라이브러리 전용이라 이 서버 없는 프로토타입에서는 재현하지 않음).

**4단계는 선택 소단원의 체제안 비교, 5단계는 완성 원고 PDF의 독립 모의심사다.** 4단계에서 소단원을 고르고 "3개 체제 초안 생성"을 누르면 균형형·활동강화형·창의형 3개가 한 번에 나와 3열로 비교되고(재생성하면 최신 3개로 교체), 그중 하나를 "이 체제로 승인"할 수 있다. 5단계는 앞 단계의 선호와 무관하게 별도 PDF를 업로드해 평가한다. 초등 체육은 체육과 편찬상의 유의점 및 **검정기준 22개**, 고등학교 체육 인정도서는 서울교육연구정보원 자료의 **인정기준 20개(교육과정 20점·선정/조직 30점·정확성/공정성 20점·활동/평가 30점)**를 선택 프로젝트에 따라 자동 구분한다. 중등 체육은 공식 심사기준 자료가 아직 연결되지 않아 채점 기능을 잠근다. 채점은 서버의 `OPENAI_API_KEY`를 사용하는 실제 API 기능이며, 키 미설정·결제 한도·스캔 PDF 등 실패 사유를 업로드 영역 아래에 계속 표시한다.

4단계의 생성 이력은 **회차 단위 아코디언**으로 표시된다(체크박스로 여러 회차를 선택해 PPT로 각각 따로 다운로드하거나 한 번에 삭제할 수 있고, 기본은 모두 접힌 채로 방금 생성한 회차만 펼쳐짐). 생성 결과는 각 체제별로 "PPT 다운로드" 버튼을 눌러 펼침면 구조(왼쪽 쪽=소단원명·본문·활동1, 오른쪽 쪽=나머지 활동·마무리)를 반영한 `.pptx`로도 받을 수 있다(`pptxgen.bundle.js`를 정적으로 내장, 외부 CDN 미사용). 원고 생성 자체는 여전히 시뮬레이션이다 — 개인 API 키로 실제 OpenAI/Gemini/Claude를 호출하는 기능을 한 번 시도했으나 Gemini 인증 문제로 삭제했다(7절 참고).

- 1~9단계 웹 화면과 이전 단계 승인 게이트
- 공식자료 5종 연결, 원본 SHA-256 확인, 검수 상태·메모
- 성취기준 49개와 해설·원본 페이지 연결
- 학년별 102차시 및 108~132쪽 자동 검증
- 14개 세부 영역과 신체활동 예시의 목차 구조 연결
- 웹 편집, 버전 기록, 낙관적 잠금으로 동시 덮어쓰기 방지
- 선택 소단원 단위 원고 편집과 펼침면 미리보기
- OpenAI Responses API의 구조화 출력 기반 선택 소단원 생성
- 처리 중 퍼센트·진행 막대 표시
- 분석·배분·목차·설계·원고·심사 결과의 표 중심 HWPX 출력
- GitHub 저장소, Vercel Python Functions, Supabase Postgres/Auth 연결
- 관리자 승인 편집자 공동 사용, 비밀번호 재설정, 편집자 관리

주요 실행 위치:

- 로컬 시작: `start-studio.cmd` 더블클릭
- 직접 실행: `python app.py`
- 로컬 주소: `http://127.0.0.1:8000`
- 원고: `/manuscript`
- 모의심사: `/review`
- HWPX: `/export`

## 6. 데이터·배포 구조

- 로컬 데이터: `data/studio.db` SQLite
- 공통 전처리 데이터: `official-data/processed`(Git·Vercel 포함)
- 배포 데이터: `POSTGRES_URL`이 있으면 Supabase Postgres
- 인증: Supabase 이메일·비밀번호
- 배포: Vercel 프로젝트 `textbook-studio`
- 민감정보: `.env.local`과 Vercel 환경 변수에만 저장하고 Git에는 포함하지 않는다.

필요한 환경 변수 이름:

- `POSTGRES_URL`
- `SUPABASE_URL`
- `SUPABASE_PUBLISHABLE_KEY` 또는 `SUPABASE_ANON_KEY`
- `STUDIO_OWNER_EMAIL`
- `OPENAI_API_KEY`
- 선택: `OPENAI_MODEL`

## 7. 현재 확인된 문제

### OpenAI API 생성

- API 키는 로컬과 Vercel에 등록되어 서버에서 인식된다.
- 실제 호출은 OpenAI까지 도달하지만 `429 quota/billing` 오류가 확인되었다.
- 키의 `Permissions: All`은 API 기능 접근 범위이며, 사용 가능 금액을 의미하지 않는다.
- 화면에 보인 `Monthly spend $0.00`도 사용 금액이 없다는 뜻이지 무료 호출 잔액이 있다는 뜻이 아니다.
- OpenAI API 결제수단·크레딧·프로젝트 사용 한도를 확인해야 실제 생성이 된다.
- 오류는 키 불일치, 쿼터 부족, 요청 과다, 시간 초과, 네트워크 실패를 한국어로 구분해 표시하도록 구현했다.

### 프로토타입 개인 API 키 연동 시도와 삭제

- 프로토타입(`/prototype.html`)에 사용자 개인 API 키(OpenAI/Gemini/Claude)로 실제 생성을 시도하는 기능을 한 번 만들었으나, `aistudio.google.com/apikey`에서 발급받은 Gemini 키가 예상한 `AIzaSy...` 형식이 아니라 `AQ....` 형식이었고, 쿼리 파라미터(`?key=`)·`x-goog-api-key` 헤더·`Authorization: Bearer` 세 가지 인증 방식을 모두 시도했지만 매번 구글이 401/403과 함께 "Request had invalid authentication credentials. Expected OAuth 2 access token..."을 반환해 원인을 찾지 못함.
- 사용자가 이 기능 자체를 삭제하기로 결정해 관련 코드(`app.py`의 중계 엔드포인트, `static/prototype.js`의 키 입력·자동 인식 로직)를 전부 제거함 — 현재 프로토타입은 다시 시뮬레이션 전용이다.
- 나중에 다시 시도한다면 참고할 것: (1) `curl`로 직접 재현해 정확한 상태코드·본문 확인, (2) Google 공식 Python SDK(`google-genai`)로 같은 키를 호출해 REST 직접 호출과 차이가 있는지 대조, (3) 다른 개인 Google 계정에서 새로 키를 발급받아 `AIzaSy...` 형식이 나오는지 비교.
- 이 문제는 아래 "OpenAI API 생성"(운영 서비스의 서버 환경변수 기반 기존 기능)과는 완전히 무관하다 — 그쪽은 결제·크레딧만 설정하면 별도로 동작 가능하다.

### 운영상 확인할 사항

- 다른 컴퓨터에서는 Git pull 후 별도 전처리 폴더 복사 없이 실행할 수 있다.
- 원본 PDF 대조가 필요할 때만 `criteria`를 별도로 복사한다.
- 배포 후 Supabase의 Site URL과 Redirect URL이 실제 Vercel 주소인지 확인한다.
- 신규 편집자는 관리자가 `/editors`에 이메일을 등록한 뒤 최초 계정을 만든다.
- 전처리의 `review.csv` 경고 페이지는 원본과 계속 대조한다.
- 로컬 SQLite와 Supabase 데이터는 자동 양방향 동기화가 아니다. 필요한 경우 제공된 이전 스크립트를 명시적으로 실행한다.

## 8. Git 상태와 이력

2026-07-30 문서 작성 직전 상태:

- 브랜치: `agent/supabase-collaboration`
- 원격: `origin`
- 기존 최신 커밋: `333d05e Fix login failing on devices without saved autofill`
- 기존 작업 트리: 깨끗함

핸드오프와 푸시 기록 자동화는 `ba02d86 Add durable handoff and push logging`으로 원격 브랜치에 반영했다.

주요 커밋:

| 날짜 | 커밋 | 내용 |
|---|---|---|
| 2026-07-26 | `950c73a` | 1~9단계 초기 웹서비스 구현 |
| 2026-07-27 | `7b28a6d` | Supabase 공동 편집과 Vercel 배포 |
| 2026-07-27 | `a303ab7` | 검증된 HWPX 출력 |
| 2026-07-27 | `1e584cb` | 비밀번호 재설정 |
| 2026-07-27 | `aee2f9a` | 선택 소단원 HWPX, 진행 표시, 개발 과정 페이지, 원고·심사 개선 |
| 2026-07-27 | `dfb3a52` | HWPX 표 가독성 개선 |
| 2026-07-27 | `333d05e` | 자동완성 없는 기기의 로그인 문제 수정 |
| 2026-07-30 | `ba02d86` | 지속형 핸드오프와 푸시 전 작업 기록 Hook |

## 9. 검증 기준

코드 수정 후 기본 검증:

```powershell
python -m unittest discover -s tests -v
python -m py_compile app.py hwpx_export.py
node --check static/workflow.js
git diff --check
```

최근 전체 자동 테스트 결과: 33개 통과.

수동 검증 시 확인:

- 3~6학년 전환과 선택 상태
- 내용·종목이 두 학년에 동시에 선정되지 않는지
- 소단원 선택 시 해당 원고만 표시되는지
- 펼침면당 활동 2~3개와 쪽수 유지
- HWPX가 한글에서 열리고 표가 읽기 쉬운지
- 모의심사 범위와 22개 기준 결과
- 다른 편집자 계정 로그인과 버전 충돌 안내

## 10. 다음 작업 시작점

1. OpenAI API 결제·크레딧 설정 후 선택 소단원 실제 생성 재시험
2. 생성 결과의 문장 반복, 활동 다양성, 안전·평가 구체성 점검
3. 실제 교과서 샘플과 비교해 펼침면 편집 UI 및 HWPX 레이아웃 개선
4. 검정심사 기준과 전체 150쪽 이상 원고 처리 성능 재검증
5. 다른 컴퓨터에서 pull 후 공식자료 5종과 49개 성취기준이 바로 표시되는지 확인
6. 필요 시 최신 변경을 커밋·푸시하고 Vercel 재배포
7. (프로토타입) 5단계 소단원 목록이 많을 때(초등 41개 등) 아코디언을 하나씩 여는 게 불편하다는 지적 — 필터(담당자별·미완료만)·전체 펼치기/접기 등 UX 개선 여부 확정
8. (프로토타입) 여러 명이 같은 초안을 각자 검토하는 방법 — 실시간 동시편집은 서버가 없어 불가능하고 운영 서비스도 실시간이 아님(저장 시점 버전 충돌 검사)을 확인함. 상태를 파일로 내보내기/가져오기하는 대안을 논의만 하고 구현하지 않았음 — 필요하면 다음에 진행

세부 세션 이력과 최신 재개 지점은 `WORKLOG.md`에서 확인한다.

## 11. 푸시 전 자동 기록

사용자가 “깃푸시해줘”라고 요청하면 별도의 추가 지시가 없어도 다음 순서로 처리한다.

1. 현재 대화와 Git 변경 내용을 확인한다.
2. `WORKLOG.md`에 이번 작업의 목적·방법·수정 파일·검증·남은 문제·다음 시작점을 기록한다.
3. 프로젝트 전체 상태가 달라졌으면 `HANDOFF.md`도 갱신한다.
4. 기록과 코드가 함께 커밋되는지 확인한다.
5. `.githooks/pre-push` 검사를 통과한 뒤 원격 브랜치에 푸시한다.

새 컴퓨터에서 저장소를 clone/pull한 뒤 `setup-hooks.cmd`를 한 번 더블클릭하면 Hook이 활성화된다. Hook은 작업 내용을 대신 작성하지 않고, 코드만 기록 없이 푸시되는 실수를 막는다.
## 2026-08-13 단계 전환 화면 중복 오류 수정

- 03단계 `renderUnits()`가 선언되지 않은 `smallUnits`를 참조해 렌더링이 중단되던 오류를 수정했다.
- 렌더링 시작 시 이전 작업 화면을 비우도록 하여, 오류가 발생해도 앞 단계 화면이 그대로 남아 같은 화면처럼 보이지 않게 했다.
- 학년 비교 보기 렌더링 중 오류가 발생해도 원래 프로젝트 상태를 `finally`에서 복원하도록 보강했다.
- 실제 Edge 브라우저에서 02 `교육과정 원문 확인`, 03 `단원 구성·차시 배분`, 04 `체제안 선택`이 각각 다르게 표시되는 것을 확인했다.
- `prototype.js` 캐시 버전을 `v=46`으로 올렸고 전체 자동 테스트 53개가 통과했다.
- 후속 확인에서 스포츠 문화 04단계의 유형 추천 변수가 03단계에 잘못 배치된 문제를 발견해 `renderFrameworks()`로 이동하고 캐시 버전을 `v=47`로 갱신했다.
- 고등 스포츠 문화 프로젝트의 04단계를 실제 Edge로 검증해 체제안 카드 3개와 주 소단원 유형 선택 상자가 출력되는 것을 확인했다.
- 3단계 종목을 프로젝트 공통 문자열로만 쓰던 구조를 `사용 후보 종목 → 소단원별 적용 종목`의 2단계 구조로 변경했다.
- 각 소단원 카드에 적용 종목 칩을 표시하고 저장하며, 목차 CSV에 `적용 종목` 열을 추가했다. 04단계와 초안 생성기는 선택한 소단원에 배정된 종목만 사용한다.
- 기존 저장 데이터에는 현재 선택 종목을 소단원별 기본값으로 자동 이전한다. CSS `v32`, JS `v48`.
- 04단계 산출물을 와이어프레임에서 편집디자인 펼침면으로 전환했다. 1400×900 PNG에는 단원 띠·쪽수·학습 목표·생각 열기·번호형 본문·하단 시각자료 발주 영역을 실제 교과서형 조판으로 그린다.
- 소단원 성격을 이론형/실기형/이론·실기 혼합형으로 단순화하고 문학·예술/프로젝트/토론/융합은 별도의 수업 구성 강조 항목으로 분리했다.
- 내부 생성기는 이론형에 설명 본문 4절, 실기형에 경기장·용구·규칙·기본 동작·전략·안전 원고, 혼합형에 개념과 참여 연결 원고를 생성한다.
- 체제별 결과에서 `교과서 원고 TXT`와 `지도서 원고 TXT`를 분리 다운로드한다. 지도서에는 목표·준비물·수업 흐름·지도상 유의점·발문·예상 답변·개별화·안전·평가가 포함된다. CSS `v33`, 엔진 `v3`, JS `v49`.
- 생성 결과 3열에서 긴 소단원명과 다운로드 버튼이 한 줄에 몰려 글자가 세로로 깨지던 문제를 수정했다. 각 카드는 최소 720px, 제목은 전체 폭, 버튼은 별도 줄이며 결과 영역 내부 가로 스크롤로 비교한다. CSS `v35`.
- 내부 생성기 schema `1.1`에서 펼침면 반복 문제를 제거했다. 이론형/실기형/혼합형마다 6개의 서로 다른 전개 청사진을 두고, 짧은 소단원도 마지막 펼침면은 평가·성찰 단계로 종결한다.
- 각 펼침면 교과서 원고는 4절×3문단이며 성취기준별 맥락·종목 정보·체제별 과제를 조합한다. 테스트 기준 최소 본문 900자를 보장하며, 지도서 목표·흐름·발문·예상 답변도 펼침면별 본문에서 파생한다. 엔진 캐시 `v4`.
