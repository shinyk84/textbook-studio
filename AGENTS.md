# 작업 맥락 유지 규칙

이 저장소에서 작업을 시작하는 사람과 AI는 다음 순서를 따른다.

1. `HANDOFF.md`를 먼저 읽어 현재 상태, 결정 사항, 미해결 문제를 확인한다.
2. `WORKLOG.md`의 최신 세션을 읽어 직전 작업의 종료 지점을 확인한다.
3. 사용자가 “작업 시작”을 알리면 `WORKLOG.md`에 새 세션을 `진행 중`으로 추가한다.
4. 작업 중에는 사용자의 요구, 실제 수정 파일, 중요한 판단 변경을 세션 항목에 기록한다.
5. 사용자가 “작업 종료”를 알리면 결과·검증·미해결 문제·다음 시작점을 기록하고 상태를 `완료` 또는 `중단`으로 바꾼다.
6. 현재 구조나 핵심 결정이 달라졌으면 `HANDOFF.md`도 함께 갱신한다.
7. API 키, 비밀번호, 토큰, 데이터베이스 접속 문자열 등 비밀값은 어떤 문서에도 기록하지 않는다. 환경 변수 이름과 설정 여부만 기록한다.
8. Git 작업 전후에는 현재 브랜치, 최신 커밋, 작업 트리 상태를 확인한다. 사용자 변경을 임의로 되돌리지 않는다.
9. 사용자가 “깃푸시”, “Git push”, “푸시해줘”라고 요청하면 다음 작업을 푸시보다 먼저 자동 수행한다.
   - 현재 대화와 Git diff를 바탕으로 `WORKLOG.md`에 작업 목적·수행 방법·수정 파일·검증·남은 문제를 기록한다.
   - 프로젝트 구조, 핵심 결정, 미해결 문제 또는 다음 시작점이 바뀌었으면 `HANDOFF.md`도 갱신한다.
   - 민감정보 포함 여부와 `git diff --check`를 검사한다.
   - 기록 문서를 실제 코드 변경과 같은 커밋 또는 바로 뒤 기록 커밋에 포함한 다음 푸시한다.
   - 푸시가 끝나면 `WORKLOG.md`의 Git 항목에 브랜치·커밋·푸시 결과를 반영한다. 이 반영 때문에 추가 커밋이 필요하면 기록 커밋으로 함께 푸시한다.
10. `.githooks/pre-push`는 코드 변경에 `WORKLOG.md`가 포함되지 않은 푸시를 막는 최종 안전장치다. Hook을 우회하지 않는다.

세션 기록에 최소한 포함할 내용:

- 작업 목적
- 사용자가 요청한 핵심
- 수행 방법과 판단 근거
- 수정한 파일
- 검증 결과
- Git 커밋·푸시 상태
- 남은 문제와 다음 작업
