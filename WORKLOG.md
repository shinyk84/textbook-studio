# 작업 세션 기록

이 문서는 세션별 변경 이유와 결과를 시간순으로 남기는 기록이다. 현재 상태의 요약은 `HANDOFF.md`를 본다.

## 기록 방법

- 사용자가 “작업 시작”을 알리면 아래 템플릿으로 새 항목을 맨 위의 ‘진행 세션’ 아래에 만든다.
- 사용자가 “작업 종료”를 알리면 실제 결과와 검증을 채우고 ‘완료 세션’으로 옮긴다.
- 대화 내용 전체를 옮기지 않고, 다음 사람이 판단을 이어갈 수 있는 요구·결정·수정·검증만 기록한다.
- 비밀값은 기록하지 않는다.

### 세션 템플릿

```markdown
## YYYY-MM-DD HH:MM · 작업 제목

- 상태: 진행 중 | 완료 | 중단
- 시작 요청:
- 작업 목적:
- 수행 방법·판단:
- 수정 파일:
- 검증:
- Git:
- 남은 문제:
- 다음 시작점:
```

## 진행 세션

### 2026-08-13 · 스포츠 문화 유형별 펼침면 이미지 초안

- 상태: 완료
- 시작 요청: 고등 스포츠 문화는 초등식 활동 1·2 구조가 맞지 않고 이론 단원과 실기 단원의 차이가 크므로, 소단원과 유형을 선택해 이미지 초안으로 비교할 수 있게 수정.
- 작업 목적: 세 체제의 긴 텍스트 원고를 세로로 비교하던 화면을 고등 스포츠 문화의 내용 유형에 맞는 시각적 펼침면 비교 흐름으로 전환.
- 수행 방법·판단: 소단원명에 따라 이론·인문 탐구형, 문학·예술 감상형, 종목 직접 참여형, 대회 프로젝트형, 융합·진로형을 자동 추천하고 주 유형과 보조 유형을 편집할 수 있게 했다. 내부 생성기는 유형별 질문·자료·참여·역할·비평·창작·진로·평가 순서를 조합한다. 결과 화면은 세 체제를 가로 비교하고, 체제별 펼침면을 Canvas 와이어프레임 이미지로 그려 가로로 넘겨 보게 했다. 이미지 확대와 개별 PNG 저장을 지원하며 기존 원고·지도서·근거는 접힌 상세 영역으로 이동했다.
- 수정 파일: `static/prototype-draft-engine.js`, `static/prototype.js`, `static/prototype.css`, `static/prototype.html`, `tests/test_prototype_draft_engine.py`, `ANALYSIS_RULES.md`, `HANDOFF.md`, `WORKLOG.md`.
- 검증: JavaScript 구문 검사, 유형별 내부 생성 결과, 추천 유형 UI, Canvas/PNG 연결을 검사했으며 전체 단위 테스트 52개와 `git diff --check`를 통과했다.
- Git: 커밋·푸시하지 않음.
- 남은 문제: 현재 이미지는 사진·삽화가 없는 레이아웃 와이어프레임이다. 실제 사진·삽화는 저작권이 확인된 내부 자산 또는 향후 외부 이미지 API 공급자를 연결해 채워야 한다. 실제 브라우저 자동 캡처 도구가 현재 환경에 없어 화면의 시각적 최종 조정은 사용자 확인이 필요하다.
- 다음 시작점: 서로 다른 유형의 소단원을 각각 생성해 이론형·참여형·프로젝트형의 지면 차이를 검토하고, 승인된 와이어프레임을 교과서 원고와 지도서 차시안 편집 화면으로 연결한다.

### 2026-08-13 · 스포츠 문화 단원 세분화 및 소단원 편집 UI 개선

- 상태: 완료
- 시작 요청: 단원·차시 배분 화면의 우측에 치우친 작은 입력란이 소단원명인지 확인하고 가독성을 개선하며, 전처리 자료를 바탕으로 중단원·소단원을 충분히 세분화.
- 작업 목적: 성취기준 1개당 소단원 1개였던 6개짜리 임시 구조를 실제 교과서 개발에 활용할 수 있는 편집 가능한 초기 목차로 확장.
- 수행 방법·판단: 2022 개정 스포츠 문화 4개사 교과서의 앞부분·목차·구성 설명에서 역사·철학·문학·예술, 물질·제도·관념 문화, 대회 기획·운영·평가, 역할, 융합·미디어·지속 가능성·진로의 반복 구조를 추출했다. 교육과정 2개 영역과 성취기준 6개를 상위 골격으로 고정하고 6개 중단원·18개 소단원 후보로 재구성했다. 총량은 32회, 본문 176쪽과 부록 4쪽으로 유지했다. 소단원명은 라벨이 있는 넓은 행으로 이동하고 근거·성취기준, 차시·쪽수, 조작 버튼을 분리했다. 수정되지 않은 구형 6개 기본안만 자동 전환하며 편집본은 버튼으로 명시적 교체하도록 했다.
- 후속 UI 수정: 넓은 화면에서 소단원명·차시·쪽수·이동/삭제/복제 버튼을 한 줄로 재배치하고 입력·버튼 높이를 42px로 통일했다. 적용 종목 선택을 3단계에 추가해 전처리 교과서 기반 12개 후보의 복수 선택과 기타 종목 입력을 지원하고, 선택값을 4단계 초안 생성에 자동 연동했다. 후보 목록은 교육과정 필수 목록이 아님을 화면에 명시했다.
- 수정 파일: `static/prototype.js`, `static/prototype.css`, `static/prototype.html`, `tests/test_prototype_draft_engine.py`, `ANALYSIS_RULES.md`, `HANDOFF.md`, `WORKLOG.md`.
- 검증: JavaScript 구문 검사 통과. 2개 대단원·6개 중단원·18개 소단원, 총 32회·소단원 본문 160쪽(단원 도입·마무리 16쪽과 합쳐 본문 176쪽), 소단원 행 UI와 3→4단계 종목 연동을 자동 검사했으며 전체 단위 테스트 51개와 `git diff --check`를 통과했다.
- Git: 커밋·푸시하지 않음.
- 남은 문제: 18개 소단원명과 개별 차시·쪽수는 공통 패턴 기반 개발 후보이므로 편집자 검토 후 조정해야 한다. 출판사별 전체 목차·실제 차시표의 정량 매핑은 후속 분석 대상이다.
- 다음 시작점: 18개 소단원의 순서·명칭·차시를 화면에서 검토한 뒤 확정된 구조를 4단계 펼침면 초안 생성 입력으로 연결한다.

### 2026-08-13 · 스포츠 문화 내부 데이터 초안 생성 구조 적용

- 상태: 완료
- 시작 요청: 분석한 개발 규칙을 기록해 초등 스튜디오 수정 때 재사용하고, 스포츠 문화부터 차근차근 수정. 로컬 AI 실패 상태에서는 내부 데이터 조합으로 초안을 만들되 향후 외부 API 연결을 고려.
- 작업 목적: 스포츠 문화 초안 생성을 초등 체육용 임시 문구 생성에서 분리하고, 교육과정 추적성·교과서와 지도서의 연동·향후 공급자 교체가 가능한 프로토타입으로 전환.
- 수행 방법·판단: 총론→스포츠 문화 각론→인정기준→단원/차시/펼침면→평가→지도서의 변환 규칙을 `ANALYSIS_RULES.md`에 고정했다. 4단계에는 `internal-rules-v1` 공급자를 추가해 스포츠 문화 성취기준 6개, 매개 종목, 8개 펼침면 기능, 전처리 자료 근거를 조합하도록 했다. 교과서 펼침면마다 목표·준비·도입/전개/정리·질문·안전·평가가 있는 지도서 파생안을 함께 생성한다. 외부 API는 동일한 입출력 스키마의 공급자를 서버 측에 등록할 수 있게 분리했으며 키를 브라우저에 두지 않는 방식을 전제로 했다. 초등의 기존 생성 동작은 변경하지 않았다.
- 수정 파일: `ANALYSIS_RULES.md`, `static/prototype-draft-engine.js`, `static/prototype.js`, `static/prototype.html`, `static/prototype.css`, `tests/test_prototype_draft_engine.py`, `HANDOFF.md`, `WORKLOG.md`.
- 검증: JavaScript 구문 검사, 대표 스포츠 문화 초안 생성 확인, 전체 Python 단위 테스트 수행. 내부 공급자 결과가 체제 3안, 선택 성취기준, 매개 종목, 지도서 파생안, 출처 근거를 포함하고 매개 종목 누락을 거부하는지 검사한다.
- Git: 커밋·푸시하지 않음.
- 남은 문제: 5단계 PDF 모의심사는 여전히 기존 OpenAI 연동이다. 내부 생성 문구의 편집 품질과 출판사별 체제 비교 규칙은 실제 사용자 검토를 거쳐 계속 조정해야 한다. 외부 API 공급자는 계약만 준비했고 구현하지 않았다.
- 다음 시작점: 스포츠 문화 1~3단계의 체제 결정 화면을 실제 편집 흐름에 맞게 세분화한 뒤, 4단계 결과의 펼침면 이미지 초안과 원고 텍스트 내보내기를 연결한다. 이후 5단계 내부 기준 사전점검과 외부 독립 모의심사의 역할을 분리한다.

### 2026-08-13 · 2022 개정 초등 체육 5 교과서·지도서 3개사 전처리

- 상태: 완료
- 시작 요청: `official-data`에 추가한 YBM·동아출판·천재교과서의 2022 개정 초등 체육 5 교과서·지도서 원본 6개를 자료 유형별 저장소로 옮기고 전처리하며, 5~6학년군 중 5학년 구현 자료이자 검정도서 비교 자료로 구분.
- 작업 목적: 초등 체육 검정 교과서·지도서와 고등 스포츠 문화 인정 교과서·지도서의 개발 규칙을 비교하고, 공통 제작 규칙을 프로토타입에 반영할 수 있는 근거 데이터 확충.
- 수행 방법·판단: 원본 3개 교과서를 `official-data/textbook`, 지도서 3개를 `official-data/teacher_guide`로 이동. 총 1,462쪽을 원본 텍스트·블록·페이지 이미지·순차 펼침면 이미지·페이지/펼침면 검색 청크로 전처리함. 펼침면 자동 분류는 오판을 피하려고 모두 `uncertain`으로 보존. manifest에 2022 개정, 초등학교 5학년, 적용 교육과정 5~6학년군, 검정 체계를 명시함. 추가 패키지 없이 재현할 수 있도록 PyMuPDF 기반 `scripts/preprocess_textbook_guides.py`를 추가함. 1차 구조 대조 결과 3개사 교과서는 공통으로 운동·스포츠·표현 대영역을 두고, 스포츠는 기술형·전략형·생태형, 표현은 스포츠·전통·현대 표현으로 조직하며 안전·디지털/융합·생태/시민·정리/평가 요소를 결합함. 지도서는 교육과정·수업·평가·연간 계획·편찬 방향의 총론과 영역 개관·차시 지도·평가·자료의 각론 구조를 공통으로 사용함. 지도서 총론에는 5~6학년군 성취기준 26개 전체가 인용되므로, 실제 5학년 배분은 단순 코드 출현이 아니라 각론의 차시별 기준표를 매핑해야 한다고 판단함.
- 수정 파일: `official-data/textbook` 및 `official-data/teacher_guide` 원본 6개(로컬·Git 제외), `official-data/processed/22개정_*_elementary_pe5_*` 6개 폴더, `scripts/preprocess_textbook_guides.py`, `official-data/README.md`, `tests/test_app.py`, `HANDOFF.md`, `WORKLOG.md`.
- 검증: YBM 교과서 144쪽·142청크·펼침면 71쌍, 지도서 292쪽·298청크·145쌍; 동아 교과서 146쪽·145청크·72쌍, 지도서 384쪽·392청크·191쌍; 천재 교과서 142쪽·136청크·70쌍, 지도서 354쪽·355청크·176쌍. 6종 모두 원본 SHA-256 일치, JSONL 전 행 파싱, manifest 행 수와 페이지·펼침면 이미지 수 일치. 앱 자료 총수 기대값을 29종으로 변경했으며 전체 자동 테스트 45개와 `git diff --check`를 통과함. 신규 산출물은 약 341.9MB임.
- Git: 커밋·푸시하지 않음.
- 남은 문제: 텍스트가 없거나 매우 적어 원본 이미지 대조가 필요한 쪽은 교과서 17쪽, 지도서 5쪽. 펼침면은 모두 `uncertain`이므로 세부 편집 분석에서 이미지 대조 필요. 검정·인정 공통 규칙의 프로토타입 반영 항목은 3개사 목차·차시·지도서 구조 분석 후 확정.
- 다음 시작점: 5~6학년군 26개 성취기준을 기준으로 3개사 5학년 목차·활동·평가·지도서 지원 요소를 매핑하고, 스포츠 문화 인정도서 4개사와 공통/고유 규칙을 분리해 프로토타입 데이터 모델과 화면에 적용.

### 2026-08-13 · 2009 개정 YBM 스포츠 문화 지도서 전처리

- 상태: 완료
- 시작 요청: `official-data/teacher_guide`에 추가한 2009 개정 YBM 스포츠 문화 지도서를 기존 비교 자료에 포함.
- 작업 목적: 2009 개정의 교과서뿐 아니라 지도서 체제와 교수·학습 지원 구조까지 2022 개정 4개 출판사 자료와 비교할 수 있도록 보강.
- 수행 방법·판단: 원본 PDF를 변경하지 않고 페이지별 텍스트·블록·미리보기 이미지, 순차 마주보기 펼침면 이미지와 페이지·펼침면 검색 청크를 생성함. 자동 레이아웃 판정은 임의로 확정하지 않고 183쌍을 `uncertain`으로 보존해 분석 시 원본 이미지와 대조하도록 함.
- 수정 파일: `official-data/processed/09개정_ybm_teacher_guide/**`, `official-data/README.md`, `tests/test_app.py`, `HANDOFF.md`, `WORKLOG.md`.
- 검증: 원본 368쪽과 페이지 JSONL·이미지 368개, 펼침면 JSONL·이미지 183개, 페이지 청크 1,478개, 펼침면 청크 1,389개를 대조함. 원본과 manifest의 SHA-256 일치, 텍스트 미추출 경고 0쪽. 자료 총수 기대값을 23종으로 수정한 뒤 전체 테스트 재실행 예정.
- Git: 커밋·푸시하지 않음.
- 남은 문제: 펼침면 183쌍은 자동 확정하지 않은 `uncertain`이므로 실제 체제 분석에서 페이지 이미지 대조 필요.
- 다음 시작점: 2009 YBM 교과서·지도서와 2022 개정 4개 출판사 교과서·지도서의 목차·단원·펼침면·지도 지원 요소를 비교 분석.

### 2026-08-12 · 2009 개정 교육과정 2종 정리·전처리

- 상태: 완료
- 시작 요청: 스포츠 문화 개발 계보의 비교 범위를 2009 개정과 2022 개정으로 바로잡고, `official-data` 바로 아래 추가한 2009 개정 교육과정 PDF 2종을 적절한 위치로 이동해 전처리해 달라는 요청.
- 작업 목적: 2015 개정을 전제로 하지 않고 2009→2022 개정의 교육과정·교과서·지도서 관계를 분석할 수 있도록 2009 개정 공식자료를 보강.
- 수행 방법·판단: 새 원본 2종을 `official-data/criteria/09개정`으로 이동. 새 체육과 교육과정 최종수정본은 기존 158쪽 PDF와 해시·쪽수(147쪽)가 달라 기존 산출물을 덮어쓰지 않고 별도 판본으로 보존함. 총론과 최종수정본을 PDF 페이지별 텍스트·검색 청크·검토 목록·manifest로 전처리하고 `curriculum_revision: 2009 개정` 및 저장소 상대 원본 경로를 기록함.
- 수정 파일: `official-data/criteria/09개정` 원본 2종(로컬·Git 제외), `official-data/processed/09_개정_초중등학교_교육과정_총론`, `official-data/processed/09_개정_별책11체육과_교육과정교육과학기술부고시_제2011_361호최종수정`, `official-data/README.md`, `tests/test_app.py`, `HANDOFF.md`, `WORKLOG.md`.
- 검증: 총론 31쪽·30청크·검토 1쪽, 체육과 교육과정 최종수정본 147쪽·145청크·검토 2쪽 생성. 두 자료 모두 원본 SHA-256과 manifest가 일치하고 JSONL 행 수가 manifest 청크 수와 일치함. 앱 인식 기대값을 총 22종으로 갱신.
- Git: 미커밋·미푸시.
- 남은 문제: 기존 158쪽 체육과 교육과정과 새 147쪽 최종수정본의 본문 차이·누락 부록을 아직 비교하지 않음. 2009 개정 지도서는 아직 확보 여부 미확인.
- 다음 시작점: 두 체육과 교육과정 판본 차이 분석 → 2009 개정 교과서의 성취기준·단원 연결 → 2022 개정 4개 출판사 교과서·지도서와 구조 비교.

### 2026-08-12 · 동아출판·미래엔 스포츠 문화 교과서·지도서 전처리

- 상태: 완료
- 시작 요청: `official-data/textbook`과 `official-data/teacher_guide`에 추가한 동아출판·미래엔 스포츠 문화 교과서와 지도서도 전처리해 달라는 요청.
- 작업 목적: 2022 개정 스포츠 문화 비교 자료를 천재·지학사뿐 아니라 동아출판·미래엔까지 확장하고 앱의 공식자료 색인에 포함.
- 수행 방법·판단: 기존 스포츠 문화 교과서·지도서 규격과 동일하게 원본 PDF를 페이지·펼침면 단위로 추출하고 검색 청크, 검토 목록, 미리보기 이미지를 생성. 각 manifest와 검색 청크에 `curriculum_revision: 2022 개정`, 출판사, 저장소 상대 원본 경로를 기록함.
- 수정 파일: `official-data/processed/22개정_donga_textbook`, `22개정_donga_teacher_guide`, `22개정_miraen_textbook`, `22개정_miraen_teacher_guide`, `official-data/README.md`, `tests/test_app.py`, `HANDOFF.md`, `WORKLOG.md`.
- 검증: 동아출판 교과서 196쪽·257청크, 지도서 288쪽·649청크, 미래엔 교과서 198쪽·262청크, 지도서 192쪽·343청크 생성. 4종 모두 원본 SHA-256, JSONL 행 수, 페이지·펼침면 이미지 수가 manifest와 일치함. 앱 인식 기대값을 총 20종으로 갱신.
- Git: 미커밋·미푸시.
- 남은 문제: 자동 펼침면 판정의 `uncertain` 후보는 동아출판 교과서 18쌍·지도서 20쌍, 미래엔 교과서 15쌍·지도서 25쌍으로 필요 시 `review.csv`와 접촉면 이미지를 육안 검토해야 함. 전처리 전체 용량은 약 514.8MB이므로 다음 Git/Vercel 배포 전에 이미지 포함 범위를 결정해야 함. `22개정 씨마스 스포츠 문화` 원본은 여전히 저장소 규격으로 전처리되지 않음.
- 다음 시작점: 4종 `review.csv` 육안 검토 범위 결정 → 씨마스 22개정 전처리 여부 결정 → Git/Vercel에 미리보기 이미지 포함 여부 결정.

### 2026-08-12 · 스포츠 문화 교과서·지도서 검증 및 2009 개정 자료 전처리

- 상태: 완료
- 시작 요청: 다른 작업 폴더에서 만든 스포츠 문화 교과서·지도서 전처리 결과가 `processed`에 제대로 들어왔는지 확인하고, `textbook`의 09개정 YBM 스포츠 문화 교과서와 `criteria/09개정`의 2009 개정 체육과 교육과정을 추가 전처리하며 기존 자료를 22개정으로 구분해 달라는 요청.
- 작업 목적: 2009/2022 개정 자료가 섞이지 않도록 세대를 명시하고, 원본과 전처리 결과의 대응·무결성을 검증.
- 수행 방법·판단:
  - 천재·지학사 스포츠 문화 교과서 2종과 지도서 2종은 원본 SHA-256이 manifest와 모두 일치했고, `chunks.jsonl`·`spread_chunks.jsonl`·`spreads.jsonl` 행 수 및 페이지/펼침면 이미지가 manifest와 일치함을 확인.
  - 기존 4개 폴더를 `22개정_chunjae_textbook`, `22개정_jihaksa_textbook`, `22개정_chunjae_teacher_guide`, `22개정_jihaksa_teacher_guide`로 구분하고 manifest·검색 청크에 `curriculum_revision: 2022 개정`, 저장소 상대 원본 경로를 기록.
  - 09개정 YBM 스포츠 문화 교과서 256쪽을 기존 교과서 전처리 규격으로 처리해 `09개정_ybm_textbook`에 물리 페이지 256개, 펼침면 쌍 127개, 페이지 청크 338개, 펼침면 청크 248개와 미리보기 이미지를 생성. 원본 SHA-256 일치 확인. 자동 이미지 판정상 127쌍 중 126쌍이 `uncertain`이므로 필요 시 사람이 `review.csv`와 접촉면 이미지를 검토해야 함.
  - 2009 개정 체육과 교육과정 PDF 158쪽을 `2009_개정_2011체육과교육과정`으로 전처리. 158쪽 모두 텍스트 추출 가능하며 158개 청크 생성, 원본 SHA-256 일치 확인.
  - 기존 공식자료 10종의 manifest·검색 청크에 `curriculum_revision: 2022 개정`을 추가. 앱의 자료 무결성 검사가 `pages.jsonl` 규격도 인식하도록 확장하고 전체 자료 수 기대값을 16종으로 갱신.
  - 원본 대용량 PDF는 Git/Vercel에 올라가지 않도록 `official-data/textbook`, `official-data/teacher_guide`를 `.gitignore`에 추가.
- 수정 파일: `.gitignore`, `app.py`, `official-data/README.md`, `official-data/processed/**/manifest.json`, 기존 공식자료 `chunks.jsonl`, 신규 2009 개정 2종 전처리 폴더, 신규/정리된 2022 개정 교과서·지도서 4종 폴더, `scripts/preprocess_official_documents.py`, `tests/test_app.py`, `HANDOFF.md`, `WORKLOG.md`.
- 검증: 전처리 문서 16종 인식, 원본이 로컬에 있는 11종 SHA-256 일치, 나머지 5종은 `processed_only`. 신규 교과서·지도서의 JSONL 파싱·행 수·페이지 이미지 수 일치. `python -m py_compile`, 전체 테스트 45개, `git diff --check` 통과.
- Git: 미커밋·미푸시.
- 남은 문제: `22개정 씨마스 스포츠 문화.pdf`는 원본만 있고 현재 저장소 규격의 전처리 폴더가 없음. YBM 09개정의 펼침면 자동 판정 126쌍은 육안 검토 후보. 교과서·지도서 미리보기 포함 전처리 산출물이 약 256MB이므로 다음 Git/Vercel 배포 전 이미지 포함 범위를 결정해야 함.
- 다음 시작점: 씨마스 22개정 전처리 여부 결정 → YBM `review.csv` 육안 검토 범위 결정 → Git에 텍스트/JSONL만 넣을지 미리보기 이미지까지 넣을지 결정.

### 2026-08-12 · 스포츠 문화 모의심사 인정기준 정정과 업로드 오류 안내

- 상태: 완료
- 시작 요청: 스포츠 문화가 인정도서인데 5단계 설명과 실제 채점이 검정기준으로 되어 있다는 지적, PDF 업로드 채점 실패가 결제수단 미등록 때문인지 확인 요청.
- 작업 목적: 프로젝트 유형에 맞는 공식 심사기준을 적용하고 채점 실패 원인을 사용자가 바로 확인할 수 있게 함.
- 수행 방법·판단: 전처리 원문 「(서울교육연구정보원) 2022 개정 교육과정 교육부 장관 고시 인정도서 편찬상의 유의점 및 인정기준」의 스포츠 문화 구간을 대조해 4개 영역·20개 인정기준·100점(20/30/20/30)을 확인. 기존 코드가 초등 체육용 22개 검정기준을 모든 프로젝트에 재사용하던 오류를 수정해 초등은 검정기준 22개, 고등 체육 인정도서는 인정기준 20개, 중등은 공식 심사기준 미연결로 구분함. API 요청에도 선택 프로젝트 ID를 보내 서버 프롬프트·배점·결과 표가 같은 기준을 사용하게 함. 기존 기준으로 저장된 결과는 숨기고 재채점을 안내함. 오류가 짧은 토스트로만 사라지지 않도록 업로드 영역 아래에 API 키 미설정·결제 한도·PDF 텍스트 추출 실패 등의 서버 오류를 지속 표시함. Vercel 공개 배포 시 제3자가 서버 OpenAI 키를 무단 사용할 수 있는 위험을 막기 위해 Supabase 인증이 활성화된 환경에서는 `/api/prototype/review`도 로그인 세션을 요구하도록 `auth-client.js`와 서버 인증 검사를 연결함(인증이 꺼진 로컬 환경은 기존처럼 사용 가능).
- 수정 파일: `app.py`, `static/prototype.js`, `static/prototype.html`, `tests/test_prototype_pdf_review.py`, `HANDOFF.md`, `WORKLOG.md`, `scripts/build_version_comparison_presentation.js`, `presentations/기존버전_vs_프로토타입_단계비교_수정본.pptx`(발표자료의 5단계도 원고 편집이 아니라 모의심사로 정정).
- 검증: `node --check static/prototype.js`, 발표자료 생성 스크립트 문법 검사, `python -m py_compile` 3개 파일, PDF 모의심사 단위 테스트 12개, 전체 테스트 45개 통과. `git diff --check` 통과(CRLF 안내만 출력). 스테이징 43개 파일을 대상으로 API 키·GitHub 토큰·Google 키·JWT 패턴 검사 결과 없음.
- Git: `c0e9ff0 Deploy five-step textbook prototype`을 `origin/main`에 푸시 완료. 연결된 Vercel Production에서 `https://textbook-studio.vercel.app/prototype.html` HTTP 200, `prototype.js?v=40`, 5단계 화면, 고등 인정기준 20개 반영을 확인함. `/api/health` 정상, 로그인하지 않은 `/api/prototype/review` 요청은 HTTP 401로 차단됨.
- 남은 문제: 실제 채점에는 서버에 유효한 `OPENAI_API_KEY`와 사용 가능한 API 크레딧이 필요함. 현재 작업 셸과 `.env.local`에는 키가 없고, 이전 실제 호출 기록에는 `429 quota/billing`이 확인됨. 이미지로만 구성된 스캔 PDF는 현재 OCR하지 않으므로 텍스트 추출 가능한 PDF가 필요함.
- 다음 시작점: `.env.local` 또는 배포 환경에 API 키를 설정하고 결제·크레딧을 활성화한 뒤 스포츠 문화 PDF 1건을 실제 재채점. 이후 프로토타입 파일을 선별 커밋·배포.

### 2026-08-12 · 기존 운영 버전과 프로토타입 단계 비교 발표자료

- 상태: 완료
- 시작 요청: 기존 버전과 프로토타입 버전을 비교해 단계와 달라진 점을 설명하는 프레젠테이션 제작 요청.
- 작업 목적: 기존 운영 버전의 9단계와 현재 프로토타입의 5단계를 발표자가 짧게 설명할 수 있도록, 단계 통합·이동·추가·후속 범위를 시각적으로 정리.
- 수행 방법·판단: 기존 9단계와 프로토타입 5단계를 직접 매핑하고, 핵심 차이를 `9→5 단계 통합`, `개발 방향 결정 시점 이동`, `선택 소단원 3안 비교`, `공식 근거·승인 흐름 강화`로 정리함. 운영 버전과 독립된 프로토타입이라는 점, 현재 AI 생성·권한은 시뮬레이션이고 모의심사는 후속 연결 대상이라는 범위도 별도 표로 명시함. 저장소에 이미 있는 `static/pptxgen.bundle.js`를 재사용해 외부 패키지 설치 없이 16:9 PPTX 9장을 생성하고 슬라이드별 발표자 노트를 포함함.
- 수정 파일: `scripts/build_version_comparison_presentation.js`, `presentations/기존버전_vs_프로토타입_단계비교.pptx`, `WORKLOG.md`.
- 검증: `node --check scripts/build_version_comparison_presentation.js` 통과. PPTX ZIP 무결성 검사 오류 없음, 슬라이드 9장·발표자 노트 9개 생성 확인. `git diff --check` 통과(기존 파일의 CRLF 안내만 출력).
- Git: 미커밋·미푸시. 기존 작업 트리의 사용자 변경과 분리해 보존함.
- 남은 문제: 실제 PowerPoint 화면에서 회사 발표 템플릿·로고·발표 시간에 맞춘 최종 미세 조정은 필요 시 진행.
- 다음 시작점: 발표 대상과 제한 시간이 정해지면 5분용으로 축약하거나 회사 템플릿에 맞춰 색상·표지·로고를 조정.

### 2026-08-07 · 4단계 생성 이력 UX 개선과 실제 AI API 연동(OpenAI/Gemini/Claude)

- 상태: 완료 (생성 이력 UI 개선은 유지·완료. 실제 AI API 연동은 Gemini 인증 미해결 끝에 사용자가 기능 자체를 삭제하기로 결정해 관련 코드를 전부 되돌림 — 아래 참고)
- 시작 요청: "생성 이력 전체 다운로드가 계속 늘어나기만 한다" → 회차별 게시글식 UI → "체크박스로 선택 삭제·다운로드" → "PPT로 다운돼야 한다" → "회차별로 따로 다운로드" → "화면에 다 펼쳐져 나오는 게 불필요하다" → 아코디언 방식으로 전환까지 4단계 생성 이력 UI를 여러 차례 반복 개선. 이어서 "개인 API 키를 넣으면 실제 생성이 되게 해달라"는 요청으로 시작해 "API를 붙이라는 뜻이 아니라 키 입력칸·발급 바로가기 버튼만 필요하다"로 한 번 축소됐다가, 다시 "이번엔 진짜로 실제 AI 호출을 붙일 것"으로 확장되어 OpenAI·Gemini·Claude 3개 실제 연동을 구현함.
- 작업 목적: (1) STEP4 생성 이력 UI를 회차가 쌓일수록 불편해지지 않게 재구성. (2) 사용자 개인 API 키로 STEP4 "3개 체제 초안 생성"이 시뮬레이션이 아닌 실제 LLM 호출로 동작하게 함.
- 수행 방법·판단:
  - **생성 이력 UI**: `state.frameworkDraftLog`를 "생성할 때마다 3개 항목이 그냥 쌓이는 배열"에서 "회차(batch) 단위 배열"(`{generatedAt, smallUnitLabel, entries:[...]}`)로 구조 변경. 처음엔 회차별 체크박스+선택 다운로드(txt)·선택 삭제 기능을 만들었다가, "PPT로 다운돼야 한다"는 요청에 따라 다운로드 결과물을 PptxGenJS 프레젠테이션으로 교체(`draftBatchesToPptx`가 `addEntrySlidesToPptx`를 회차별로 호출). 처음엔 선택한 여러 회차를 파일 하나로 합쳤다가 "회차별로 따로 다운되게" 요청에 따라 회차마다 독립된 `.pptx` 파일을 순차(`await`) 생성하도록 재수정(비동기 경합 가능성을 없애기 위해 반복문에서 매번 await함). 마지막으로 "화면 아래로 전부 펼쳐져 나오는 게 불필요하다"는 지적에 따라, 이력 목록과 3열 비교 화면을 하나의 아코디언(`<details data-draft-log-index>`)으로 합쳐 기본은 전부 접히고 방금 생성한 회차만 자동으로 펼쳐지게 함(`expandedDraftBatches` 모듈 변수로 펼침 상태를 재렌더링 간 유지 — 기존 `evidenceDetailsOpen` 패턴과 동일). 체크박스 클릭이 아코디언을 여닫지 않도록 `click` 이벤트에서 `stopPropagation` 처리.
    - 중간에 체크박스가 `<input>` 전역 스타일(`width:100%; padding:11px 12px`)을 그대로 물려받아 글상자처럼 커지면서 "전체 선택" 글자를 밀어내는 버그를 발견해 `.draft-log-summary input[type="checkbox"]` 전용 규칙(16px 고정)으로 수정.
  - **개인 API 키 입력**: 처음엔 OpenAI/Gemini/Claude 입력칸 3개 + 발급 바로가기 버튼 3개로 만들었으나, 사용자가 "칸을 굳이 3개로 나눌 필요가 있냐"고 되물어 입력칸 1개로 합치고 값의 접두사로 공급자를 자동 인식하는 방식(`detectApiKeyProvider`)으로 축소함. 처음엔 이 단계에서 "실제 생성에 연결하지 않음"으로 합의했다가, 바로 다음 요청에서 "이번엔 진짜로 실제 호출을 붙일 것"으로 뒤집혀 아래 실제 연동 작업으로 이어짐.
  - **실제 API 연동(OpenAI/Gemini/Claude)**: 브라우저에서 각 서비스로 직접 `fetch`하면 OpenAI가 CORS로 막힐 가능성이 높다는 점을 설명하고, 이미 로컬에서 띄우고 있는 `app.py`를 순수 중계(키를 서버에 저장하지 않고 그대로 전달만 함)로 쓰기로 사용자와 합의함. `app.py`에 `/api/prototype/generate`를 `do_POST`의 `require_user()` 호출보다 먼저 분기해 로그인 없이 접근 가능하게 추가하고(`/api/health` 예외 처리 패턴과 동일), `call_openai_relay`/`call_claude_relay`/`call_gemini_relay`/`call_prototype_ai_relay`를 기존 `call_openai_for_small_unit`과 같은 스타일(urllib, HTTPError 한국어 매핑)로 신설. `static/prototype.js`의 `simulatedSpreadDraft` 위에 미리 설계해 두었던 프롬프트 주석을 실제 `buildFrameworkPrompt`/`SPREADS_JSON_SCHEMA`/`callRealFrameworkDraft` 함수로 전환했고, 인덱스 기반 파생 필드(역할·쪽번호·제목 등)를 `deriveSpreadMeta`로 뽑아내 시뮬레이션·실제 API 양쪽이 공유하게 리팩터링함. 생성 버튼은 입력된 키가 있으면 `async`로 3개 체제를 병렬 호출하고, 하나라도 실패하면 전부 취소(부분 저장 없음)하도록 만듦.
  - **디버깅**: (a) `app.py`를 고쳐도 이미 떠 있는 서버 프로세스는 자동 재시작되지 않아 "API 경로를 찾을 수 없습니다"가 계속 뜨는 문제 — Python `http.server`는 파일을 다시 읽지 않는다는 걸 설명하고, 이후 `app.py`를 고칠 때마다 포트 8000 프로세스를 찾아(`netstat`) `taskkill`로 끄고 `python app.py`를 다시 백그라운드로 띄우는 것을 반복함. (b) OpenAI는 실제로 키 인증까지는 통과했으나 결제 수단 미등록으로 `429 quota/billing` 오류가 남(HANDOFF.md에 이미 기록된 기존 이슈와 동일 원인). (c) Gemini는 `aistudio.google.com/apikey`에서 발급한 키가 예상한 `AIzaSy...` 형식이 아니라 `AQ....` 형식으로 나왔고, 쿼리 파라미터(`?key=`)·`x-goog-api-key` 헤더·`Authorization: Bearer` 세 가지 인증 방식을 모두 시도했지만 매번 구글이 "Expected OAuth 2 access token" 오류를 반환함 — 사용자가 개인(비-Workspace) 계정이고 공식 API 키 페이지에서 복사한 값임을 확인했음에도 원인을 못 찾은 채 사용자가 "스톱"을 요청해 미해결로 남음.
  - **기능 삭제 결정**: 이후 사용자가 "무료 개인 API 키 입력 기능은 실패했으니 그 부분은 삭제하라"고 확정함(운영 서비스의 기존 `OPENAI_API_KEY` 서버 키 기반 생성 기능은 이번 작업과 무관하며 그대로 유지 — 결제만 등록하면 별도로 동작 가능함을 서로 확인함). 이에 따라 `app.py`의 `/api/prototype/generate` 및 4개 릴레이 함수·상수, `tests/test_prototype_ai_relay.py`, `static/prototype.js`의 API 키 패널·자동 인식·`callRealFrameworkDraft` 등 관련 코드를 전부 제거하고 생성 버튼을 다시 시뮬레이션 전용 동기 로직으로 되돌림(`app.py`는 삭제 후 이번 세션 시작 전 커밋 상태와 diff 없음으로 확인). 생성 이력 UI(회차 배치·아코디언·PPT 다운로드)는 API 키 기능과 무관하므로 그대로 유지됨.
- 수정 파일: `static/prototype.js`, `static/prototype.css`, `static/prototype.html`(캐시 버전 최종 `v25`/`v38`대). `app.py`는 추가했다가 전부 되돌려 최종적으로 무변경. `tests/test_prototype_ai_relay.py`는 생성 후 삭제.
- 검증: 매 변경 후 `node --check static/prototype.js`, `python -m py_compile app.py`. 삭제 후 `git diff app.py`가 빈 결과임을 확인. `python -m unittest discover -s tests` 33개(신규 14개는 관련 코드와 함께 삭제) 전체 통과.
- Git: 변경 사항 전부 미커밋.
- 남은 문제:
  - 생성 이력 UI(회차별 아코디언·체크박스 선택·PPT 다운로드)는 유지되어 계속 시뮬레이션 데이터로 동작함.
  - 실제 AI 연동은 이번 세션에서 시도했다가 삭제함 — 운영 서비스의 기존 `OPENAI_API_KEY` 서버 기반 생성 기능(7절 "OpenAI API 생성")은 결제·크레딧만 설정하면 이번 작업과 무관하게 별도로 테스트 가능.
  - Gemini `AQ.` 형식 키 인증 실패 원인은 끝내 못 찾음 — 프로토타입에 개인 키 기능을 다시 붙이게 되면 참고용으로 남겨둠(원인 후보: (a) `curl` 재현, (b) Google 공식 SDK 대조, (c) 다른 계정 키 비교).
- 다음 시작점: 운영 서비스 OpenAI 결제·크레딧 설정 후 기존 생성 기능 재시험(10절 1번과 동일). 프로토타입 쪽은 별도 요청 전까지 추가 작업 없음.

### 2026-08-06 16:57 · 분권세트형 확장, 1~5단계 다수 UX 수정, 4·5단계 원고 흐름 재설계

- 상태: 진행 중
- 시작 요청: "쪽수 차시 근거의 '수정' 버튼이 한쪽만 있어" 같은 세부 UI 버그 리포트들에서 시작해, 대화가 이어지며 (1) 중등체육을 초등처럼 분권세트형으로 만들고, (2) 1~3단계의 여러 UI 문제를 고치고, (3) 4단계(체제안 선택)·5단계(원고 편집)를 "실제 API 붙였을 때 쓸 수 있는 형태"로 재설계해 달라는 요청으로 확장됨. 마지막엔 "5단계가 불편하다(소단원이 너무 많아 일일이 클릭), 여러 명이 초안을 각자 검토할 방법이 있나"는 질문과 함께 "오늘은 여기까지 기록"으로 마무리.
- 작업 목적: 프로토타입(`static/prototype.*`)을 실제 운영 이관을 염두에 두고 구조적으로 더 정확하게 다듬고, 4~5단계를 지금 있는 시뮬레이션 수준을 넘어 "실제 AI API가 붙었을 때 그대로 쓸 수 있는 데이터 흐름"으로 만듦.
- 수행 방법·판단:
  - **분권세트형 확장**: 초등 3~4/5~6학년군은 이미 정상 동작 확인. 고등체육1·체육2(기존 완전히 별개 프로젝트 2개)와 중등체육(기존 미연결 단일 placeholder)을 초등 학년군과 같은 "분권 프로젝트"(책 프로젝트 1개, 내부에 책 2권) 구조로 통합. 처음엔 카탈로그를 `middle-pe-1`/`middle-pe-2`, `high-pe-1`/`high-pe-2` 각 2줄로 만들었다가, 사용자가 "초등처럼 카탈로그엔 한 줄만 있어야 한다"고 지적해 `middle-pe`/`high-pe` 단일 카탈로그 항목으로 되돌리고 `projectForCatalog`를 초등과 동일한 단순 구조로 재작성함. `isPairedProject`/`isPlaceholderProject` 헬퍼로 `type==="elementary-band"` 리터럴 비교를 일반화. 구형 localStorage(단권 고등체육1/2 두 프로젝트, 또는 단일 미연결 중등체육 프로젝트, 심지어 이번 세션 중간에 있었던 잘못된 2줄-카탈로그 상태까지)를 자동으로 새 구조로 병합하는 `migratePairedCatalogProjects` 마이그레이션을 추가함.
  - **중등체육을 편집 가능하게**: 중등체육은 성취기준 51개·272시간은 공식 자료로 있지만 교과서 쪽수 공식 기준이 없어 지금까지 1단계에서 완전히 막혀 있었음. 사용자가 "쪽수 미연결 안내만 넣고 직접 쪽수·차시를 입력할 수 있게 해달라"고 요청해, `pagePolicyFor()`에 중학교용 느슨한 정책(범위 제한 없음, 학년군 시수만 272로 검증)을 추가하고 1단계 책 카드 입력창을 활성화함. 그 결과 2단계(교육과정 확인)도 사용자가 열 수 있게 됐는데, 기존 `renderCurriculum()`이 중학교 케이스를 처리하지 않고 초등 로직으로 잘못 빠지는 실제 버그를 발견해 `renderMiddleCurriculum()`을 새로 만들어 51개 성취기준을 정식으로 보여주도록 분리함(기존엔 이 51개 목록이 임시로 1단계 안내 패널에 끼워져 있었는데, 이번에 제자리인 2단계로 옮기고 1단계에서는 뺐음).
  - **1단계 UI**: 학교급·교과목명 readonly 필드 제거(왼쪽 드롭다운과 중복). "전처리 공식자료 연결됨" 배너를 긴 문장 하나에서 카드형 통계 그리드(교과서·부록 기준/부속자료/시수 등 각각 박스)로 재설계, 숫자를 크게 강조. 중등체육처럼 공식 쪽수 자료가 없는 경우엔 같은 배너를 빨간색("공식 쪽수 자료 미연결 — 직접 입력한 값입니다")으로 표시. 소단원 카드의 쪽수/차시 합계 중복 표시(같은 숫자가 두 줄에 나옴)를 하나로 합치고 학년 제목 옆에 크게 배치.
  - **2단계 버그 수정**: (a) "쪽수·차시 근거 한 번에 보기" 안의 두 섹션 중 한쪽만 `renderEditableSource`(수정 버튼 있음)를 쓰고 한쪽은 고정 텍스트였던 것을 통일. 이 과정에서 STEP1과 STEP2가 같은 sourceId(`middle-pe-hours`)를 재사용해 한쪽을 수정하면 다른 쪽도 편집 상태로 같이 열리는 부작용을 발견해 sourceId를 분리함. (b) 성취기준 코드(`[12스문01-01]` 등)가 고정폭 90px 칸에 안 들어가 2줄로 줄바꿈되는 CSS 버그를 140px로 넓혀 수정(초·중·고 전부 같은 클래스 공유라 한 번에 적용됨). (c) 고등 스포츠 문화 2단계의 "별도의 '신체활동 예시' 표는 없습니다" 안내가 사실과 다름을 조사 에이전트로 원문 대조해 확인(표는 있으나 종목명이 "스포츠 종목 중 택 1 이상"으로만 적혀 있어 구체적 종목명이 없는 것) — 문구를 사실대로 수정함.
  - **3단계 UI**: "배열표 미리보기"·"목차 다운로드" 두 버튼 사이가 벌어지는 문제 원인이 `sectionHeading()`이 여러 버튼을 감싸는 wrapper 없이 그대로 flex 자식으로 넣어 `justify-content: space-between`이 버튼 사이도 벌려버린 구조적 버그임을 확인, `sectionHeading()` 자체에 `.section-actions` wrapper를 추가해 앞으로 버튼이 몇 개든 안전하게 만듦. 앞부속·뒷부속·부록 쪽수 입력 카드 3개를 단원 목록 위쪽에 한 번에 모아 3단 그리드로(반응형 760px 이하에서는 3단으로 쌓임) 재배치.
  - **다운로드 기능**: 성취기준·목차·배열표(신규) 다운로드를 CSV로 통일(엑셀에서 바로 열림, UTF-8 BOM 추가로 한글 깨짐 방지). 실제 .hwp/.hwpx는 100% 서버·외부 라이브러리 전용이라 프로토타입(서버 없음)에서 재현 불가능함을 확인하고 사용자 선택에 따라 CSV/텍스트로 대체함. 이 과정에서 `curriculumDownloadContent()`가 중학교 케이스를 처리하지 않던 것도 같이 발견해 고침.
  - **전체 폰트 크기**: 사용자가 "작은 글자들이 일반 사이트 평균 크기보다 작다"고 지적해 `prototype.css`의 10~13px 글자를 12~15px로 단계적으로 상향(순서를 큰 값부터 처리해 중복 치환 방지). 공용 `styles.css`(운영 화면과 공유)는 건드리지 않음.
  - **4단계(체제안 선택)·5단계(원고 편집) 재설계**: 사용자가 "AI초안 생성을 누르면 전체 원고가 나오기도, 선택한 것만 나오기도 해서 혼란스럽다"고 지적 → 원인은 4단계가 소단원 1개만 초안화하고 5단계는 그와 무관한 고정 샘플 1개를 보여주는 완전히 분리된 화면이었기 때문임을 확인. 사용자가 제안한 새 흐름(① 슬라이더 조정 → ② 소단원 선택 → ③ 생성하면 균형형·활동강화형·창의형 3개가 한 번에 나와 비교 → ④ 재조정하면 최신 3개로 교체 → ⑤ 하나를 승인하면 5단계에서 전체 원고 초고 작성)을 그대로 구현함. `state.frameworkComparison`(최신 3개 비교, 화면표시용)과 `state.frameworkDraftLog`(전체 생성 이력, 다운로드용)를 분리하고, `state.frameworkApproval`(프로젝트 전역 1회 승인, 1단계 팀장승인과 같은 게이트 패턴)을 추가해 승인 전에는 5단계 진입을 막음. 5단계(`renderManuscript`)를 기존 고정 샘플 1개 편집창에서 "책 전체 소단원 목록(41개 등) + 실제 편집 가능"으로 전면 교체하고, 승인된 체제 기준으로 소단원마다 AI 초안을 자동 채워 넣어 그걸 고쳐 쓰는 구조로 만듦. "여러 집필자가 자기 계정으로 작성"은 서버가 없는 프로토타입에서 근본적으로 불가능함을 설명하고, 대신 소단원마다 "담당 집필자" 이름 입력란만 넣는 가벼운 대체안으로 합의함. 이 과정에서 이제 완전히 안 쓰이게 된 옛 고정 샘플 원고 데이터(`state.manuscript`, `sportsCultureManuscript()` 등)를 정리함.
  - 3개 비교 화면에서 한 소단원이 여러 펼침면(예: 24쪽짜리는 12개)으로 나뉘는데, 이전 세션에서 "불필요한 글자 빼기" 작업 때 펼침면 구분 표시까지 다 지워서 마치 "다른 소단원이 섞여 나온 것"처럼 보인다는 리포트를 받음 — 각 펼침면 카드에 "펼침면 N" 작은 배지를 다시 추가해 해결.
  - **실제 API 연동을 위한 사전 설계**: 사용자가 "체제마다 준비물 같은 보조단이 있고 없고 하게 다양해야 하는데, 텍스트박스로 하기 애매해서 이미지로 하면 자유로운지" 질문 → 이미지 방식은 (a) 5단계에서 텍스트 수정이 안 되고 (b) 이미지 생성 모델이 긴 한글 문단을 정확히 못 그린다는 이유로 부적절함을 설명. 대신 지금 `support_boxes`/`activities`가 이미 "개수·종류 자유로운 배열" 구조로 돼 있음을 코드로 보여주고, 진짜 필요한 건 "이 배열을 프레임워크·슬라이더 지표에 따라 실제 LLM이 자유롭게 채우게 하는 것"이라고 정리함. 사용자가 "지금 계획은 내가 3개를 손으로 다르게 써두는 것뿐이지 진짜 LLM 기반이 아니지 않냐"고 정확히 반문해, 화면에 보이는 변화 없이 "나중에 실제 API를 붙일 자리에 쓸 프롬프트 설계"만 남기기로 합의(사용자가 "B만 해"로 확정). `simulatedSpreadDraft` 함수 바로 위에 실제 system/user 프롬프트 초안을 주석으로 남김(체제 정보·슬라이더 5개 지표·소단원 정보를 넣고, 보조단 구성을 모델이 자유롭게 정하게 하며, 지금 코드가 쓰는 것과 같은 JSON 모양으로 응답받아 화면 코드 수정 없이 바로 꽂을 수 있게 설계). 코드 동작 자체는 바뀌지 않음.
  - 캐시 문제: 이번 세션 내내 `prototype.html`의 `?v=` 캐시 버스팅 쿼리를 한 번도 안 올렸다가, 사용자가 "4단계 슬라이더·초안생성이 사라졌다"고 리포트한 일이 있었음. 헤드리스로 재현해보니 코드 자체는 정상이었고, 서버(`app.py`)도 정적 파일을 매 요청마다 디스크에서 새로 읽어 캐시 헤더 없이 보내는 것을 확인해 브라우저 캐시 문제로 결론, 이후 편집마다 `prototype.css`/`prototype.js`의 `?v=` 값을 올리는 습관을 들임(v14→v17, v20→v23).
- 수정 파일: `static/prototype.js`, `static/prototype.css`, `static/prototype.html` (전부 Git 미추적 유지). `app.py`·운영 화면·`scripts/build_prototype_curriculum_data.py`·`static/prototype-curriculum-data.js`는 이번 세션에서 건드리지 않음(읽기 참고만).
- 검증: 매 변경 후 `node --check static/prototype.js` 통과. Node `vm` 모듈로 실제 브라우저 없이 각 화면을 렌더링해 확인하는 방식을 계속 사용함(예: 분권 프로젝트 4종 전환, 중등체육 1~2단계 편집·검증 통과, 성취기준 51개 렌더링, STEP4 3개 비교→승인→STEP5 41개 소단원 카드 자동 채움, 초등/고등/스포츠문화/중등 전체 회귀 없음). `python -m unittest discover -s tests` 33개 전체 통과(매 변경 후 반복 확인, `app.py` 무변경이므로 항상 그린).
- Git: 변경 사항 전부 미커밋.
- 남은 문제:
  - **5단계 업무 효율화**: 소단원이 많은 책(초등 41개 등)은 `<details>` 아코디언을 하나씩 열어야 해서 불편하다는 지적을 받음(오늘 논의만, 구현 안 함). 개선 방향 후보: 담당자별/미완료만 보기 필터, 전체 펼치기·접기 토글, 검색/이동.
  - **여러 명이 초안을 각자 검토하는 방법**: 진짜 실시간 동시편집(구글독스식)은 프로토타입(서버 없음)에서 근본적으로 불가능하고, 확인해보니 운영 서비스도 실시간이 아니라 "저장 시점 버전 충돌 검사"(낙관적 잠금, 다른 편집자가 먼저 저장했으면 409 에러) 방식임을 조사로 확인함. 프로토타입 안에서 유일하게 가능한 대안은 "현재 상태를 파일로 내보내기 → 다른 사람이 자기 브라우저에 가져오기"인데, 이건 논의만 하고 구현하지 않음.
  - `support_boxes`를 프레임워크·슬라이더 지표에 따라 실제로 다르게 구성하는 로직(A안, "블록 목록" 구조 자체는 이미 있으나 프레임워크별 분기 로직은 없음)은 사용자가 "B만 해"로 보류함 — 필요해지면 진행.
  - 활동별 삽화(이미지) 생성은 논의만 하고 구현 안 함.
  - `HANDOFF.md` 5절("현재 구현")이 여전히 "6단계 최소비용 프로토타입"으로 남아 있던 것을 이번 세션에서 갱신함(5단계·분권세트형 4종·4~5단계 재설계 반영).
- 다음 시작점: 5단계 소단원 목록 UX 개선(필터·전체펼치기) 여부 확정 → 필요 시 진행. 다인 검토 방식(내보내기/가져오기 등) 필요 여부 확정. 그 외엔 이전 항목의 "체제안 개발 방향 슬라이더 미연결" 등 운영 서비스 쪽 기존 결함은 여전히 미해결로 남아 있음.

### 2026-08-05 17:13 · 프로토타입 단계 정리와 체제안·원고 미리보기

- 상태: 완료 (남은 문제였던 "4단계 드롭다운 중복"은 이후 세션에서 다른 방식으로 해결— 드롭다운을 없앤 게 아니라 4단계 전체를 "3체제 동시 생성·비교·승인" 구조로 재설계함. "HANDOFF.md 5절 갱신"도 2026-08-06 세션에서 처리함.)
- 시작 요청: 프로토타입(`/prototype.html`)의 3~4단계가 중복돼 보인다는 지적을 검토하고 필요하면 정리해 달라는 요청. 이어서 4단계(체제안 선택)에 소단원별 AI 초안 미리보기를 화면 흐름만 확인하는 수준으로 추가해 달라는 요청.
- 작업 목적: (1) 프로토타입 6단계 중 3단계(단원·차시 배분)와 4단계(쪽수 설계)의 실제 기능 차이를 확인하고 중복이면 정리, (2) 운영 서비스의 "개발 방향" 슬라이더가 AI 생성에 실제로 반영되는지 확인, (3) 프로토타입에 체제안 비교 지표와 소단원 선택 → AI 초안(펼침면) 미리보기 흐름을 실제 API 호출 없이 화면 흐름만 보이도록 추가.
- 수행 방법·판단:
  - 운영 앱(app.py)의 3/4단계(성취기준·차시 배분 vs 내용·종목 선정)와 프로토타입의 3/4단계(단원·차시 배분 vs 쪽수 설계)를 혼동하지 않도록 각각 코드로 직접 확인함. 프로토타입 4단계 "쪽수 설계"는 3단계 "배열표 미리보기" 팝업과 같은 `buildPagePlan()`을 쓰되, 팝업은 저장되지 않는 확인용 스냅샷이고 4단계는 영구 저장되는 개별 쪽 편집 기능이었음. 다만 그 편집 결과(state.pages의 영역·유형·상태)를 이후 화면(체제안 선택, 원고 편집) 어디에서도 읽지 않는다는 것을 확인해, 사용자 판단대로 4단계 제거를 진행함.
  - 4단계(쪽수 설계) 삭제: `renderPages`, `pageSummary`, `areaAllocationSummary`, `pageAreas`, `PAGE_TYPES`/`PAGE_STATUSES`, `pageEditor`와 관련 이벤트 핸들러(배열 재생성, 쪽 클릭/범위선택, 일괄편집), `lastClickedPage` 변수를 제거. `steps`/`renderers` 배열을 5단계로 재정렬(프로젝트 설정→교육과정 확인→단원·차시 배분→체제안 선택→원고 편집). 3단계 팝업 안내 문구에서 사라진 "4단계" 언급 삭제.
  - 운영 서비스의 "개발 방향" 화면(`static/direction.js`) 슬라이더(교육과정·실행·흥미·참신성·안전 5개 지표)가 `call_openai_for_small_unit`(app.py)의 프롬프트에 전혀 전달되지 않음을 코드로 확인함. 화면 문구가 주장하는 "선택한 안이 3단계(성취기준·차시 배분)의 기본 전략이 된다"는 것도 `generated_grade_allocation_payload`가 선택된 방향을 참조하지 않아 실제로는 지켜지지 않는 상태임을 확인함(기존 결함 발견, 이번 세션에서 수정하지 않음).
  - 프로토타입은 서버 호출이 전혀 없는 순수 클라이언트 코드라 API 키를 안전하게 쓸 수 없다는 점을 설명하고, 사용자 확인에 따라 이번엔 실제 API 호출 없이 화면 흐름만 보여주는 시뮬레이션으로 진행하기로 함.
  - 4단계(체제안 선택)에 프레임워크별 5개 비교 지표 슬라이더(운영 앱과 동일한 항목·동일한 드래그 중 로컬 갱신 패턴), 소단원 선택 드롭다운, "AI 초안 생성" 버튼을 추가함. 처음에는 결과 문구에 "가짜"라는 표현을 그대로 넣었으나, 사용자가 나중에 운영 서비스에 그대로 옮길 것을 지적해 버튼명·생성 문구·함수명(`simulatedManuscriptDraft`)·CSS 클래스(`draft-preview-result`)에서 "가짜" 표현을 모두 제거하고, "지금은 시뮬레이션"이라는 사실은 별도의 한 줄 안내(`.prototype-only-note`)에만 남겨 이후 포팅 시 그 줄만 지우고 API 호출로 바꾸면 되도록 구조화함.
  - 이어서 생성 결과를 실제 서비스와 같은 펼침면 레이아웃으로 바꿔 달라는 요청에 따라, `app.py`의 `small_unit_ai_schema`·`manuscript_spread_draft`와 `workflow.js`/`workflow.css`의 펼침면 렌더링을 그대로 참고해 소단원 쪽수 기반 다중 펼침면, 좌/우/양쪽걸침 활동 배치, 준비물·안전·전략(점검)·도움말 상자, 동일 CSS 클래스명을 프로토타입에 이식함(내용은 여전히 정적 템플릿 반복 — 다양성은 실제 AI 연결 후 과제로 남김).
  - 이후 사용자가 "3단계와 4단계가 왜 똑같이 나오냐", "6→5단계로 줄이기로 했는데 하나를 통째로 없앴다"고 문제 제기함. 확인 결과: (a) 진행률 텍스트가 `/6`으로 하드코딩되어 있어 5단계로 줄인 뒤에도 분모가 안 바뀌는 실제 버그를 발견해 `steps.length`를 참조하도록 수정함. (b) `steps` 배열은 정확히 5개 항목으로 정상이며 누락된 단계는 없음을 재확인하고, 6→5로 줄어드는 계산을 예시와 함께 설명함. (c) 진짜 남아있는 중복은 "4단계 소단원 선택 드롭다운"이 "3단계 소단원 카드 목록"과 같은 데이터를 다시 보여주는 것이라는 점을 짚어, 3단계 카드에 "이 소단원으로 초안 생성" 버튼을 추가하고 4단계 드롭다운을 없애는 방향을 제안했으나 아직 사용자 확정 답변을 받지 못해 구현하지 않음.
- 수정 파일: `static/prototype.js`, `static/prototype.css` (둘 다 Git 미추적 상태 유지, 이번 세션에서 추가 수정만 함)
- 검증: 매 변경 후 `node --check static/prototype.js` 통과, `python -m unittest discover -s tests` 33개 전체 통과, 로컬 서버(`http://127.0.0.1:8000`)에서 `/prototype.html`·`/prototype.js`·`/prototype.css` HTTP 200 확인. 헤드리스 브라우저 도구가 없어 실제 화면 렌더링은 눈으로 확인하지 못함.
- Git: 변경 사항 미커밋. `static/prototype.*`는 이전부터 미추적 상태.
- 남은 문제:
  - 4단계 "소단원 선택" 드롭다운과 3단계 소단원 카드 목록의 중복 — 제안한 정리(3단계 카드에 초안 생성 버튼 추가, 4단계 드롭다운 제거)에 대한 사용자 확정 필요.
  - 운영 앱의 "개발 방향" 슬라이더가 실제 AI 생성 프롬프트와 3단계 배분 로직 어디에도 연결되어 있지 않다는 기존 결함 발견(이번 세션에서 수정하지 않음, 별도 작업 필요).
  - `HANDOFF.md`의 "현재 구현"(5절)이 아직 "6단계 최소비용 프로토타입"으로 되어 있어, 실제로는 5단계로 바뀐 지금 상태와 불일치함 — 다음에 손볼 것.
- 다음 시작점: 4단계 드롭다운 중복 정리 여부 확정 → 필요 시 운영 앱의 개발 방향→배분/생성 연결 작업 → `HANDOFF.md` 5절 프로토타입 단계 설명 갱신.

### 2026-08-05 08:57 · 고등학교 「스포츠 문화」 분석과 초안 준비

- 상태: 진행 중
- 시작 요청: 오늘 작업을 기록하면서 고등학교 체육 선택 과목인 「스포츠 문화」의 과목 분류, 기준 시수, 교과서 분량을 확인하고 이후 초안 제작 준비.
- 작업 목적: 2022 개정 교육과정의 공식 근거를 바탕으로 「스포츠 문화」가 고등학교 교육과정에서 어떤 선택 과목인지 확정하고, 차시·분량 가정과 초안 범위를 결정할 자료를 정리.
- 수행 방법·판단: 저장소의 전처리 공식자료 중 「체육과 교육과정」과 「초등학교 교육과정」의 고등학교 편제·학점 배당 및 「스포츠 문화」 본문을 우선 대조. 초등 체육용 검정기준의 120쪽 기준을 고등학교 과목에 임의 적용하지 않았고, 서울특별시교육청 인정도서 공고와 출판사별 2022 개정판 정보를 추가 대조함.
- 수정 파일: `WORKLOG.md`, `HANDOFF.md`, `.gitignore`, `official-data/README.md`, `scripts/preprocess_official_documents.py`, `scripts/build_prototype_curriculum_data.py`, `static/prototype.js`, `static/prototype.css`, `static/prototype-curriculum-data.js`, `tests/test_app.py`, `official-data/processed` 신규 5종.
- 검증: 「스포츠 문화」가 선택 중심 교육과정의 진로 선택 과목이며 별표 과목으로 기본 2학점임을 전처리 원문에서 확인. 1학점은 50분 수업 16회이므로 기본 운영량은 32차시이며 1학점 범위에서 감축 편성이 가능함. 교육과정은 ‘스포츠 인문 문화’와 ‘스포츠 경기 문화’ 2개 영역, 성취기준 6개로 구성됨. 추가 전처리한 서울교육연구정보원 인정기준에서 공식 기준 분량이 부록 포함 180쪽, ±10%(162~198쪽)임을 확인함. 출판사 도서의 전체 쪽수 192·194·200쪽은 부속자료 포함 여부를 원본과 별도로 대조해야 함.
- 추가 확인: 초등 체육 3~4학년군의 공식 기준 수업 시수는 2년간 204시간(1시간 40분, 연간 34주 기준)이며 체육은 감축할 수 없음. 학년별 배분은 학교 자율이므로 3학년 102차시·4학년 102차시는 공식 고정값이 아니라 현재 프로젝트의 균등 배분값임. 교과서 분량은 3학년책·4학년책 각각 부록 포함 기준 120쪽, 허용 범위 108~132쪽이고 기타 부속자료는 책별 최대 10쪽이므로 겉표지 제외 전체 최대는 책별 142쪽임.
- 자료 색인: 「스포츠 문화」의 진로 선택 분류와 기본 2학점 근거는 `official-data/processed/초등학교_교육과정/document.md` PDF 원본 34~35쪽, 과목 성격·목표·2개 영역·성취기준 6개·교수학습 및 신체활동 예시는 `official-data/processed/체육과_교육과정/document.md` PDF 원본 87~92쪽에 있음. 인정도서 구분과 출판사별 실제 쪽수는 현재 저장소 전처리 자료가 아니라 서울특별시교육청 공고 및 각 출판사 도서 정보에서 확인함.
- 전처리 자료 재점검: 저장소에는 `[별책2] 초등학교 교육과정`, `[별책11] 체육과 교육과정`, `체육 2022 편찬상의 유의점 및 검정기준`, `검정도서 개발 가이드라인(평가원 검토용)`, `편수자료 II 인문사회과학·체육·음악·미술편`의 5종만 전처리되어 있음. 고등학교 「스포츠 문화」 개발을 위해 우선 추가할 후보는 `[별책1] 초·중등학교 교육과정 총론`, `[별책4] 고등학교 교육과정`, `2022 개정 인정도서 인정 실시 공고·편찬상의 유의점 및 인정기준`, `편수자료 I 편수 일반편`이며, `편수자료 III 기초과학·정보편`은 과학·정보 내용까지 다룰 때 후순위로 추가 검토함.
- 추가 전처리: 사용자가 `official-data/criteria`에 넣은 자료 중 `[별책1] 초·중등학교 교육과정 총론`, `[별책4] 고등학교 교육과정`, 서울교육연구정보원 인정도서 편찬상의 유의점 및 인정기준, 인정도서 심의본 접수·수수료 안내, 접수 질의응답의 5종을 추가하여 총 10종으로 확장함. HWP/HWPX/PDF 공용 처리기 `scripts/preprocess_official_documents.py`를 추가했으며 HWP 내부 제어문자를 제거하고 문서 구역·문단 순서를 보존함. HWP/HWPX는 자동 쪽수 색인이 불가능하므로 최종 인용 시 원본 대조 경고를 남김. 새 체육 편수자료Ⅱ PDF는 기존 자료와 중복되고 이미지 PDF라 빈 중복 산출물을 만들지 않음. 원본 폴더는 `.gitignore`로 제외함.
- 전처리 검증: 신규 5종 모두 `document.md`, `chunks.jsonl`, `manifest.json`, `review.csv`, `report.md` 생성 및 JSON 파싱 확인. 공식자료 검색 결과 총 10종, 원본 해시 확인 5종·전처리 사본 사용 가능 5종으로 인식됨. 전체 자동 테스트 33개와 `git diff --check` 통과.
- 스포츠 문화 프로토타입 추가: `/prototype.html`에서 학교급을 고등학교로 바꾸면 「스포츠 문화」만 활성화되도록 연결함. 프로젝트 설정은 교과서·부록 180쪽을 기본으로 162~198쪽을 검증하고, 별도 부속자료 기준이 확인되기 전까지 부속자료는 0쪽으로 잠금. 교육과정 단계에는 진로 선택·기본 2학점·32차시·2개 영역·성취기준 6개와 해설·자료 색인을 표시함. 단원 단계에는 32차시와 본문 176쪽·부록 4쪽의 편집 가능한 초기 배분안을 넣고, 쪽수 배열·체제안·스포츠 문화 전용 샘플 원고까지 6단계 전체 흐름을 연결함. 다른 중·고등 과목은 자료 연결 전 상태로 유지함.
- 프로토타입 검증: 생성 스크립트가 스포츠 문화 성취기준 6개와 해설을 추출하는지 확인. JavaScript 6개 화면 렌더링 점검, 32차시·단원 176쪽·전체 배열 180쪽 일치, 162쪽 미만·198쪽 초과·부속자료 1쪽 입력 차단 확인. `node --check static/prototype.js`, 전체 자동 테스트 33개, 후행 공백 검사와 `git diff --check` 통과.
- 책 프로젝트 분리: 단일 덮어쓰기 저장을 `ai-textbook-studio:v0.2:projects` 다중 프로젝트 구조로 변경함. 초등은 3~4학년군 또는 5~6학년군 비교형으로 생성해 두 학년책을 독립 저장하고 같은 단계를 읽기 전용으로 나란히 비교하며, 스포츠 문화는 비교 기능 없는 단일형으로 생성함. 프로젝트 생성 순서는 학교급 → 학년군(초등만) → 과목명 → 프로젝트명이며, 생성 후 책의 정체성 값은 잠그고 이름만 수정함. 기존 v0.1 단일 저장값은 해당 학년군 또는 스포츠 문화 프로젝트로 자동 이전하고, 초기화는 현재 프로젝트만 대상으로 함.
- 전처리 분량 정책 연결: 생성 스크립트가 초등 체육 편찬기준·검정도서 가이드라인과 스포츠 문화 인정기준 전처리 문서에서 기준값·원문·자료 색인을 확인해 정적 데이터의 `page_policies`로 생성하도록 함. 새 프로젝트 선택 즉시 초등 120쪽·108~132쪽·부속자료 0~10쪽 또는 스포츠 문화 180쪽·162~198쪽·부속자료 미확인 상태를 표시하고, 생성 후 1단계에도 동일 근거와 원문을 표시함. 연결되지 않은 과목은 임의 수치를 만들지 않는 기존 원칙을 유지함.
- 다중 프로젝트 검증: 3~4학년책 각각 102차시·독립 상태, 프로젝트명 동기화, 스포츠 문화 32차시·180쪽, 기존 4학년 단일 저장값의 3~4학년군 이전, 학교급 → 과목명 → 프로젝트명 표시 순서, 전처리 정책 120/132·180/198 로드를 Node 상태 검사로 확인. 전체 자동 테스트 33개, JavaScript·Python 문법 검사, 프로토타입 HTML·JS·CSS·생성 데이터의 로컬 HTTP 로드와 `git diff --check` 통과.
- 1단계 제작 프로필 적용: 초등 학년군 분권형은 공통 프로젝트 정보 아래 3·4학년 또는 5·6학년의 두 책 카드를 동시에 표시하고, 책별 본문·부록·부속자료·차시 수정과 분량 기준 및 두 책 차시 합계 204 검증을 연결함. 스포츠 문화 선택과목 단권형은 한 권 카드에 180쪽 기준·162~198쪽 허용·기본 2학점 32차시·16차시까지 감축 가능 조건을 표시함. 새 프로젝트 선택 미리보기에도 전처리 쪽수·차시와 자료 색인을 추가함.
- 1단계 승인 흐름: 프로젝트 설정을 `편집 중 → 팀장 승인 대기 → 승인 완료`로 저장하고, 승인 시 설정 스냅샷과 버전을 남기며 입력을 잠금. 승인본에서 수정본을 만들면 기존 승인 스냅샷을 보존한 채 다시 승인하도록 구성함. 실제 로그인 역할 연결 전이므로 현재 버튼은 프로토타입 시뮬레이션임을 화면에 명시하고, 승인 완료 전에는 다음 단계 이동을 차단함.
- 새 프로젝트 설정 접근성 수정: 기존 프로젝트의 학교급·학년군·과목명은 데이터 손실 방지를 위해 구분값으로 유지하되, 잠금 이유와 새 프로젝트 생성 경로를 1단계에 명시함. 현재 프로젝트의 설정값이 유효하지 않아도 `새 프로젝트 설정`은 항상 열리게 했으며, 브라우저 대화상자 의존성을 없애고 본문 상단에 학교급 → 학년군 → 과목명 → 프로젝트명 순서의 인라인 패널을 펼치도록 변경함. 초등·고등 전환, 과목 옵션, 전처리 쪽수·차시 근거 갱신과 패널 열기 상태를 Node DOM 모사로 확인함.
- 새 프로젝트 선택 구조 정정: 별도 학년군 선택란을 제거하고 `학교급 → 교과목명 → 프로젝트명`으로 단순화함. 초등학교의 교과목명 선택지는 `3~4학년군`, `5~6학년군`이고 고등학교는 `스포츠 문화`이며, 선택값으로 초등 분권형 또는 고등 단권형 프로젝트를 생성함. 기존 프로젝트 1단계도 학교급 다음에 동일한 교과목명 구분값만 표시하도록 정리함.
- 책 프로젝트 선택 단순화: 별도 새 프로젝트 설정 패널을 제거하고 좌측 `책 프로젝트` 드롭다운을 교육과정의 체육 관련 목록으로 고정함. 초등 체육 3~4학년군·5~6학년군, 중등 체육, 고등 체육1·체육2·운동과 건강·스포츠 문화·스포츠 과학·스포츠 생활1·스포츠 생활2를 표시함. 초등 두 학년군과 스포츠 문화는 선택 즉시 전처리된 쪽수·차시가 1단계에 자동 입력되며, 나머지는 임의 값을 만들지 않고 빈 입력과 `공식자료 미연결` 상태를 표시해 다음 단계 이동을 막음. 제거한 패널의 이벤트 참조와 스타일도 함께 정리했으며 JavaScript 문법 검사, 전체 자동 테스트 33개, `git diff --check`를 통과함.
- 중·고등 전처리 데이터 연결: 「체육과 교육과정」에서 중등 성취기준 51개와 고등 7과목 성취기준 48개를 과목·영역별로 추출하고, 총론의 중등 체육 272시간 및 고등 선택과목 학점 규정과 인정도서 기준의 과목별 180쪽(162~198쪽)을 정적 데이터로 생성함. 고등 체육1·체육2·운동과 건강·스포츠 생활1·2는 기본 3학점 48차시, 스포츠 문화·스포츠 과학은 기본 2학점 32차시로 자동 입력하며 교육과정 확인부터 단원·쪽수·원고 화면까지 과목별 데이터로 연결함. 중등은 공식 교과서 분량 자료가 현재 없어 272시간과 성취기준 수만 표시하고 쪽수 입력 및 다음 단계는 차단함. 고등 7과목의 설정 검증·차시 합계·180쪽 배열 및 대표 4과목의 6단계 렌더링을 Node 모사로 확인했고, JavaScript·Python 문법 검사, 전체 자동 테스트 33개, `git diff --check`를 통과함.
- 스포츠 문화 기존 저장값 보정: 전처리 정적 데이터에는 스포츠 문화 180쪽·162~198쪽 기준이 정상 생성되어 있었으나, 브라우저 `localStorage`에 이전 버전의 미연결 프로젝트나 빈 쪽수 값이 남아 있으면 새 연결값이 적용되지 않을 수 있어 로드·선택 시 자동 승격 로직을 추가함. 사용자가 입력한 값은 유지하고 빈 쪽수·차시만 180쪽·32차시로 채우며, 미연결 프로젝트 형식은 같은 프로젝트 ID와 이름을 유지한 채 단권형으로 전환함.
- 초·중·고 근거 원본 쪽수 표시: 프로젝트 설정·교육과정 확인·단원 근거에 자료명, PDF 원본 쪽, 문서 인쇄면, 추출 원문을 함께 표시하도록 통일함. 중등 체육 272시간은 PDF 28쪽·인쇄면 20쪽, 고등 선택과목 학점은 PDF 35쪽·인쇄면 27쪽으로 연결하고, 중등 성취기준 51개와 고등 과목별 성취기준·해설에는 각 원본 쪽수를 개별 표시함. 인정기준 HWP는 전처리에서 과목별 페이지 경계가 보존되지 않으므로 임의 페이지를 만들지 않고 원본 목차에서 확인되는 `고등학교 체육 20~45쪽` 범위와 과목별 소제목 대조 경고를 표시함. 전체 테스트 33개와 초·중·고 대표 화면의 원본·인쇄면 표시 검증을 통과함.
- 쪽수·차시 근거 동시 열기: 프로젝트 설정의 쪽수 근거와 차시 근거를 별도 접이식 카드 두 개에서 `쪽수·차시 근거 한 번에 보기` 카드 하나로 합침. 한 번 펼치면 두 근거의 자료 색인과 원문이 연속해서 보이며, 초등과 고등 전체 과목에 동일하게 적용함. 대표 초등·고등 프로젝트에서 접이식 카드 1개와 내부 근거 구역 2개가 생성되는지 확인하고 JavaScript 문법 검사와 `git diff --check`를 통과함.
- 근거 카드 반응형 2단 배치: 하나의 접이식 카드는 유지하면서 펼친 쪽수 근거와 차시 근거를 PC에서는 좌우 2단으로 표시하고 세로 구분선을 추가함. 화면 폭 760px 이하에서는 위아래 1단과 가로 구분선으로 자동 전환되도록 함. 초등·고등 렌더 구조, JavaScript 문법과 `git diff --check`를 확인함.
- 스포츠 문화 목차·차시 자동 복구: 스포츠 문화 교육과정에는 초등처럼 별도 `신체활동 예시` 표가 없고 2개 영역·성취기준 6개가 제시된다는 안내를 2단계에 추가함. 이를 근거로 스포츠 인문 문화 15차시(이해, 역사·철학, 문학·예술과 진로), 스포츠 경기 문화 17차시(역할, 대회 기획·운영, 문화 융합)의 32차시 목차 가안을 사용함. 이전 브라우저 저장값에서 단원 또는 페이지 배열이 비어 있으면 사용자 편집값이 없는 부분에 한해 이 자동안을 복구하도록 함. 빈 이전 프로젝트에서 2개 영역·32차시·180쪽과 3단계 표시가 복원되는지 확인하고 JavaScript 문법 검사와 `git diff --check`를 통과함.
- 고등 부속자료 임시 입력 허용: 인정기준에서 별도 부속자료 상한을 확인하지 못한 고등 과목은 기본값 0쪽을 유지하되, 프로젝트 설정이 `편집 중`일 때 편집자가 0 이상의 임시 쪽수를 입력할 수 있도록 변경함. 공식 상한 검증은 적용하지 않고 화면에 `공식 기준 미확인·편집자 임시값`을 표시하며, 팀장 승인 완료 후에는 다른 설정값과 함께 잠김. 스포츠 문화 7쪽 입력의 설정 유효성, 편집 가능 상태와 승인 후 잠금 상태를 확인하고 JavaScript 문법 검사와 `git diff --check`를 통과함.
- 배열표 미리보기 최소 수정: 기존 고정 팝업 구조를 유지하면서 팝업 내부 스크롤 시 헤더와 닫기 버튼이 상단에 계속 보이도록 `sticky` 처리함. `Esc` 키로도 닫을 수 있게 하고 사용자가 기능을 발견할 수 있도록 버튼 문구를 `닫기 · Esc`로 표시하며 접근성 설명을 추가함. JavaScript 문법 검사와 `git diff --check`를 통과함.
- 인수인계 기록 정리: 사용자 요청에 따라 오늘 구현한 프로젝트 드롭다운, 중·고등 전처리 연결, 원본 쪽수 근거, 스포츠 문화 목차·차시 가안, 고등 부속자료 임시 입력, 근거 카드 반응형 표시와 배열표 닫기 UX를 `HANDOFF.md`의 현재 구현에 반영함. 세부 변경과 검증 결과는 이 세션의 기존 항목에 이어서 보존함.
- Git: `main`, 최신 커밋 `aa967d1`; 기존 미커밋 프로토타입 파일은 보존하며 오늘 기록만 추가.
- 남은 문제: 인정기준 HWP에서 「스포츠 문화」 구간의 실제 원본 쪽수 대조, 편수자료Ⅰ 추가 확보, 32차시를 두 영역과 도입·정리·평가에 배분하는 편집 원칙 결정.
- 다음 시작점: 기준 2학점·32차시와 부록 포함 180쪽(162~198쪽 허용)을 바탕으로 두 영역과 성취기준 6개를 대단원·중단원·소단원 구조에 배치.

## 완료 세션

### 2026-07-30 · GitHub 병합과 Vercel 최종 배포

- 상태: 완료
- 시작 요청: 미리보기 확인 후 최종 배포 요청.
- 작업 목적: 검증된 `agent/supabase-collaboration` 변경을 정식 `main` 브랜치와 운영 사이트에 반영.
- 수행 방법·판단: Vercel Preview 검사 성공과 GitHub 병합 가능 상태를 확인하고 PR #1을 Draft에서 준비 상태로 전환한 뒤 merge commit 방식으로 병합함. Vercel Git 연동이 시작한 Production 배포의 상태와 정식 별칭을 확인함.
- 수정 파일: 이 배포 결과를 기록한 `WORKLOG.md`.
- 검증: PR #1 병합 커밋 `e07caf6`; Production 배포 `dpl_HgiTbE9oYqMyodjANXo7AXiuwzqm` Ready; `https://textbook-studio.vercel.app/api/health` HTTP 200; 정식 첫 화면 HTTP 200; 로그인 전 `/api/bootstrap` HTTP 401로 인증 보호 확인.
- Git: `main` 브랜치에 병합 완료. 이 기록 커밋을 추가 푸시한 뒤 최종 Production 상태를 다시 확인.
- 남은 문제: 없음.
- 다음 시작점: 정식 사이트에서 편집자 로그인 후 공식자료 5종과 기존 프로젝트 데이터 확인.

### 2026-07-30 · 발표용 페이지와 재생성 파일 정리

- 상태: 완료
- 시작 요청: PT용으로 만들었던 파일을 삭제하고 그 외 불필요한 파일도 정리 요청.
- 작업 목적: 실제 교과서 제작·공동 편집에 필요한 파일만 남겨 저장소와 배포 구성을 단순화.
- 수행 방법·판단: 발표용 `/ai-journey` HTML·CSS·JavaScript, 로그인 연결 링크·전용 스타일·서버 라우트를 제거. 추가 정리는 다시 생성되는 Python 캐시 4곳과 내용 없는 서버 로그 2개로 제한함.
- 보존 항목: 전처리 데이터, 원본 연동 구조, `data/studio.db`, 백업 DB, `.env.local`, `.vercel`, HWPX 템플릿.
- 수정·삭제 파일: `static/journey.*`, `static/login.html`, `static/login.css`, `app.py`, `HANDOFF.md`, `WORKLOG.md`; 로컬 `__pycache__`와 빈 로그.
- 검증: 발표용 경로·파일·스타일 참조 0건. 전체 자동 테스트 33개, Python·JavaScript 구문 검사, `git diff --check` 통과. 테스트가 다시 만든 Python 캐시도 재삭제함.
- Git: `agent/supabase-collaboration` 브랜치의 `5c8d18c` 커밋에 포함하여 `origin/agent/supabase-collaboration`로 푸시 완료.
- 남은 문제: 없음.
- 다음 시작점: 실제 제작 화면과 공동 편집 흐름만 유지하며 계속 검증.

### 2026-07-30 · Git pull만으로 전처리 자료 사용

- 상태: 완료
- 시작 요청: 다른 컴퓨터에서 Git pull 후 전처리 데이터가 없다는 문제를 해결하고 사용하기 쉬운 구조로 변경 요청.
- 작업 목적: 저장소를 받은 어느 컴퓨터에서도 별도 폴더 복사 없이 공식 전처리 자료 5종을 즉시 사용.
- 수행 방법·판단: 원본 PDF 45MB는 저장소에서 제외하고, 실행에 필요한 전처리 산출물 약 8MB만 `official-data/processed`에 포함. 원본이 있으면 SHA-256을 검증하고, 없으면 `processed_only` 상태로 구분해 제작 기능을 허용함.
- 수정 파일: `official-data/**`, `scripts/sync_official_data.py`, `app.py`, `static/app.js`, `vercel.json`, `README.md`, `HANDOFF.md`, `WORKLOG.md`, 테스트.
- 검증: 5종·1,213쪽·3,973청크 동기화 완료. 개인 컴퓨터 절대경로 0건 확인. 원본 없는 환경에서 5종 모두 `processed_only`로 사용 가능함을 모사. 현재 환경에서 원본 해시 5종 확인 및 성취기준 49개 로드. 전체 자동 테스트 33개 통과, Python·JavaScript 구문 및 `git diff --check` 통과.
- Git: `agent/supabase-collaboration` 브랜치의 `5c8d18c` 커밋에 포함하여 `origin/agent/supabase-collaboration`로 푸시 완료.
- 남은 문제: 원본 PDF 대조는 `criteria`를 별도로 둔 컴퓨터에서만 가능.
- 다음 시작점: 새 컴퓨터에서 pull 후 `start-studio.cmd` 실행 → 공식자료 5종 표시 확인.

### 2026-07-30 · Git push 전 작업 기록 자동화

- 상태: 완료
- 시작 요청: 사용자가 “깃푸시해줘”라고 요청할 때 이전까지의 작업 내용을 자동으로 핸드오프에 남기도록 요청함.
- 작업 목적: 별도 지시 없이도 푸시 전에 작업 맥락을 기록하고, 기록 없는 코드 푸시를 차단함.
- 수행 방법·판단: AI 작업 규칙은 대화와 diff를 읽어 의미 있는 기록을 작성하도록 `AGENTS.md`에 명시함. Git Hook은 작업 의미를 추측하지 않고 `WORKLOG.md` 누락만 검사하는 안전장치로 분리함.
- 수정 파일: `.githooks/pre-push`, `setup-hooks.cmd`, `AGENTS.md`, `HANDOFF.md`, `WORKLOG.md`
- 검증: `pre-push` 구문 확인. 기록 없이 코드 변경이 있는 과거 커밋 범위는 종료 코드 1로 차단하고, 변경이 없는 범위는 종료 코드 0으로 통과함을 확인. 현재 저장소의 `core.hooksPath=.githooks` 설정 완료.
- Git: `ba02d86 Add durable handoff and push logging` 커밋을 `origin/agent/supabase-collaboration`에 푸시 완료.
- 남은 문제: 새 컴퓨터에서는 `setup-hooks.cmd`를 한 번 실행해야 로컬 Hook이 활성화됨.
- 다음 시작점: 이후 “깃푸시해줘” 요청부터 별도 설명 없이 작업 기록 갱신 → 검증 → 커밋 → 푸시 순서로 처리.

### 2026-07-30 · 지속형 핸드오프 체계 도입

- 상태: 완료
- 시작 요청: 컴퓨터·대화창이 바뀌고 Git push/pull을 해도 작업 맥락이 이어지도록 처음부터 현재까지의 핸드오프와 향후 세션 기록 체계를 요청함.
- 수행 방법·판단: 전처리 5종의 매니페스트·보고서, 저장소 README, 현재 코드, Git 커밋 이력을 대조함. 추정 내용은 배제하고 확인 가능한 사실과 현재 미해결 문제를 기록함.
- 수정 파일: `AGENTS.md`, `HANDOFF.md`, `WORKLOG.md`, `README.md`
- 검증: 민감정보 값 미기록, 전처리 문서 수·쪽수·청크 수·경고 수 대조, 브랜치·원격 커밋 대조.
- Git: `ba02d86` 커밋에 포함하여 `origin/agent/supabase-collaboration`에 푸시 완료.
- 남은 문제: OpenAI API 쿼터 부족으로 실제 원고 생성이 아직 성공하지 않음.
- 다음 시작점: 사용자가 다음 “작업 시작”을 알리면 이 문서에 세션을 만들고 작업을 이어감.

### 2026-07-27~30 · AI 원고 생성과 선택 소단원 로딩

- 상태: 완료(실제 생성은 외부 쿼터 문제로 보류)
- 시작 요청: 반복적인 고정 문구를 줄이기 위해 실제 AI를 연결하고, 전체 원고 대신 원하는 소단원만 표시하도록 요청함.
- 수행 방법·판단: OpenAI Responses API와 JSON Schema 구조화 출력을 서버에서 호출하도록 구성. 브라우저에는 소단원 카탈로그만 보내고, 선택한 소단원의 펼침면만 별도 API로 불러오도록 분리함.
- 수정 내용: 학년·대단원·중단원·소단원 선택기, 선택 소단원 저장·AI 생성·HWPX 출력, 전체 원고 초기 전송 축소, 구체적인 한국어 API 오류 안내.
- 검증: 전체 자동 테스트 30개 통과. 키 인식과 OpenAI 도달 확인. 실제 호출 결과는 `429 quota/billing`.
- Git: 관련 코드는 `aee2f9a` 이후 커밋들에 포함되어 원격 브랜치에 반영됨.
- 남은 문제: API 결제/크레딧 설정 후 생성 품질 확인 필요.
- 다음 시작점: 크레딧 반영 후 소단원 1건을 생성하고 반복도·활동 다양성·쪽수 보존을 확인.

### 2026-07-27 · 공동 편집·배포·로그인 안정화

- 상태: 완료
- 시작 요청: 코드는 우선 혼자 수정하되, 배포 후 여러 교과서 편집자가 공동으로 사용하고 데이터를 누적하도록 요청함. 별도 서버형 호스팅 없이 무료 계정 중심을 원함.
- 수행 방법·판단: GitHub는 코드 이력, Vercel은 웹 배포, Supabase는 영속 데이터와 인증을 담당하도록 분리함. 편집자 승인 목록과 버전 충돌 방지를 추가함.
- 수정 내용: Vercel Python Function, Supabase Postgres/Auth, 편집자 관리, 로그인·회원가입·비밀번호 재설정, SQLite→Postgres 이전 스크립트.
- 검증: GitHub 인증·푸시, Vercel 연결, Supabase 리디렉션 설정, 로그인 오류 수정.
- Git: `7b28a6d`, `1e584cb`, `333d05e`.
- 남은 문제: 새 배포 주소가 생기면 Supabase Redirect URL 재확인.
- 다음 시작점: 실제 편집자 계정 2개로 동시 편집·충돌 동작을 수동 검증.

### 2026-07-27 · HWPX 출력과 진행 표시

- 상태: 완료
- 시작 요청: 분석과 원고 초안을 HWP/HWPX로 내보내고, 긴 처리 중 멈춘 것처럼 보이지 않도록 진행 상황을 표시해 달라고 요청함.
- 수행 방법·판단: 빈 HWPX 템플릿을 기반으로 순수 Python에서 문서를 구성하고, 표 중심 출력과 선택 소단원 출력을 추가함. API 요청을 감싼 공통 진행 오버레이를 적용함.
- 수정 내용: 전체 단계별 HWPX 다운로드, 표 가독성, 선택 소단원 출력, 처리 퍼센트·진행 막대.
- 검증: HWPX ZIP 패키지 구조·본문 추출 자동 검사 및 한글 2024 열기 확인.
- Git: `a303ab7`, `aee2f9a`, `dfb3a52`.
- 남은 문제: 실제 편집팀 양식에 맞춘 스타일 세부 조정.
- 다음 시작점: 회사별 표 스타일과 원고 전달 양식을 받아 HWPX 템플릿을 조정.

### 2026-07-26~27 · 1~9단계 웹서비스 구현

- 상태: 완료
- 시작 요청: 교육과정 분석부터 원고 초안까지 각 단계에서 AI가 첫 안을 만들고 편집팀이 웹에서 수정하며, 이전 단계도 다시 고칠 수 있는 서비스를 요청함. 자동검증·최종검토는 독립 모의심사처럼 동작해야 함.
- 수행 방법·판단: 제작 흐름을 1~9단계로 나누고, 각 단계에 웹 편집·버전·승인 게이트를 연결함. 모의심사는 제작 데이터와 공식자료는 공유하되 평가 판단은 분리함.
- 주요 수정: 프로젝트 설정, 교육과정 분석, 개발 방향, 성취기준·차시 배분, 내용·종목 선정, 목차·분량, 단원 설계, 원고 초안, 자동검증·모의심사.
- 편집 규칙: 영역→대단원, 세부 영역→부제, 신체활동 예시→중·소단원 토대. 학년 간 공통 종목 선정 금지. 중·소단원 카드 복제와 중복 허용.
- 사용성 수정: 전체 글자 확대, 단계 제목 크기 통일, 중복 학년 UI 제거, 선택 상태 의미 명확화.
- 원고 수정: 한 쪽 단위가 아니라 좌우 펼침면, 펼침면당 활동 2~3개, 안전·전략·평가·정리 구성.
- Git: 초기 구현 `950c73a`, 이후 확장 `aee2f9a`.
- 다음 시작점: 실제 편집 업무로 반복 사용하며 화면·생성 규칙을 계속 보정.

### 2026-07-16 · 공식 PDF 5종 전처리

- 상태: 완료
- 시작 배경: 교육과정과 평가원 자료를 이후 질문과 웹서비스의 공통 근거 데이터로 사용하기 위해 PDF 전처리를 먼저 수행함.
- 원본: `../criteria`의 체육과 교육과정, 초등학교 교육과정, 체육 편찬상의 유의점 및 검정기준, 검정도서 개발 가이드라인, 편수자료 II.
- 수행 방법·판단: PyMuPDF 기본 텍스트·표 추출을 우선 사용하고, 기본 추출이 어려운 평가원 가이드라인 11쪽은 OCR 처리. 깨진 한글 매핑 복원, 머리말 제거, 제목 계층·표·원본 페이지를 구조화함.
- 산출물: 자료별 `document.md`, `chunks.jsonl`, `manifest.json`, `review.csv`, `report.md`.
- 검증: 원본 SHA-256 기록, 총 1,213 PDF쪽과 3,973개 청크 생성, 경고 페이지는 `review.csv`에 보존.
- 수정 파일: `../preprocess_criteria.py`, `../processed/**`.
- 남은 문제: 경고·OCR 페이지를 원본과 사람이 계속 대조해야 함.
- 다음 시작점: 전처리를 다시 돌리기보다 기존 산출물을 우선 검색하고, 원본과 불일치가 발견될 때만 해당 범위를 교정.
### 2026-08-13 · 프로토타입 02·03·04 단계 화면 중복 오류 수정

- 상태: 완료
- 증상: 02에서 03 또는 04로 이동해도 이전 화면이 남아 세 단계가 같은 화면처럼 보임.
- 원인: 03단계 `renderUnits()`에서 초기화되지 않은 `smallUnits`를 조회해 `ReferenceError`가 발생했고, 새 HTML 계산이 끝나기 전에 예외가 나 기존 작업 화면이 유지됨.
- 수정: `smallUnitOptions()` 결과를 먼저 선언하고, 작업 화면 선삭제 및 비교 보기 상태의 `try/finally` 복원을 추가함.
- 회귀 방지: 03단계에서 소단원 목록 초기화가 조회보다 먼저 오는지 확인하는 테스트를 추가함.
- 검증: `node --check static/prototype.js`, `python -m unittest discover -s tests -p 'test_*.py'` 통과(53개). Edge 실브라우저 자동 확인 결과 02·03·04 제목과 본문 제목이 모두 서로 다름.
- 후속 수정: 스포츠 문화의 소단원 유형 추천 변수를 03단계가 아닌 04단계에서 초기화하도록 이동하고 정적 파일 버전을 `v47`로 갱신함.
- 후속 브라우저 검증: 고등 스포츠 문화 04단계에서 체제안 카드 3개, 주 소단원 유형 선택 상자, `체제안 선택` 본문이 정상 출력됨.

### 2026-08-13 · 스포츠 문화 종목과 소단원 목차 연결

- 상태: 완료
- 문제: 3단계에서 종목을 선택해도 어느 소단원과 목차에 적용되는지 보이지 않고, 04단계가 프로젝트의 전체 종목 문자열만 사용함.
- 수정: 상단 선택은 책의 후보 종목으로 정의하고, 18개 소단원 카드마다 적용 종목 체크 칩과 적용 문구를 추가함.
- 연결: `small.sports`를 저장 데이터와 `smallUnitOptions()`에 포함하고 04단계 표시·초안 생성의 `carrierSport`가 해당 소단원 배정값만 사용하도록 변경함.
- 출력: 목차 CSV에 `적용 종목` 열을 추가함.
- 이전: 기존 스포츠 문화 데이터의 소단원에는 현재 선택 종목을 기본 배정하고, 후보에서 제거된 종목은 소단원 배정에서도 정리함.
- 검증: 전체 테스트 53개 통과. Edge 실브라우저에서 소단원 카드 18개/종목 배정 필드 18개/기본 배정 18개, 목차 CSV 종목 열, 04단계 배드민턴 연결을 확인함.

### 2026-08-13 · 스포츠 문화 실무형 원고·편집디자인 산출물 전환

- 상태: 완료
- 문제: 기존 캔버스는 구조 확인용 상자뿐이고 본문·지도서 원고도 활동 문구 수준이라 출판 실무에 사용할 수 없었음.
- 정보 구조: 체제안은 책 전체 콘셉트로 유지하고, 소단원 성격은 이론형/실기형/혼합형으로 재분류. 문학·예술/토론/프로젝트/융합은 수업 구성 강조로 분리함.
- 교과서 원고: 이론형은 역사·철학·사회 맥락의 본문, 실기형은 종목별 시설·규격·규칙·기본 동작·전략·안전, 혼합형은 개념과 참여를 연결하는 4절 원고를 생성함.
- 지도서 원고: 교과서 데이터에서 목표·준비물·수업 흐름·유의점·발문·예상 답변·개별화·안전·평가를 파생함.
- 디자인: 펼침면 PNG를 1400×900으로 확대하고 단원 띠, 대형 쪽수, 제목, 학습 목표, 생각 열기, 번호형 본문, 곡선형 하단 비주얼과 이미지 발주 문구가 포함된 교과서 편집 시안으로 변경함.
- 출력: 체제별 `PNG 저장`, `교과서 원고 TXT`, `지도서 원고 TXT`, 기존 PPT를 각각 제공함. 실제 사진·삽화는 저작권과 API 제약 때문에 발주 슬롯으로 분리함.
- 검증: JS 2종 문법 검사, 전체 자동 테스트 53개 통과. Edge에서 새 결과 3체제×3펼침면=캔버스 9개와 원고 다운로드 버튼 3세트를 확인함.

### 2026-08-13 · 체제 비교 카드 머리글 깨짐 수정

- 상태: 완료
- 원인: 약 450px 카드 안에서 긴 체제·소단원명과 다운로드 버튼 3개를 한 줄 flex로 배치해 제목 폭이 한 글자 수준으로 축소됨.
- 수정: 카드 최소 폭을 720px로 확보하고 제목과 버튼을 두 줄 grid로 분리함. 제목은 한글 어절 단위 줄바꿈, 버튼은 줄바꿈 가능한 별도 행으로 배치함.
- 비교 방식: 3개 카드를 억지로 한 화면에 축소하지 않고 결과 영역 내부 가로 스크롤과 스냅으로 비교하도록 변경함.
- 검증: 실제 Edge 1500px 뷰포트에서 카드 720px, 제목 690×23px, 버튼 영역 690×40px, 내부 가로 스크롤 활성 상태를 확인함.

### 2026-08-13 · 고등 수준 원고 확장 및 펼침면 반복 제거

- 상태: 완료
- 원인: `textbookManuscriptFor()`가 펼침면 index를 내용에 사용하지 않고 유형별 동일 4절을 반환해 쪽수만큼 같은 원고가 반복됨.
- 전개 규칙: 이론형은 개념→역사→철학·가치→사회 쟁점→자료 비평→현대 확장, 실기형은 종목 문화→시설·안전→규칙·판정→기본 동작→전술·역할→운영·평가, 혼합형은 개념·경험→역사·환경→규칙·가치→동작 실천→다양한 역할→비평·창작으로 구분함.
- 내용 확장: 성취기준 6개마다 핵심 개념·역사 맥락·쟁점·산출물을 정의하고 펼침면당 4절×3문단으로 조합함. 종목 정보와 탐구·참여 균형형/대회 프로젝트형/문화 융합형의 서로 다른 과제도 반영함.
- 지도서: 각 펼침면의 실제 제목과 본문으로 목표, 도입·전개1·전개2·정리, 발문, 예상 답변을 별도 생성함.
- 품질 검사: 테스트에서 펼침면 제목과 지도서 핵심 발문의 전면 중복 금지, 펼침면당 본문 최소 900자를 확인함. 10쪽 실기형 표본은 5개 펼침면이 서로 다른 주제이며 각 1,056~1,133자로 생성됨.
- 검증: JS 문법 검사와 전체 테스트 54개 통과. 엔진 schema `1.1`, 캐시 `v4`.

### 2026-08-13 · 실제 교과서 분량 대조와 반복 문장 품질 검사

- 원인 보완: 앞선 원고 확장에서 펼침면 주제는 달라졌지만 공통 설명·자료 분석·과제 문장으로 분량을 채워 같은 말이 반복되었다.
- 실측: 전처리 스포츠 문화 교과서 5종의 유효 본문 펼침면 중앙값은 출판사별 약 1,333~1,693자였다. 유형별 중앙값은 이론 1,457자·실기 1,592자·혼합 1,583자였다.
- 수정: 이론·실기·혼합형 각각의 펼침면 역할에 대응하는 고유 심화 내용 뱅크를 추가하고, 근거 자료·분석 관점·학생 과제를 펼침면 위치에 따라 다르게 조합했다.
- 품질 검사: 한 원고 안에서 35자 이상 동일 문장을 제거하고 `manuscriptQuality`에 펼침면별 글자 수와 중복 문장 수를 기록한다. 자동 테스트에 본문 최소 1,400자와 중복 문장 0건 조건을 추가했다.
- 상세 기준: `docs/sports-culture-spread-density-rules.md`
- 버전: 엔진 schema `1.3`, 브라우저 캐시 `v6`.

### 2026-08-13 · 단일 전체 스타일과 소단원 다중 초고 생성

- 스포츠 문화 4단계에서 체제안 3개 비교·승인을 제거하고 `안정·정석형 ↔ 균형형 ↔ 참신·활동형` 전체 스타일 슬라이더 하나로 교체했다.
- 각 소단원에 이론형·실기형을 저장하고, 이론형은 `활동 포함`, 실기형은 `개념 설명 포함` 보조 구성을 선택할 수 있게 했다.
- 체크박스로 표본 소단원 하나 또는 여러 소단원을 선택해 초고를 단일·일괄 생성하도록 변경했다. 생성 결과는 소단원마다 한 개만 생성한다.
- 대단원마다 도입·마무리 생성 후보를 자동 제공하고, 읽을거리·인물·진로·안전·문화 비평·프로젝트·수행평가 특별 페이지를 추가·삭제·생성할 수 있게 했다.
- 분량 검사는 페이지 역할별로 분리했다. 표본 결과는 대단원 도입 460자, 마무리 742자, 특별 페이지 604자이며 동일 문장 중복은 모두 0건이다.
- 검증: JS 문법 검사, 패치 형식 검사, 전체 자동 테스트 57개 통과. 엔진 schema `1.4`, CSS 캐시 `v36`, 엔진 캐시 `v7`, 화면 JS 캐시 `v51`.

### 2026-08-14 · 목차 기반 종목 사용 방식 분리

- 전처리 교과서 목차를 비교해 종목 독립 단원형, 개념·종목 실천 분리형, 종목 중심 실천형, 대표 종목 연결형의 공통 규칙을 정리했다.
- 상단의 종목 선택은 책의 후보 목록으로 변경하고, 모든 소단원에 후보 전체를 자동 배정하던 동작을 제거했다.
- 소단원별 종목 사용 방식을 `종목 없음`, `여러 종목 사례`, `주 종목 중심`으로 분리했다. 사례형은 복수 선택, 주 종목형은 단일 선택을 사용한다.
- 초기 18개 목차에는 전처리 패턴에 따라 종목 없음 2개, 여러 종목 사례 9개, 주 종목 중심 7개가 추천된다.
- 사용자가 수정한 목차·차시·쪽수·종목 설정을 보존하며, `현재 목차명으로 다시 추천`을 누른 소단원만 다시 분석한다.
- 생성 엔진은 종목 없음에서 종목을 요구하지 않고, 사례형에서는 모든 선택 종목을 비교하며, 주 종목형에서만 시설·규칙·동작 정보를 한 종목에 연결한다.
- 목차 CSV와 교과서·지도서 TXT에도 종목 사용 방식과 종목을 구분해 출력한다.
- 상세 기준: `docs/sports-culture-sport-application-rules.md`
- 검증: JS 문법 검사, 패치 형식 검사, 전체 자동 테스트 61개 통과. 엔진 schema `1.5`, CSS 캐시 `v37`, 엔진 캐시 `v8`, 화면 JS 캐시 `v52`.

### 2026-08-14 · 근거 기반 생성 엔진 분리(사후 기록, 코덱스 세션)

- 상태: 완료(다른 CLI 도구 세션에서 수행, 이 기록은 사후 재구성)
- 배경: "전처리한 교육과정·교과서·지도서 분석 결과가 실제 생성 로직에 반영되지 않고 화면·분량 파라미터만 계속 조정된다"는 지적에 따라, 생성기 핵심을 전처리 근거에 직접 연결하도록 재설계함.
- 수정 내용:
  - `static/prototype-draft-engine.js`(신설, 기존 `prototype.js` 내부 생성기를 분리·재작성)에 `evidenceRecordFor()`(소단원 제목→전처리 근거 매칭, 정확 일치 실패 시 키워드 스코어링으로 재시도), `textbookManuscriptFor()`(교과서 원고), `teacherGuideFor()`(지도서 원고), `sourceEvidenceFor()`(근거 목록)를 구현.
  - 18개 소단원별 `UNIT_CONTENT_PLANS`(펼침면 전개 각도 7개씩)와 `UNIT_THESES`(소단원 주제문)를 신설해 소단원마다 다른 집필 각도를 갖도록 함.
  - 소단원 제목에 매칭되는 전처리 근거가 없으면 일반 문장으로 때우지 않고 `throw`로 생성을 중단하도록 함.
  - `scripts/build_sports_culture_evidence.py`(신설)가 4개 출판사(미래엔·지학사·동아·천재) 22개정 스포츠 문화 교과서·지도서 전처리 결과(`official-data/processed/22개정_*_{textbook,teacher_guide}`)에서 소단원별 키워드 매칭으로 근거를 추출해 `static/sports-culture-evidence.js`(18개 소단원×36개 근거)를 생성.
  - `tests/test_prototype_draft_engine.py`(신설, 22개 테스트)로 근거 연결·추적성·펼침면 분량을 검증.
  - 캔버스 와이어프레임의 원형 장식을 이미지 배치 상자로 변경하고 미리보기 이미지의 본문 노출량을 확대.
- 검증: 전체 자동 테스트 67개 중 66개 통과. 1개 실패 — `test_internal_provider_generates_traceable_textbook_and_guide_draft`가 펼침면 최소 분량 1,400자 기준에서 911자로 미달.
- 남은 문제: (1) 위 분량 실패, (2) `WORKLOG.md` 갱신 없이 세션이 중단됨(이 항목이 그 사후 기록), (3) `sourceEvidenceFor()`가 근거 미매칭 시 조용히 범용 출처로 대체하여 "근거 없으면 생성 중단" 원칙과 불일치.
- 다음 시작점: 911자 회귀의 원인 진단, 근거 폴백 일관성 정리, 이론형·실기형·특별페이지 실제 생성 결과의 육안 검증.

### 2026-08-14 · 펼침면 분량 회귀 수정과 근거 폴백 정리

- 상태: 완료
- 배경: 위 세션이 남긴 "22개 중 1개 실패(911자 vs 1,400자 기준)"를 진단하고, 코덱스가 못 끝낸 이론형·실기형·특별페이지 실제 결과 검증까지 마무리함.
- 원인 진단: `removeRepeatedManuscriptSentences()`가 원고 전체(한 소단원의 모든 펼침면)를 대상으로 35자 이상 동일 문장을 1회만 남기고 제거하는데, `textbookManuscriptFor()`의 여러 문장(실기형 `interpretation` 전체, 이론형 `interpretation`의 `context.core` 절, `sourceLine`의 `thesis` 절, `detailLines`·`learningTask`의 일부 절)이 펼침면 번호나 소단원 내 전개 각도(`angle`)를 전혀 참조하지 않아 매 펼침면·매 절마다 글자 그대로 반복되었다. 그 결과 첫 펼침면만 문장이 온전히 남고 이후 펼침면은 반복 문장이 통째로 제거되어 분량이 계속 줄었다(실측: 펼침면 3개 중 마지막이 911자, 이론형 6펼침면 표본은 마지막이 717자까지 하락).
- 수정: 위 문장들을 모두 `angle`(펼침면마다 다른 전개 각도)을 포함하는 하나의 문장으로 재구성해, 반복 제거 로직이 같은 문장으로 인식하지 않도록 함. `stripEnd()` 헬퍼를 추가해 인용 문장의 마침표를 제거한 뒤 더 큰 문장에 자연스럽게 이어붙임(단, 이어붙일 때 마침표 누락으로 문장이 붙어버리는 문제가 있어 정확한 위치에 마침표를 다시 삽입).
- 부수 발견·수정: 위 반복 제거 로직이 `13.40 m`처럼 소수점이 있는 실제 종목 규격(`SPORT_REFERENCE`)을 `13. 40 m`로 잘못 분리하는 버그가 있었음(마침표를 문장 구분자로 쓰는 정규식이 소수점을 오인). `removeRepeatedManuscriptSentences()`에 소수점 보호 치환을 추가해 수정.
- `sourceEvidenceFor()` 근거 폴백 정리: 소단원(pageRole `small-unit`) 요청에서 근거가 없으면 `textbookManuscriptFor()`와 동일하게 오류로 생성을 중단하도록 통일. 대단원 도입·마무리·특별 페이지(소단원 단위 근거가 원래 존재하지 않는 범위)는 여전히 범용 출처로 대체하되, 각 항목에 `matchedSmallUnit: false`를 표시하고 role에 `[대단원 수준 일반 참고]` 접두를 붙여 소단원 전용 근거와 혼동되지 않게 함.
- 검증: 이론형(보조 구성 없음/`활동 포함`), 실기형(보조 구성 없음/`개념 설명 포함`), 특별 페이지(대단원 도입·마무리·커스텀 특별 페이지) 총 7개 조합을 직접 생성해 펼침면 분량·중복 문장 0건·근거 연결을 육안 확인. 전체 자동 테스트 67개 전부 통과(`python -m unittest discover -s tests -v`).
- 알려진 남은 항목(이번 세션에서 다루지 않음): 지도서 학습목표 문장의 조사 결합(`~과`/`~와` 등)이 일부 부자연스러움(`static/prototype-draft-engine.js`의 `teacherGuideFor()` 인근), `manuscriptParagraphs()` 함수는 현재 어디서도 호출되지 않는 죽은 코드로 남아 있음.
- 수정 파일: `static/prototype-draft-engine.js`.
- 다음 시작점: 사용자 확인 후 8/13~14 작업분(엔진 신설분 포함)을 한 번에 커밋할지 결정.

### 2026-08-14 · 소단원 간 원고 문체가 똑같다는 지적 재확인 및 본문 생성 방식 교체

- 상태: 완료(사용자가 추가 검증 요청, 실제로 문제가 남아 있었음)
- 배경: 위 세션에서 분량·중복 버그를 고쳤다고 보고했으나, 사용자가 브라우저에서 다시 생성해도 "초안이 똑같다"고 재차 지적함. 원인은 두 가지였다.
  1. `prototype.html`의 `/prototype-draft-engine.js?v=9` 캐시 버전을 안 올려서, 브라우저가 수정 전 파일을 계속 쓰고 있었음(→ `v=10`, 이후 `v=11`로 갱신).
  2. 진짜 문제: `textbookManuscriptFor()`가 여전히 "출처 인용 + 빈칸 채우기" 4문장 골격(정의 확인 요청→자료 확인 요청→해석 지침→과제 지시)을 모든 소단원·모든 펼침면에 동일하게 적용하고 있었고, 성취기준별 고정 문장(`context.core` 등)을 조사만 바꿔 반복 사용하고 있었다. 실제 전처리 교과서(미래엔 고등 스포츠 문화 등)의 서술은 문단마다 역할이 다르고(정의→사례→비교→활동), 완전히 다른 문장으로 쓰여 있어 이 골격 방식 자체가 "사람이 보면 똑같다"고 느끼는 근본 원인이었다.
- 발견: 같은 파일에 이미 어디서도 호출되지 않는 죽은 함수 `manuscriptParagraphs()`(및 `MANUSCRIPT_BLUEPRINTS`, `SPREAD_ENRICHMENTS`)가 있었고, 여기 담긴 문장들은 역할(이론형 6단계: 핵심 개념 세우기·역사적 형성 과정·철학과 가치 탐구·사회 제도와 쟁점·자료 비교와 비평·현대적 확장과 성찰, 실기형도 6단계)별로 실제로 다르게 쓰인 프로즈였다. 8/14 근거-인용 재설계 때 이 엔진을 안 쓰고 더 기계적인 인용 골격으로 교체하면서 orphan 코드가 됐던 것으로 보인다.
- 수정: `textbookManuscriptFor()`가 각 펼침면마다 `blueprintIndex = 펼침면 순번 % 6`으로 역할을 순환시키고, 그 역할의 실제 절 제목(`MANUSCRIPT_BLUEPRINTS[유형][blueprintIndex].titles`)과 본문(`manuscriptParagraphs()`의 `detailParagraph`/`analysisParagraphs`/`evidenceParagraph`/`taskParagraph`)을 그대로 살려 쓰도록 바꿈. 근거 인용(출판사·자료유형·쪽수, 지도서 근거)은 각 절의 시작 문장(`sourceLine`)과 `evidenceParagraph`·`taskParagraph`에 실제 매칭된 근거로 계속 연결되도록 유지함(추상적 "evidenceKind" 라벨 대신 실제 `first.publisher`/`sourcePdfPage`를 인용).
- 부수 수정: `manuscriptParagraphs()` 안에서 성취기준 고정 문장(`context.core`/`history`/`issue`)과 종목 규격(`reference.facility`/`rule`)을 맨 문장으로 그대로 반환하던 슬롯들이 펼침면마다 반복될 위험이 있어, `angle`을 받아 감싸도록 하고 `stripEnd()`로 마침표를 정리해 이어붙임. `analysisParagraphs[0]`이 `detailParagraph`와 같은 `context.core` 문장을 한 절 안에서 두 번 반복하는 문제도 발견해 제거함.
- 검증: 서로 다른 3개 소단원(개념형·이론형-축구·실기형-배드민턴)을 직접 생성해 절 제목·본문이 실제로 다른 문장인지 육안 확인. 전체 자동 테스트 67개 통과.
- 남은 한계(다음에 볼 것): `analysisParagraphs`/`evidenceParagraph`/`taskParagraph`의 마무리 문장(예: "핵심 주장, 근거, 예상되는 반론…의 관계로 재구성하여 ~의 완성도를 높인다")은 여전히 절 번호(sectionIndex)에만 따라 정해지는 공통 지시문이라, 18개 소단원 전체에서 그 부분만은 계속 반복된다. 지금은 나머지 문장(사실 설명·근거 인용)이 실제로 다르므로 이전보다는 낫지만, 완전히 없애려면 이 마무리 문장도 소단원·역할별로 따로 써야 한다.
- 수정 파일: `static/prototype-draft-engine.js`, `static/prototype.html`(캐시 버전).

### 2026-08-14 · 규칙 조합기 한계 재확인과 실시간 AI 생성 경로 신설

- 상태: 진행 중(코드는 완료, 실제 OpenAI 키로 라이브 호출 검증은 사용자의 키 등록 이후로 남음)
- 배경: 위 세션에서 "메타 문장(출처 인용·자기 제목 되받기)을 실제 설명문으로" 고친 뒤에도 사용자가 "여전히 분석글 같다, 도입·마무리·특별페이지는 더 이상하다, 그냥 GPT/클로드에 써 달라고 하면 낫다"고 지적함. 규칙 기반 문자열 조합기는 아무리 템플릿을 다양화해도 실제 언어모델이 쓰는 자연스러운 글을 만들 수 없다는 근본 한계를 재확인함.
- 확인차 이 세션(Claude)이 실제로 소단원 2개(개념형 "스포츠 문화의 의미와 형성", 실기형 "가치·전략과 경기 참여자의 역할"·배드민턴)와 단원 도입·마무리·특별페이지(문화 비평·인물진로) 샘플을 실제 전처리 근거를 바탕으로 직접 집필해 제시했고, 사용자가 방향은 승인함.
- 그런데 사용자가 핵심 제약을 지적함: 4단계는 `전체 스타일` 슬라이더(안정·정석형/균형형/참신·활동형)로 같은 내용을 다른 문체로 비교하는 것이 목적이고, 3단계에서 소단원명·적용 종목을 자유롭게 수정할 수 있다. 미리 손으로 쓴 고정 원고는 스타일 비교도, 종목·제목 변경 반영도 안 된다 — 정적 텍스트(품질↑ 유연성↓)와 규칙 조합기(유연성↑ 품질↓)가 서로 배타적이라는 점을 정확히 짚음.
- 결정: 생성 시점에 실제 언어모델 API를 호출하는 새 공급자를 추가해 두 요구를 동시에 만족시키기로 함(사용자 선택: 새 OpenAI API 키 발급 후 연결, 기존 인프라 재사용).
- 구현:
  - `app.py`: `call_openai_for_sports_culture_manuscript()`(+ `sports_culture_manuscript_schema()`, `call_prototype_sports_culture_manuscript()`)를 기존 `call_openai_for_small_unit()`(초등 체육 운영 서비스의 OpenAI Responses API 구조화 출력 패턴)과 동일한 구조로 신설. 오류 처리(인증/쿼터·결제/타임아웃/네트워크)도 동일한 한국어 안내 문구를 재사용함. 새 엔드포인트 `POST /api/prototype/sports-culture-manuscript`는 `/api/prototype/review`와 같이 로그인 없이 호출 가능(프로토타입 성격).
  - 근거 매칭(`evidenceRecordFor`, 소단원 제목 기반)은 그대로 클라이언트(JS)가 담당하고, 서버는 클라이언트가 조립해 보낸 소단원·종목·스타일·전개각도·근거 발췌문을 받아 실제 문장만 새로 쓴다 — 즉 "무엇을 근거로 쓸지"는 로컬 데이터가 정하고, "어떻게 문장으로 표현할지"만 AI가 맡는 구조.
  - `static/prototype-draft-engine.js`: 새 공급자 `external-ai-v1`(`externalAiGenerate`)을 `registerProvider`로 등록. 펼침면마다 전개각도(`angle`)·역할별 절 제목(`MANUSCRIPT_BLUEPRINTS`)·근거 발췌문 묶음(펼침면마다 다른 근거 5~6개 윈도)을 조립해 서버로 보내고, 받은 문장을 기존 스키마(sections/paragraphs/headline 등)에 그대로 꽂아 넣어 지도서 파생(`teacherGuideFor`)·분량 검사·HWPX 출력 등 나머지 파이프라인을 그대로 재사용함.
  - 프롬프트에 이번 세션에서 확인된 문제를 전부 금지 규칙으로 명시함: 본문에 출판사·쪽수 인용 금지, 절 제목을 본문에서 되받아 설명 금지("이 절의 핵심은~"), 제공 근거에 없는 사실 창작 금지, 종목 유무·스타일 값에 따른 문체 차등, 펼침면 간 반복 금지.
  - `static/prototype.js`: "생성 제공자" 드롭다운은 이미 `providerOptions()`를 그대로 반영하도록 만들어져 있어 코드 추가 없이 새 공급자가 자동으로 선택 목록에 나타남. 다만 "외부 API · 추후 연결"이라는 비활성 안내 문구가 남아 있어 삭제하고 안내 문구를 갱신함.
  - 프론트엔드 UI/스키마는 전혀 바꾸지 않음 — `HANDOFF.md`에 이미 기록된 "비동기 공급자 등록 계약, 향후 외부 API는 서버에서 인증 정보를 관리하는 별도 공급자로 추가, 화면과 결과 스키마는 그대로 유지" 설계를 그대로 따름.
- 검증: `python -m py_compile app.py`, `node --check` 양쪽 JS, 전체 자동 테스트 67개 통과(기존 내부 공급자 경로는 변경 없음, 새 공급자는 테스트에서 호출하지 않음).
- 남은 문제: 이 컴퓨터에는 `.env.local`이 없어 `OPENAI_API_KEY`가 미설정 상태. 사용자가 새 키(결제수단 연결된 것)를 발급해 프로젝트 루트(`textbook-studio/textbook-studio/.env.local`)에 `OPENAI_API_KEY=...`로 등록해야 실제 라이브 호출 검증이 가능함. 특별페이지(도입·마무리·특별)는 아직 기존 고정 문구(`SPECIAL_PAGE_COPY`)를 그대로 쓰며 AI 경로로 연결하지 않음 — 소단원 경로가 실제로 검증된 뒤 다음 단계로 확장 예정.
- 다음 시작점: 사용자가 `.env.local`에 키를 등록하면, 실제 브라우저에서 "가치·전략과 경기 참여자의 역할"(배드민턴) 등으로 생성 제공자를 "AI 실시간 생성"으로 바꿔 생성해 보고 실제 문장 품질을 확인한다. 문제 없으면 특별페이지도 같은 방식(서버 AI 호출)으로 연결한다.
- 수정 파일: `app.py`, `static/prototype-draft-engine.js`, `static/prototype.js`, `static/prototype.html`(캐시 버전).

### 2026-08-14 · 실제 키 등록 후 라이브 호출 검증과 테스트 격리 버그 수정

- 상태: 완료(엔드포인트·서버 코드는 정상 확인, 실제 생성은 OpenAI 결제 설정 대기)
- 사용자가 새 OpenAI 키를 `.env.local`에 등록(카드 결제는 아직 미완료)한 뒤, 로컬 서버를 재시작(코드가 메모리에 로드된 뒤 추가된 라우트는 재시작해야 반영됨)하고 `curl`로 `/api/prototype/sports-culture-manuscript`에 실제 근거 데이터를 담아 라이브 요청함. 결과: 404(엔드포인트 없음) → 서버 재시작 → 400 `"OpenAI API 사용 가능 금액이 없습니다"`로 정상적으로 도달함. 즉 라우팅·요청 스키마·키 인식은 정상이고, 남은 건 OpenAI 계정의 결제/크레딧 설정뿐임.
- 부수 발견: `.env.local`을 실제로 만들어 두자 `tests/test_prototype_pdf_review.py`의 `test_call_openai_for_pdf_review_missing_key`가 실패함. 이 테스트는 `os.environ`에서 `OPENAI_API_KEY`만 지우면 "키 없음" 상태가 된다고 가정했는데, `secret_environment_value()`는 env var가 없으면 `APP_DIR/.env.local` 파일로 폴백하므로, 실제 키가 든 로컬 `.env.local`이 있으면 이 테스트가 진짜 OpenAI 호출을 시도하게 됨(테스트가 개발자 로컬 환경의 실제 비밀 파일 존재 여부에 암묵적으로 의존하고 있었다는 뜻). `secret_environment_value` 자체를 테스트 동안만 스텁으로 바꾸도록 수정해 파일 존재 여부와 무관하게 격리되도록 고침.
- 보안 처리: 키 값이 IDE 파일 변경 알림을 통해 대화에 노출되어, 사용자에게 키 폐기 후 재발급을 안내함. 이후 사용자가 파일 탐색기로 직접 새 키를 입력함(대화에는 값이 노출되지 않음).
- 검증: 전체 자동 테스트 67개 통과.
- 수정 파일: `tests/test_prototype_pdf_review.py`.
- 다음 시작점: 사용자가 OpenAI 결제 수단·크레딧을 등록하면, 동일한 방식으로 다시 라이브 호출해 실제 생성 문장의 품질을 확인한다.

### 2026-08-19 · 전체 스타일 버그 수정, 실사용 검증, 스타일 선택 UI 단순화, 펼침면 미리보기 HTML/CSS 전환

- 상태: 완료
- 전체 스타일 버그: `sportsCultureStyleProfile()`(`prototype.js`)에서 "안정·정석형"(0~33)과 "균형형"(34~66)이 둘 다 `id: "balanced"`를 반환해, 문체 분기에 쓰이는 유일한 지점(`manuscriptParagraphs`의 `taskVariants[framework.id]`)에서 두 스타일이 실질적으로 같은 결과를 냈다. 균형형에 `id: "activity"`를 부여해 세 스타일이 각자 다른 결과를 내도록 수정.
- OpenAI 결제 등록 후 실사용 검증: 같은 소단원(가치·전략과 경기 참여자의 역할·배드민턴)을 "안정·정석형"과 "참신·활동형"으로 각각 실제 생성해 비교. 인용·자기 지시적 문장 없이 자연스러운 설명문으로 나왔고, 두 스타일이 문체·전개 방식에서 실제로 다르게 나옴을 확인(정석형은 정의부터 차분히 전개, 활동형은 장면·질문으로 시작해 활동을 강조).
- 스타일 선택 UI 단순화: 1~100 슬라이더는 숫자상 100단계처럼 보이지만 실제로는 3단계(0~33/34~66/67~100)로만 분기해 결과가 나오므로, 숫자 표시가 있는 그레인지 없는 정밀도를 암시하는 문제가 있었음(사용자 지적). `#bookStyleSlider`(range input)를 3개 버튼(`.book-style-option`, 대표값 15/50/85)으로 교체하고 숫자 라벨(`#bookStyleValueLabel`)과 `framework.name`의 숫자 표기를 제거함.
- 펼침면 미리보기 캔버스 → HTML/CSS 전환: 기존엔 `<canvas>`에 좌표를 계산해 픽셀로 직접 그려서(`drawDraftSpreadCanvas`) 인쇄물이 아니라 슬라이드처럼 보인다는 지적(사용자: "PPT처럼 구리다")을 받음. 새 함수 `renderSpreadPageView()`가 같은 데이터(헤드라인·학습목표·생각 열기·절 4개·시각자료 발주)를 실제 HTML/CSS로 그려 브라우저가 폰트·줄바꿈·간격을 처리하게 함. AI 이미지 생성은 검토했으나 텍스트가 많은 지면에는 부적합(글자 왜곡·오류 위험)하다고 판단해 채택하지 않음 — 이 전환은 순수 렌더링 변경으로 추가 API 비용이 들지 않음.
  - 기존 `<canvas>`는 DOM에 유지하되 화면에서는 숨기고(`display:none`), PNG 저장 버튼(`data-download-draft-png`)은 그대로 이 캔버스의 `toDataURL()`을 사용하도록 유지함(내보내기 기능 하위 호환).
  - "크게 보기" 확대 시 `.spread-page-view`가 커지도록 `.expanded` 규칙을 캔버스 대상에서 새 뷰 대상으로 변경함.
- 회귀 방지: `tests/test_prototype_draft_engine.py`의 `test_sports_draft_studio_uses_one_global_slider_and_bulk_targets`가 제거된 `id="bookStyleSlider"`를 찾고 있어 실패함 → `data-book-style-value=`/`book-style-option` 마커를 확인하도록 수정(테스트명도 `..._style_picker_...`로 변경).
- 검증: `node --check static/prototype.js`, 전체 자동 테스트 67개 통과. 실제 브라우저 렌더링(스크린샷)은 이 세션에서 직접 확인하지 못했으므로, 사용자가 강력 새로고침 후 육안 확인 필요.
- 수정 파일: `static/prototype.js`, `static/prototype.css`, `static/prototype.html`(캐시 버전), `tests/test_prototype_draft_engine.py`.
- 다음 시작점: 브라우저에서 새 3버튼 스타일 선택과 새 HTML/CSS 펼침면 미리보기가 실제로 잘 보이는지 확인. 문제 없으면 이번 변경분 커밋·푸시.

### 2026-08-19 · 대단원 도입·마무리·특별 페이지도 AI 실시간 생성 경로에 연결

- 상태: 완료
- 배경: 사용자가 실사용 확인 중 "특별페이지는 AI로 생성해도 그대로다"를 지적함 — 의도된 상태였음(소단원 원고만 `external-ai-v1`에 연결했고 특별 페이지는 여전히 고정 문구 사용). 사용자가 특별페이지도 스타일별로 다르게, AI가 실제 교과서 원고처럼 쓰도록 요청함.
- 구현(`externalAiGenerate`, `prototype-draft-engine.js`): `pageRole !== "small-unit"`일 때 더 이상 `specialPageManuscriptFor()`(고정 문구)로 바로 빠지지 않고, 소단원 경로와 같은 방식으로 서버 AI를 호출함.
  - 근거: 특정 소단원에 매칭되는 근거가 없으므로(도입·마무리·특별 페이지는 대단원 전체를 다룸), `UNIT_CONTENT_PLANS`의 18개 키를 대단원별로 9개씩 나눠(순서상 앞 9개=스포츠 인문 문화, 뒤 9개=스포츠 경기 문화) 해당 대단원 소속 소단원들의 교과서 근거를 모아 폭넓게 근거로 제공함.
  - 절 제목·역할은 기존 `SPECIAL_PAGE_COPY`/`SPECIAL_PAGE_CONTINUATION_COPY`의 도입/마무리/특별 페이지별 제목을 그대로 사용해 화면 구조는 유지함.
  - 서버(`app.py`, `call_openai_for_sports_culture_manuscript`)에 `page_role`을 전달해, 프롬프트가 "지금 쓰는 지면은 소단원 본문/대단원 도입/대단원 마무리/특별 페이지"임을 구분해 인지하도록 안내문을 추가함. 도입·마무리·특별 페이지에서는 여러 소단원 근거를 종합·재구성해도 된다고 완화함(소단원 원고처럼 근거 그대로만 쓰라고 하면 지나치게 좁아짐).
- 검증: 대단원 도입(스포츠 인문 문화, 참신·활동형)을 실제로 생성해 확인 — 인용·자기지시 문장 없이, 질문으로 시작하는 자연스러운 도입문이 나옴("스포츠를 움직이는 몸, 스포츠를 읽는 눈" 등). 전체 자동 테스트 67개 통과.
- 수정 파일: `static/prototype-draft-engine.js`, `static/prototype.html`(캐시 버전), `app.py`.
- 다음 시작점: 없음(요청한 범위는 모두 AI 경로에 연결됨). 남은 건 사용자가 브라우저에서 여러 특별 페이지 유형(읽을거리·인물진로 등)을 직접 생성해 품질을 눈으로 확인하는 것.

### 2026-08-19 · Vercel 배포본에서 "OpenAI API 키가 유효하지 않습니다" 트러블슈팅

- 상태: 진행 중
- 증상: 로컬 `.env.local`의 키는 OpenAI에 직접 요청해 유효함을 확인(HTTP 200)했고, 같은 값을 Vercel `OPENAI_API_KEY`(Production)에 등록·Save·Redeploy까지 했는데도 배포본에서는 계속 401 "유효하지 않습니다"가 뜸.
- 점검 순서: (1) Vercel 목록에 `OPENAI_API_KEY`가 실제로 존재하는지 확인 — 처음엔 이름 칸에 값까지 같이 넣어 저장이 실패했던 것을 발견해 재입력함. (2) 이름을 올바르게 넣은 뒤 목록에 정상 등록됨을 확인. (3) 로컬 키의 유효성을 OpenAI `/v1/models`에 직접 요청해 확인(정상). (4) 그런데도 배포본에서 동일 오류 지속.
- 다음 가설: Vercel의 "Redeploy"가 기존 빌드/캐시를 재사용해 새 환경변수를 읽지 않았을 가능성. 이를 배제하기 위해 이 커밋으로 완전히 새 배포를 트리거함.
- 다음 시작점: 이 커밋 배포 완료 후에도 같은 오류가 나면, Vercel 프로젝트의 "Production Branch" 설정이 `main`인지, 그리고 실제 접속 중인 URL이 그 프로덕션 배포인지(프리뷰 배포로 잘못 들어간 게 아닌지) 확인이 필요함.
- **원인 확정**: Vercel 로그의 "Deployment Information → Deployment ID"를 확인해보니, 사용자가 실제로 접속하던 URL이 `textbook-studio-<임의해시>-shinyk.vercel.app` 형태의 **배포 고정 URL**이었음. 이 URL은 그 배포 하나에 영구히 고정되어, 이후 몇 번을 재배포해도 절대 바뀌지 않음. 랜덤 해시 없는 정식 도메인(`textbook-studio.vercel.app`)으로 접속하니 해결됨. 키·환경변수·재배포 절차는 전부 처음부터 정상이었음.

### 2026-08-19 · 펼침면 삽화 레이아웃도 고정 틀에서 AI 자율 구성으로 전환

- 상태: 완료
- 배경: 배포 문제 해결 후 사용자가 화면 하단에 항상 3개씩 뜨는 시각자료 박스도 "일괄 틀"이라고 지적함. 실제 전처리한 미래엔 교과서 펼침면 콘택트시트를 눈으로 확인한 결과, 실제 교과서는 펼침면마다 삽화 구성이 크게 다름(전면 아이소메트릭 일러스트 한 장인 페이지, 작은 사진 여러 개가 흩어진 페이지, 삽화 없이 텍스트·표만 있는 페이지 등)을 확인해 지적이 타당함을 검증함.
- 구현: `app.py`의 `sports_culture_manuscript_schema()`에서 고정 `visual_briefs`(펼침면당 2~3개 문자열) 스키마를 없애고, 페이지별(`left_visuals`/`right_visuals`, 각각 0~3개) 배열로 교체. 각 항목은 `size`(full/half/small), `placement`(top/bottom/left/right/background/inline), `description`을 가짐. 프롬프트에 "실제 교과서는 펼침면마다 삽화 구성이 다르다 — 개념설명 페이지는 전면 삽화 하나 또는 삽화 없음, 사례 여러 개를 보여주는 페이지는 작은 컷 여러 개일 수 있다. 내용에 맞게 개수·크기·배치를 직접 판단하라"는 규칙을 추가함. 텍스트 원고를 쓰는 같은 호출 안에서 함께 결정하므로, 삽화 구성이 실제 그 펼침면의 내용과 맞물려 나옴.
- 클라이언트(`prototype-draft-engine.js`)는 `visualBriefs` 대신 `visuals: {left, right}` 형태로 매핑. 화면(`prototype.js`/`prototype.css`)은 새 유연한 렌더러(`spread-visual-flex`, 크기별 `size-full/half/small` 클래스)를 추가하고, 기존 규칙 기반 내부 조합기(`internal-rules-v1`)의 고정 3박스 출력은 하위 호환을 위해 그대로 유지(구분은 `manuscript.visuals` 존재 여부로 분기).
- 검증: 실기형(배드민턴)·개념형(스포츠 문화의 의미와 형성) 두 소단원을 실제로 생성해 확인 — 왼쪽 절반+작은컷, 오른쪽 절반 2개처럼 고정 3개가 아닌 구성이 나왔고, 삽화 설명이 실제 그 펼침면 내용(관념문화, 참여자 역할 관계도 등)과 맞아떨어짐. 다만 샘플이 2건뿐이라 "전면 1개" "삽화 없음" 같은 더 극단적인 다양성은 아직 직접 보지 못함 — 실제 사용 중 다양한 소단원에서 계속 확인 필요. 전체 자동 테스트 67개 통과.
- 수정 파일: `app.py`, `static/prototype-draft-engine.js`, `static/prototype.js`, `static/prototype.css`, `static/prototype.html`(캐시 버전).
- 다음 시작점: (1) 사용자가 브라우저에서 여러 소단원에 걸쳐 실제로 삽화 구성이 다양하게 나오는지 확인. (2) 이전에 논의한 "실제 이미지 생성"(현재는 설명 텍스트만 있는 자리)을 이 유연한 레이아웃 위에 얹을지는 별도 결정 필요.

### 2026-08-19 · 삽화 실제 이미지 생성(옵션) 추가, TXT 내보내기 옛 구조 버그 발견·수정

- 상태: 완료
- 배경: 사용자가 실제 생성 결과를 보고 "이미지는 왜 안 나오고 글자·큰 네모 박스만 있냐"고 질문 — 지난 세션에서 레이아웃 구조(개수·크기)만 유연하게 만들고, 그 자리를 실제 이미지로 채우는 건 구현하지 않았던 상태였음. 사용자가 이미지 생성을 구현하되 **켜고 끌 수 있는 옵션**으로 만들 것을 요청함(비용 때문).
- 서버(`app.py`): `call_openai_for_sports_culture_image()`(+`call_prototype_sports_culture_image()`) 신설, OpenAI 이미지 생성(`POST /v1/images/generations`, 모델 `gpt-image-2`)을 호출해 `b64_json`을 받아 반환. 삽화 크기(`full`/`half`→`1536x1024`, `small`→`1024x1024`)에 따라 해상도를 다르게 요청해 비용을 통제함. 프롬프트에 "실제 인물을 닮게 그리지 말 것, 글자를 그리지 말 것"을 명시함(이미지 생성 모델이 텍스트를 정확히 못 그리는 한계 때문). 새 엔드포인트 `POST /api/prototype/sports-culture-image`는 기존 원고 생성 엔드포인트와 동일한 로그인·오류 처리 패턴을 따름.
- 클라이언트(`prototype-draft-engine.js`): `externalAiGenerate`에 `request.includeImages`가 true일 때만 실행되는 `fillVisualImages()`를 추가. 원고·레이아웃을 다 받은 뒤 모든 `left_visuals`/`right_visuals` 항목에 대해 이미지 생성을 병렬로 요청하고, 항목별로 성공하면 `imageBase64`를 붙이고 실패하면 그 항목만 문구 placeholder로 남긴다(한 이미지가 실패해도 전체 생성이 실패하지 않음).
- 화면(`prototype.js`): "생성 제공자" 옆에 **"삽화 이미지" 체크박스**를 추가(`state.includeImages`, 기본 꺼짐). 일괄 생성·단일 생성 양쪽 호출에 모두 전달. 렌더러(`renderSpreadVisualRow`)는 `imageBase64`가 있으면 실제 `<img>`로, 없으면 기존처럼 설명 문구 placeholder로 그린다.
- 부수 발견·수정: `textbookManuscriptText()`(교과서 원고 TXT 내보내기)가 "[사진·삽화·정보 그래픽 발주]" 목록을 여전히 옛 `manuscript.visualBriefs`(지난 세션에서 없어진 필드)로만 채워서, AI 생성 원고에서는 이 목록이 항상 빈 칸이었음(사용자가 "TXT·PPT가 최신 구조를 반영하냐"고 물어봐서 발견함). `manuscript.visuals` 유무로 분기해 새 구조(좌/우, 크기·배치·설명)도 출력하도록 수정.
- 확인했지만 이번엔 손대지 않은 것: **PPT 다운로드**는 지금의 교과서 원고(헤드라인·절·문단·삽화)를 전혀 쓰지 않고, 그보다 훨씬 오래된 "활동 카드" 구조(`activities`/`support_boxes`)를 그대로 쓰고 있음이 확인됨 — 사용자에게 보고했고, 다시 만들어야 한다는 데 동의했지만 아직 착수 전.
- 검증: 이미지 생성 엔드포인트를 직접 호출해 실제 PNG(약 1.6MB, 유효한 PNG 매직바이트)를 받았고, 육안으로 "배드민턴 복식 경기를 위에서 본 편집 삽화"가 실제로 설명과 맞게 그려진 것을 확인함. 전체 자동 테스트 67개 통과.
- 수정 파일: `app.py`, `static/prototype-draft-engine.js`, `static/prototype.js`, `static/prototype.css`, `static/prototype.html`(캐시 버전).
- 다음 시작점: (1) 사용자가 브라우저에서 체크박스 켜고 실제 배치 생성해서 비용·속도·품질 확인. (2) PPT 다운로드를 실제 원고 구조로 다시 만드는 작업.

### 2026-08-19 · 프로토타입 상태를 서버 DB에도 동기화(다른 컴퓨터에서 이전 초안이 안 보이던 문제)

- 상태: 완료
- 배경: 사용자가 다른 컴퓨터에서 프로토타입을 열었더니 이전에 생성한 초안들이 안 보인다고 지적함. 확인해보니 지금까지 이 프로토타입의 모든 상태(`projectStore`: 프로젝트·단원·초안 생성 이력 등)는 브라우저 `localStorage`에만 저장되고 있어서, 다른 컴퓨터·다른 브라우저에서는 항상 빈 상태로 시작하는 게 원래 설계였음(사용자에게 원인을 설명한 뒤, 서버 DB 동기화로 개선하기로 진행함).
- 서버(`app.py`): 운영 서비스의 `workflow_stages`(단일 공유 레코드 + 낙관적 잠금 버전 관리) 패턴을 그대로 따라 `prototype_state` 테이블(`id=1` 고정 행, `payload`, `version`, `updated_at`)을 신설. `GET /api/prototype/state`(현재 저장값 조회, 없으면 `payload:null`)와 `POST /api/prototype/state`(저장, `expectedVersion`이 최신과 다르면 409 반환)를 추가. 편집자 여러 명이 동시에 쓰는 게 아니라 한 사람이 여러 기기에서 쓰는 상황을 가정해 프로젝트별이 아닌 단일 공유 레코드로 둠.
- 클라이언트(`prototype.js`): `loadProjectStore()`의 마이그레이션·정합성 검사 로직을 `hydrateProjectStorePayload()`로 분리해 localStorage 값과 서버 값 양쪽에 재사용. 페이지 로드 시 `syncProjectStoreFromServer()`를 호출해 서버에 더 최신 데이터가 있으면 그걸로 덮어써서 다시 그린다. `persist()`가 호출될 때마다(거의 모든 편집 동작마다 호출됨) 2초 디바운스로 `POST /api/prototype/state`를 예약해, 매 입력마다 서버에 쏘지 않고 편집이 멈춘 뒤 한 번만 저장한다. 저장이 409로 충돌하면 서버의 최신값을 다시 불러와 덮어쓰고 토스트로 안내한다(복잡한 실시간 병합은 하지 않음 — 한 사람이 순차적으로 여러 기기를 쓰는 상황을 기준으로 설계함). localStorage는 그대로 유지해 오프라인일 때도 즉시 저장·복구되게 함.
- 검증: 로컬 서버에 직접 `GET`(초기엔 빈 값) → `POST`(저장) → `GET`(같은 값 재확인) → 낡은 `expectedVersion`으로 `POST`(409 충돌 정상 반환)까지 실제로 호출해 확인함. 전체 자동 테스트 67개 통과.
- 수정 파일: `app.py`, `static/prototype.js`, `static/prototype.html`(캐시 버전).
- 다음 시작점: 사용자가 실제로 컴퓨터 A에서 저장 후 컴퓨터 B에서 열어 최신 초안이 보이는지 확인.

### 2026-08-19 · 대단원 도입·마무리·특별페이지 초안 생성 시 응답 시간 초과 수정

- 상태: 완료
- 배경: 사용자가 "단원 도입·마무리·특별페이지 초고" 생성을 눌렀더니 "초고 구성중…" 상태로 있다가 "OpenAI 응답 제한 시간(50초)을 초과했습니다" 오류로 계속 실패한다고 지적함.
- 원인 진단: 로컬 서버에 실제 OpenAI 호출을 직접 타이밍해 확인함. 소단원 본문(펼침면 1개)은 약 22초가 걸려 지금까지는 50초 한도 안에 들어왔지만, 단원 도입·마무리·특별페이지는 4쪽(펼침면 2개)을 한 번의 OpenAI 요청에 몰아서 보내고 있어서 같은 요청이 약 45초가 걸렸다. 여기에 Vercel 배포 환경의 네트워크 지연이 더해지면 서버 쪽 50초 제한(`urlopen(..., timeout=50)`, `vercel.json`의 `maxDuration: 60`에 맞춘 값)을 넘기게 되어 매번 실패했다. 즉 이번 서버 동기화 작업과는 무관하게, 펼침면 2개 이상을 한 번에 요청하는 경로에 원래부터 있던 여유 부족 문제였다.
- 수정: `static/prototype-draft-engine.js`의 `externalAiGenerate`에서 소단원 본문·특별페이지 두 경로 모두, 여러 펼침면을 한 번의 `/api/prototype/sports-culture-manuscript` 요청에 묶어 보내던 것을 펼침면마다 별도 요청으로 나눠 `Promise.all`로 병렬 호출하도록 바꿈. 서버(`app.py`)는 이미 요청받은 펼침면 수에 맞춰 스키마를 만들므로 별도 수정이 필요 없었다.
- 검증: 로컬 서버에 같은 2펼침면 요청을 병렬 2건의 1펼침면 요청으로 나눠 직접 호출해 총 소요 시간이 약 45초 → 약 25.6초로 줄어드는 것을 확인함(50초 한도 대비 여유 확보). 전체 자동 테스트 67개 통과.
- 수정 파일: `static/prototype-draft-engine.js`, `static/prototype.html`(캐시 버전).
- 다음 시작점: 사용자가 실제 배포본에서 단원 도입·마무리·특별페이지 초안 생성이 정상적으로 완료되는지 확인.

### 2026-08-19 · "이미지 포함" 켜고 초안 생성 시 "fail to stream" 오류 수정

- 상태: 완료
- 배경: 바로 앞 항목에서 응답 시간 초과를 고친 뒤에도, 사용자가 "이미지 포함"을 켠 상태로 초안 생성을 시도하면 "fail to stream…"이라는(우리가 작성한 한국어 오류 문구가 아닌, 브라우저·플랫폼 쪽에서 나오는 원문 그대로로 보이는) 오류로 계속 실패한다고 지적함.
- 원인 진단: `static/prototype-draft-engine.js`의 `fillVisualImages`가 한 펼침면당 최대 6개(좌우 각 최대 3개)의 삽화 이미지 요청을 전부 `Promise.all`로 동시에 쏘고 있었다. 이번에 응답 시간 초과를 고치며 펼침면별 원고 요청은 이미 나눠 병렬화했으므로, 단원 도입·마무리·특별페이지(펼침면 2개)에서는 이미지 요청만 최대 12건이 한꺼번에 동시에 나갈 수 있었다. 이미지 1건은 로컬 실측으로도 약 16초·약 2.3MB가 걸리는 무거운 요청이라, 12건을 동시에 보내면 배포 환경(Vercel)에서 동시 실행·요청 수 제한에 걸려 연결이 중간에 끊기고, 그 결과가 우리 서버의 정상적인 JSON 오류가 아니라 브라우저가 있는 그대로 보여주는 저수준 네트워크 오류("fail to stream")로 나타난 것으로 판단된다.
- 수정: `fillVisualImages`를 전체 이미지 요청을 한꺼번에 병렬 실행하던 방식에서, 동시에 최대 3건만 처리하는 워커 풀(worker pool) 방식으로 바꿨다. 전체 이미지 개수나 완료 여부는 그대로지만, 한 번에 나가는 요청 수가 줄어 배포 환경의 동시 실행 제한에 덜 걸리게 된다.
- 검증: `tests/test_prototype_draft_engine.py`에 `test_external_ai_provider_throttles_parallel_image_requests`를 추가함 — `fetch`를 모킹해 원고 생성 2회·이미지 생성 12회가 실제로 일어나고, 동시에 진행 중인 이미지 요청이 3건을 넘지 않는지 직접 검증한다. 전체 자동 테스트 68개 통과.
- 수정 파일: `static/prototype-draft-engine.js`, `static/prototype.html`(캐시 버전), `tests/test_prototype_draft_engine.py`.
- 남은 문제: "fail to stream"이 정확히 어느 플랫폼 계층(브라우저 네트워크 계층 vs Vercel 함수 동시 실행 제한)에서 나온 문구인지 로그로 직접 확인하지는 못했다. 동시성을 3으로 낮춘 것이 실제로 재현 오류를 없애는지는 사용자의 실제 배포본 재시도로 확인이 필요하다. 이미지가 많은 지면(최대 12장)은 순차 처리로 인해 전체 생성 시간이 늘어날 수 있다(워커 3개 기준 최대 약 4배).
- 다음 시작점: 사용자가 배포본에서 "이미지 포함"을 켠 채로 단원 도입·마무리·특별페이지 초안 생성을 다시 시도해 정상 완료되는지, 그리고 소요 시간이 감내할 만한지 확인.
