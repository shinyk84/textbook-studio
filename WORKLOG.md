# 작업 세션 기록

이 문서는 세션별 변경 이유와 결과를 시간순으로 남기는 기록이다. 현재 상태의 요약은 `HANDOFF.md`를 본다.

## 기록 방법

- 사용자가 “작업 시작”을 알리면 아래 템플릿으로 새 항목을 맨 위의 ‘진행 세션’ 아래에 만든다.
- 사용자가 “작업 종료”를 알리면 실제 결과와 검증을 채우고 ‘완료 세션’으로 옮긴다.
- 대화 내용 전체를 옮기지 않고, 다음 사람이 판단을 이어갈 수 있는 요구·결정·수정·검증만 기록한다.
- 비밀값은 기록하지 않는다.

### 세션 템플릿

```markdown
## YYYY-MM-DD HH:MM · 작업 제목

- 상태: 진행 중 | 완료 | 중단
- 시작 요청:
- 작업 목적:
- 수행 방법·판단:
- 수정 파일:
- 검증:
- Git:
- 남은 문제:
- 다음 시작점:
```

## 진행 세션

현재 진행 중인 세션 없음.

## 완료 세션

### 2026-07-30 · GitHub 병합과 Vercel 최종 배포

- 상태: 완료
- 시작 요청: 미리보기 확인 후 최종 배포 요청.
- 작업 목적: 검증된 `agent/supabase-collaboration` 변경을 정식 `main` 브랜치와 운영 사이트에 반영.
- 수행 방법·판단: Vercel Preview 검사 성공과 GitHub 병합 가능 상태를 확인하고 PR #1을 Draft에서 준비 상태로 전환한 뒤 merge commit 방식으로 병합함. Vercel Git 연동이 시작한 Production 배포의 상태와 정식 별칭을 확인함.
- 수정 파일: 이 배포 결과를 기록한 `WORKLOG.md`.
- 검증: PR #1 병합 커밋 `e07caf6`; Production 배포 `dpl_HgiTbE9oYqMyodjANXo7AXiuwzqm` Ready; `https://textbook-studio.vercel.app/api/health` HTTP 200; 정식 첫 화면 HTTP 200; 로그인 전 `/api/bootstrap` HTTP 401로 인증 보호 확인.
- Git: `main` 브랜치에 병합 완료. 이 기록 커밋을 추가 푸시한 뒤 최종 Production 상태를 다시 확인.
- 남은 문제: 없음.
- 다음 시작점: 정식 사이트에서 편집자 로그인 후 공식자료 5종과 기존 프로젝트 데이터 확인.

### 2026-07-30 · 발표용 페이지와 재생성 파일 정리

- 상태: 완료
- 시작 요청: PT용으로 만들었던 파일을 삭제하고 그 외 불필요한 파일도 정리 요청.
- 작업 목적: 실제 교과서 제작·공동 편집에 필요한 파일만 남겨 저장소와 배포 구성을 단순화.
- 수행 방법·판단: 발표용 `/ai-journey` HTML·CSS·JavaScript, 로그인 연결 링크·전용 스타일·서버 라우트를 제거. 추가 정리는 다시 생성되는 Python 캐시 4곳과 내용 없는 서버 로그 2개로 제한함.
- 보존 항목: 전처리 데이터, 원본 연동 구조, `data/studio.db`, 백업 DB, `.env.local`, `.vercel`, HWPX 템플릿.
- 수정·삭제 파일: `static/journey.*`, `static/login.html`, `static/login.css`, `app.py`, `HANDOFF.md`, `WORKLOG.md`; 로컬 `__pycache__`와 빈 로그.
- 검증: 발표용 경로·파일·스타일 참조 0건. 전체 자동 테스트 33개, Python·JavaScript 구문 검사, `git diff --check` 통과. 테스트가 다시 만든 Python 캐시도 재삭제함.
- Git: `agent/supabase-collaboration` 브랜치의 `5c8d18c` 커밋에 포함하여 `origin/agent/supabase-collaboration`로 푸시 완료.
- 남은 문제: 없음.
- 다음 시작점: 실제 제작 화면과 공동 편집 흐름만 유지하며 계속 검증.

### 2026-07-30 · Git pull만으로 전처리 자료 사용

- 상태: 완료
- 시작 요청: 다른 컴퓨터에서 Git pull 후 전처리 데이터가 없다는 문제를 해결하고 사용하기 쉬운 구조로 변경 요청.
- 작업 목적: 저장소를 받은 어느 컴퓨터에서도 별도 폴더 복사 없이 공식 전처리 자료 5종을 즉시 사용.
- 수행 방법·판단: 원본 PDF 45MB는 저장소에서 제외하고, 실행에 필요한 전처리 산출물 약 8MB만 `official-data/processed`에 포함. 원본이 있으면 SHA-256을 검증하고, 없으면 `processed_only` 상태로 구분해 제작 기능을 허용함.
- 수정 파일: `official-data/**`, `scripts/sync_official_data.py`, `app.py`, `static/app.js`, `vercel.json`, `README.md`, `HANDOFF.md`, `WORKLOG.md`, 테스트.
- 검증: 5종·1,213쪽·3,973청크 동기화 완료. 개인 컴퓨터 절대경로 0건 확인. 원본 없는 환경에서 5종 모두 `processed_only`로 사용 가능함을 모사. 현재 환경에서 원본 해시 5종 확인 및 성취기준 49개 로드. 전체 자동 테스트 33개 통과, Python·JavaScript 구문 및 `git diff --check` 통과.
- Git: `agent/supabase-collaboration` 브랜치의 `5c8d18c` 커밋에 포함하여 `origin/agent/supabase-collaboration`로 푸시 완료.
- 남은 문제: 원본 PDF 대조는 `criteria`를 별도로 둔 컴퓨터에서만 가능.
- 다음 시작점: 새 컴퓨터에서 pull 후 `start-studio.cmd` 실행 → 공식자료 5종 표시 확인.

### 2026-07-30 · Git push 전 작업 기록 자동화

- 상태: 완료
- 시작 요청: 사용자가 “깃푸시해줘”라고 요청할 때 이전까지의 작업 내용을 자동으로 핸드오프에 남기도록 요청함.
- 작업 목적: 별도 지시 없이도 푸시 전에 작업 맥락을 기록하고, 기록 없는 코드 푸시를 차단함.
- 수행 방법·판단: AI 작업 규칙은 대화와 diff를 읽어 의미 있는 기록을 작성하도록 `AGENTS.md`에 명시함. Git Hook은 작업 의미를 추측하지 않고 `WORKLOG.md` 누락만 검사하는 안전장치로 분리함.
- 수정 파일: `.githooks/pre-push`, `setup-hooks.cmd`, `AGENTS.md`, `HANDOFF.md`, `WORKLOG.md`
- 검증: `pre-push` 구문 확인. 기록 없이 코드 변경이 있는 과거 커밋 범위는 종료 코드 1로 차단하고, 변경이 없는 범위는 종료 코드 0으로 통과함을 확인. 현재 저장소의 `core.hooksPath=.githooks` 설정 완료.
- Git: `ba02d86 Add durable handoff and push logging` 커밋을 `origin/agent/supabase-collaboration`에 푸시 완료.
- 남은 문제: 새 컴퓨터에서는 `setup-hooks.cmd`를 한 번 실행해야 로컬 Hook이 활성화됨.
- 다음 시작점: 이후 “깃푸시해줘” 요청부터 별도 설명 없이 작업 기록 갱신 → 검증 → 커밋 → 푸시 순서로 처리.

### 2026-07-30 · 지속형 핸드오프 체계 도입

- 상태: 완료
- 시작 요청: 컴퓨터·대화창이 바뀌고 Git push/pull을 해도 작업 맥락이 이어지도록 처음부터 현재까지의 핸드오프와 향후 세션 기록 체계를 요청함.
- 수행 방법·판단: 전처리 5종의 매니페스트·보고서, 저장소 README, 현재 코드, Git 커밋 이력을 대조함. 추정 내용은 배제하고 확인 가능한 사실과 현재 미해결 문제를 기록함.
- 수정 파일: `AGENTS.md`, `HANDOFF.md`, `WORKLOG.md`, `README.md`
- 검증: 민감정보 값 미기록, 전처리 문서 수·쪽수·청크 수·경고 수 대조, 브랜치·원격 커밋 대조.
- Git: `ba02d86` 커밋에 포함하여 `origin/agent/supabase-collaboration`에 푸시 완료.
- 남은 문제: OpenAI API 쿼터 부족으로 실제 원고 생성이 아직 성공하지 않음.
- 다음 시작점: 사용자가 다음 “작업 시작”을 알리면 이 문서에 세션을 만들고 작업을 이어감.

### 2026-07-27~30 · AI 원고 생성과 선택 소단원 로딩

- 상태: 완료(실제 생성은 외부 쿼터 문제로 보류)
- 시작 요청: 반복적인 고정 문구를 줄이기 위해 실제 AI를 연결하고, 전체 원고 대신 원하는 소단원만 표시하도록 요청함.
- 수행 방법·판단: OpenAI Responses API와 JSON Schema 구조화 출력을 서버에서 호출하도록 구성. 브라우저에는 소단원 카탈로그만 보내고, 선택한 소단원의 펼침면만 별도 API로 불러오도록 분리함.
- 수정 내용: 학년·대단원·중단원·소단원 선택기, 선택 소단원 저장·AI 생성·HWPX 출력, 전체 원고 초기 전송 축소, 구체적인 한국어 API 오류 안내.
- 검증: 전체 자동 테스트 30개 통과. 키 인식과 OpenAI 도달 확인. 실제 호출 결과는 `429 quota/billing`.
- Git: 관련 코드는 `aee2f9a` 이후 커밋들에 포함되어 원격 브랜치에 반영됨.
- 남은 문제: API 결제/크레딧 설정 후 생성 품질 확인 필요.
- 다음 시작점: 크레딧 반영 후 소단원 1건을 생성하고 반복도·활동 다양성·쪽수 보존을 확인.

### 2026-07-27 · 공동 편집·배포·로그인 안정화

- 상태: 완료
- 시작 요청: 코드는 우선 혼자 수정하되, 배포 후 여러 교과서 편집자가 공동으로 사용하고 데이터를 누적하도록 요청함. 별도 서버형 호스팅 없이 무료 계정 중심을 원함.
- 수행 방법·판단: GitHub는 코드 이력, Vercel은 웹 배포, Supabase는 영속 데이터와 인증을 담당하도록 분리함. 편집자 승인 목록과 버전 충돌 방지를 추가함.
- 수정 내용: Vercel Python Function, Supabase Postgres/Auth, 편집자 관리, 로그인·회원가입·비밀번호 재설정, SQLite→Postgres 이전 스크립트.
- 검증: GitHub 인증·푸시, Vercel 연결, Supabase 리디렉션 설정, 로그인 오류 수정.
- Git: `7b28a6d`, `1e584cb`, `333d05e`.
- 남은 문제: 새 배포 주소가 생기면 Supabase Redirect URL 재확인.
- 다음 시작점: 실제 편집자 계정 2개로 동시 편집·충돌 동작을 수동 검증.

### 2026-07-27 · HWPX 출력과 진행 표시

- 상태: 완료
- 시작 요청: 분석과 원고 초안을 HWP/HWPX로 내보내고, 긴 처리 중 멈춘 것처럼 보이지 않도록 진행 상황을 표시해 달라고 요청함.
- 수행 방법·판단: 빈 HWPX 템플릿을 기반으로 순수 Python에서 문서를 구성하고, 표 중심 출력과 선택 소단원 출력을 추가함. API 요청을 감싼 공통 진행 오버레이를 적용함.
- 수정 내용: 전체 단계별 HWPX 다운로드, 표 가독성, 선택 소단원 출력, 처리 퍼센트·진행 막대.
- 검증: HWPX ZIP 패키지 구조·본문 추출 자동 검사 및 한글 2024 열기 확인.
- Git: `a303ab7`, `aee2f9a`, `dfb3a52`.
- 남은 문제: 실제 편집팀 양식에 맞춘 스타일 세부 조정.
- 다음 시작점: 회사별 표 스타일과 원고 전달 양식을 받아 HWPX 템플릿을 조정.

### 2026-07-26~27 · 1~9단계 웹서비스 구현

- 상태: 완료
- 시작 요청: 교육과정 분석부터 원고 초안까지 각 단계에서 AI가 첫 안을 만들고 편집팀이 웹에서 수정하며, 이전 단계도 다시 고칠 수 있는 서비스를 요청함. 자동검증·최종검토는 독립 모의심사처럼 동작해야 함.
- 수행 방법·판단: 제작 흐름을 1~9단계로 나누고, 각 단계에 웹 편집·버전·승인 게이트를 연결함. 모의심사는 제작 데이터와 공식자료는 공유하되 평가 판단은 분리함.
- 주요 수정: 프로젝트 설정, 교육과정 분석, 개발 방향, 성취기준·차시 배분, 내용·종목 선정, 목차·분량, 단원 설계, 원고 초안, 자동검증·모의심사.
- 편집 규칙: 영역→대단원, 세부 영역→부제, 신체활동 예시→중·소단원 토대. 학년 간 공통 종목 선정 금지. 중·소단원 카드 복제와 중복 허용.
- 사용성 수정: 전체 글자 확대, 단계 제목 크기 통일, 중복 학년 UI 제거, 선택 상태 의미 명확화.
- 원고 수정: 한 쪽 단위가 아니라 좌우 펼침면, 펼침면당 활동 2~3개, 안전·전략·평가·정리 구성.
- Git: 초기 구현 `950c73a`, 이후 확장 `aee2f9a`.
- 다음 시작점: 실제 편집 업무로 반복 사용하며 화면·생성 규칙을 계속 보정.

### 2026-07-16 · 공식 PDF 5종 전처리

- 상태: 완료
- 시작 배경: 교육과정과 평가원 자료를 이후 질문과 웹서비스의 공통 근거 데이터로 사용하기 위해 PDF 전처리를 먼저 수행함.
- 원본: `../criteria`의 체육과 교육과정, 초등학교 교육과정, 체육 편찬상의 유의점 및 검정기준, 검정도서 개발 가이드라인, 편수자료 II.
- 수행 방법·판단: PyMuPDF 기본 텍스트·표 추출을 우선 사용하고, 기본 추출이 어려운 평가원 가이드라인 11쪽은 OCR 처리. 깨진 한글 매핑 복원, 머리말 제거, 제목 계층·표·원본 페이지를 구조화함.
- 산출물: 자료별 `document.md`, `chunks.jsonl`, `manifest.json`, `review.csv`, `report.md`.
- 검증: 원본 SHA-256 기록, 총 1,213 PDF쪽과 3,973개 청크 생성, 경고 페이지는 `review.csv`에 보존.
- 수정 파일: `../preprocess_criteria.py`, `../processed/**`.
- 남은 문제: 경고·OCR 페이지를 원본과 사람이 계속 대조해야 함.
- 다음 시작점: 전처리를 다시 돌리기보다 기존 산출물을 우선 검색하고, 원본과 불일치가 발견될 때만 해당 범위를 교정.
